# 메모리 승격 다이제스트 — trpc/trpc
> 생성일: 2026-05-22 · promote v0.6.0

## 설정

- Provider: `openai` (embeddings + HAC + llmRefine)
- Models: `gpt-4.1-mini` (classify), `gpt-4.1-nano` (draft), `text-embedding-3-small` (embed)
- Output language: `ko`
- Thresholds: similarity `0.8`, min confidence `0.75`, min occurrences `2`
- Window: `90` days (overridden via --since; config default is 60)
- Privacy: redactSecrets=`true`, sendDiffHunksToLLM=`false`

## 요약

- Scanned: **423** review comments across **62** PRs
- AI reviewer comments: **361**
- Clusters found: **274**
- Repeated clusters: **45**
- Promotion candidates: **21**
- Estimated cost: $0.0595
- Timings: fetch 6.2s · normalize 26ms · cluster 43.8s · conv-fetch 6.2s · reply-ctx 7.2s · memory 960ms · classify+draft 1m 20s · **total 2m 25s**

---

## 1. www/scripts/check-twoslash.ts 파일 내에서 인덱스나 캡처 접근 시 non-null assertion 연산자(!) 사용을 제거하고 명시적 undefined 검사 또는 optional chaining으로 대체해야 한다.

- **ID**: `candidate_001`
- **Target**: `path_scoped_rule` → `www/scripts/CODING_GUIDELINES.md`
- **Confidence**: 0.95
- **Scope**: 🌐 cross-PR (3 PRs)
- **Occurrences**: 3 comments
- **Path scope**: `www/scripts/**`

### 근거

- PR #7196 `www/scripts/check-twoslash.ts` — "⚠️ Potential issue | 🟠 Major

Remove non-null assertions from indexed/capture a..." [link](https://github.com/trpc/trpc/pull/7196#discussion_r2869759038)
- PR #7176 `www/scripts/check-twoslash.ts` — "⚠️ Potential issue | 🟠 Major

Replace non-null assertions with guards/optional ..." [link](https://github.com/trpc/trpc/pull/7176#discussion_r2876202469)
- PR #7195 `www/scripts/check-twoslash.ts` — "⚠️ Potential issue | 🟡 Minor

Remove non-null assertions in this file to mainta..." [link](https://github.com/trpc/trpc/pull/7195#discussion_r2869717508)

### 제안된 패치

```markdown
---
applyTo: "www/scripts/**"
---
# www/scripts/check-twoslash.ts 파일 내 규칙
- 인덱스 또는 캡처 접근 시 사용된 non-null assertion(`!`) 연산자를 제거하고, 명시적 `undefined` 검사 또는 선택적 체이닝(`?.`)으로 대체하세요.
- 48, 58, 67, 148행의 `!` 연산자는 null 체크를 강제하는 것으로, 이를 적절한 조건문 또는 선택적 체이닝으로 수정하세요.
- null 또는 undefined 가능성을 고려하여 안전하게 접근하는 패턴을 사용하여, 잠재적 런타임 오류를 방지하세요.
- 해당 파일 내에서만 적용되는 규칙이므로, 다른 파일에는 영향을 미치지 않도록 주의하세요.
- 코드 가독성과 안전성을 높이기 위해, non-null assertion 사용을 지양하고 명시적 검사를 우선시하세요.
```

### 이 대상을 선택한 이유

이 패턴은 www/scripts/check-twoslash.ts 파일에 국한된 코딩 가이드라인 위반 사항으로, 특정 경로에 적용되는 규칙으로 분류하는 것이 적절하다. 여러 PR에서 반복적으로 지적되었으며, 이는 전사적 규칙보다는 해당 파일 또는 경로에 특화된 규칙이다.

### 고려된 대안

- `agents`: 만약 non-null assertion 사용 금지가 전사적 코딩 컨벤션이라면 agents 문서에 포함할 수 있다.
- `pr_only`: 각 PR 별로만 의미가 있는 일회성 지적일 경우.

### 실행 명령어

```bash
promote candidate_001 --target path_scoped_rule
promote ignore candidate_001 --reason "..."
promote snooze candidate_001 --days 30
```

---

## 2. `@ts-expect-error` 지시자에 대해 ESLint가 요구하는 3자 이상의 설명을 자동 생성 코드에 포함시키는 규칙

- **ID**: `candidate_003`
- **Target**: `path_scoped_rule` → `eslint.config.js`
- **Confidence**: 0.95
- **Scope**: 🌐 cross-PR (2 PRs)
- **Occurrences**: 4 comments
- **Path scope**: `packages/openapi/test/routers/**/client/client.gen.ts`

### 근거

- PR #7231 `packages/openapi/test/routers/superjsonRouter-heyapi/client/client.gen.ts` — "⚠️ Potential issue | 🟡 Minor

Add description to `@ts-expect-error` directive.
..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128597)
- PR #7283 `packages/openapi/test/routers/cyclicTypesRouter-heyapi/client/client.gen.ts` — "⚠️ Potential issue | 🟡 Minor

Add description to `@ts-expect-error` directive.
..." [link](https://github.com/trpc/trpc/pull/7283#discussion_r2977549460)
- PR #7231 `packages/openapi/test/routers/appRouter-heyapi/client/client.gen.ts` — "⚠️ Potential issue | 🟡 Minor

Add description to `@ts-expect-error` directive.
..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128573)
- PR #7231 `packages/openapi/test/routers/errorFormatterRouter-heyapi/client/client.gen.ts` — "⚠️ Potential issue | 🟡 Minor

Add a reason to the `@ts-expect-error` or remove ..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128580)

### 제안된 패치

```markdown
---
applyTo: "packages/openapi/test/routers/**/client/client.gen.ts"
---
# `@ts-expect-error` 지시자 설명 규칙
- `@ts-expect-error` 주석에는 최소 3자 이상의 설명을 포함시켜야 합니다.
- ESLint 규칙에 따라, 자동 생성된 클라이언트 코드 내에서 이 규칙이 일관되게 적용되도록 합니다.
- 설명은 해당 오류 또는 경고가 발생하는 이유를 명확히 기술해야 합니다.
- `eslint-disable-next-line` 또는 유사한 패턴과 함께 사용하여 가독성을 유지합니다.
- 코드 생성기 또는 자동화 도구에 이 규칙이 반영되도록 설정을 검토합니다.
```

### 이 대상을 선택한 이유

이 패턴은 여러 자동 생성된 클라이언트 코드 파일에서 반복적으로 나타나며, ESLint 규칙에 따라 `@ts-expect-error` 주석에 설명을 반드시 포함해야 한다는 점이 공통적이다. 이는 특정 경로 내 자동 생성 코드에만 적용되는 규칙으로, 향후 자동 생성기 설정이나 코드 생성 프로세스 개선에 도움이 되므로 path_scoped_rule로 분류했다.

### 고려된 대안

- `agents`: 만약 이 규칙이 모든 코드베이스에 걸쳐 적용된다면 repo-wide AI instruction에 포함할 수 있다.
- `pr_only`: 각 PR에서만 개별적으로 적용되는 일회성 수정이라면 pr_only가 적합하다.

### 실행 명령어

```bash
promote candidate_003 --target path_scoped_rule
promote ignore candidate_003 --reason "..."
promote snooze candidate_003 --days 30
```

---

## 3. 레포지토리 내 pnpm 버전 불일치 문제로 인해 asdf/rtx 환경에서 설치 재현성이 깨질 수 있으므로, .tool-versions와 package.json의 pnpm 버전을 일치시켜야 한다.

- **ID**: `candidate_006`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.95
- **Scope**: 🌐 cross-PR (2 PRs)
- **Occurrences**: 2 comments
- **Path scope**: `.tool-versions`

### 근거

- PR #7194 `.tool-versions` — "The repo declares pnpm 9.x via package.json (engines/packageManager), but .tool-..." [link](https://github.com/trpc/trpc/pull/7194#discussion_r2869277485)
- PR #7200 `.tool-versions` — "`.tool-versions` still pins `pnpm 8.15.5`, but the repo’s root `package.json` de..." [link](https://github.com/trpc/trpc/pull/7200#discussion_r2872405266)

### 제안된 패치

```markdown
## pnpm 버전 일치 정책

- `.tool-versions` 파일 내 pnpm 버전과 `package.json`의 `packageManager` 또는 `engines.pnpm` 버전을 반드시 동일하게 유지하세요.
- `.tool-versions`에 명시된 pnpm 버전이 최신 또는 프로젝트 요구 사항에 맞게 업데이트되었는지 정기적으로 확인하세요.
- 버전 불일치가 발견되면 즉시 `.tool-versions` 또는 `package.json`을 수정하여 일치시켜 주세요.
- 이 정책은 레포지토리 전반에 걸쳐 일관된 도구 버전 관리를 위해 적용됩니다.
- 버전 불일치로 인한 재현성 문제를 방지하기 위해 PR 시 반드시 버전 일치 여부를 검증하세요.
```

### 이 대상을 선택한 이유

이 패턴은 레포지토리 전반에 걸쳐 적용되는 도구 버전 관리 정책에 관한 것으로, 여러 PR에서 반복적으로 지적되었으며, 향후 개발자 및 AI 에이전트가 일관된 개발 환경을 유지하는 데 도움이 되기 때문에 에이전트 지침 문서에 저장하는 것이 적합하다.

### 고려된 대안

- `pr_only`: 특정 PR에만 국한된 문제로, 일반화된 규칙이 아님
- `path_scoped_rule`: 특정 경로에만 적용되지만, 이 경우는 도구 버전 관리 정책으로 더 넓은 범위에 적용됨

### 실행 명령어

```bash
promote candidate_006 --target agents
promote ignore candidate_006 --reason "..."
promote snooze candidate_006 --days 30
```

---

## 4. 테스트 파일에서 beforeAll을 vitest에서 임포트하지 않으면 런타임 에러가 발생하므로, vitest에서 describe/it/expect와 함께 beforeAll을 반드시 임포트해야 한다.

- **ID**: `candidate_007`
- **Target**: `path_scoped_rule` → `packages/openapi/test/TESTING_GUIDELINES.md`
- **Confidence**: 0.95
- **Scope**: 🌐 cross-PR (2 PRs)
- **Occurrences**: 4 comments
- **Path scope**: `packages/openapi/test/**`

### 근거

- PR #7231 `packages/openapi/test/edgeCases.test.ts` — "`beforeAll` is used but not imported from Vitest, which will cause the test file..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2935826088)
- PR #7231 `packages/openapi/test/edgeCases.test.ts` — "`beforeAll` is used but not imported from Vitest, which will cause the test file..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2935826082)
- PR #7283 `packages/openapi/test/cyclicTypes.test.ts` — "`beforeAll` is used in this test but not imported from vitest, which will throw ..." [link](https://github.com/trpc/trpc/pull/7283#discussion_r2977455435)
- PR #7283 `packages/openapi/test/cyclicTypes.test.ts` — "`beforeAll` is used but not imported from vitest, so this test file will throw a..." [link](https://github.com/trpc/trpc/pull/7283#discussion_r2977503215)

### 제안된 패치

```markdown
---
applyTo: "packages/openapi/test/**"
---
# 패턴별 테스트 임포트 규칙
- `beforeAll`을 사용할 경우, 반드시 `vitest`에서 임포트해야 합니다.
- `describe`, `it`, `expect`와 함께 `beforeAll`을 임포트하는 것을 권장합니다.
- 임포트 누락 시 런타임 에러(ReferenceError)가 발생하므로 주의하세요.
- `vitest`에서 필요한 모든 테스트 훅을 명시적으로 임포트하는 습관을 가지세요.
```

### 이 대상을 선택한 이유

이 패턴은 테스트 파일 내에서 vitest 테스트 훅을 사용할 때 반드시 beforeAll을 임포트해야 한다는 구체적이고 반복적인 규칙으로, packages/openapi/test/ 경로에 국한되어 적용된다. 이는 테스트 관련 규칙이지만 특정 경로에 한정되어 있어 path_scoped_rule이 적합하다.

### 고려된 대안

- `test`: 테스트 관련 규칙이지만 이 경우는 특정 경로에 한정되어 있어 범용 테스트 규칙으로 보기는 어렵다.
- `agents`: 테스트 훅 임포트 규칙이지만 모든 테스트 파일에 적용되는 일반 규칙이 아니라 특정 경로에 집중되어 있다.

### 실행 명령어

```bash
promote candidate_007 --target path_scoped_rule
promote ignore candidate_007 --reason "..."
promote snooze candidate_007 --days 30
```

---

## 5. 패키지의 package.json에서 bin 필드에 명시된 실행 파일 경로가 files 배열에 포함되어 있지 않으면, 해당 실행 파일이 배포 시 누락되어 소비자에게 깨진 바이너리가 전달된다. 따라서 bin 디렉토리를 files에 반드시 포함시켜야 한다.

- **ID**: `candidate_012`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.95
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 10 comments

### 근거

- PR #7252 `packages/server/package.json` — "The package defines a `bin` entry (`./bin/intent.js`), but `bin/` is not include..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968125965)
- PR #7252 `packages/tanstack-react-query/package.json` — "The package exports an `intent` binary but `bin/` is not included in the `files`..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968125922)
- PR #7252 `packages/client/package.json` — "The package now exposes an `intent` binary, but `bin/` is not part of the `files..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968125972)
- PR #7252 `packages/next/package.json` — "The package declares `"bin": { "intent": "./bin/intent.js" }` but `bin/` is not ..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968125893)
- PR #7252 `packages/client/package.json` — "`bin.intent` points to `./bin/intent.js`, but there is no `packages/client/bin/`..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937362621)
- ... and 5 more

### 제안된 패치

```markdown
## 패키지 배포 규칙: bin 디렉토리 포함 필수

- `package.json`의 `bin` 필드에 명시된 실행 파일 경로(`./bin/`)는 반드시 `files` 배열에 포함시켜야 한다.
- `files` 배열에 `bin/` 디렉토리가 누락되면, 배포 시 해당 실행 파일이 누락되어 바이너리 깨짐 문제가 발생한다.
- `bin/` 디렉토리를 `files`에 추가하는 것을 잊지 말고, 배포 전 반드시 검증한다.
- 이 규칙은 모든 패키지에 일관되게 적용되어야 하며, 배포 관행의 표준으로 준수한다.
- 위 규칙 위반 시, 배포된 패키지의 바이너리 파일이 누락되어 사용자에게 문제가 발생할 수 있다.
```

### 이 대상을 선택한 이유

이 패턴은 여러 패키지에서 반복적으로 나타나는 배포 설정 관련 규칙으로, 모든 패키지에 적용되는 공통된 관례이다. 따라서 특정 PR에 국한되지 않고, 전체 저장소의 패키지 관리 및 배포 관행에 관한 지침으로 AGENTS.md 같은 AI 에이전트 지침 문서에 저장하는 것이 적합하다.

### 고려된 대안

- `pr_only`: 각 PR에만 국한된 일회성 문제로 판단될 경우
- `path_scoped_rule`: 특정 경로에만 적용되는 규칙일 경우
- `none`: 이미 문서화되어 있거나 중요하지 않은 경우

### 실행 명령어

```bash
promote candidate_012 --target agents
promote ignore candidate_012 --reason "..."
promote snooze candidate_012 --days 30
```

---

## 6. `maxBatchSize` 설정이 있어도 `opts.path.split(',')`가 먼저 배열을 할당하여 DoS 공격에 취약하므로, 경로 내 쉼표 개수를 직접 세어 제한하는 방식으로 개선해야 한다.

- **ID**: `candidate_014`
- **Target**: `path_scoped_rule` → `packages/server/src/unstable-core-do-not-import/http/README.md`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/server/src/unstable-core-do-not-import/http/**`

### 근거

- PR #7191 `packages/server/src/unstable-core-do-not-import/http/contentType.ts` — "Even with `maxBatchSize` set, `opts.path.split(',')` will allocate an array prop..." [link](https://github.com/trpc/trpc/pull/7191#discussion_r2867601933)
- PR #7191 `packages/server/src/unstable-core-do-not-import/http/contentType.ts` — "`opts.path.split(',')` allocates an array with one entry per comma before the si..." [link](https://github.com/trpc/trpc/pull/7191#discussion_r2974473905)

### 제안된 패치

```markdown
---
applyTo: "packages/server/src/unstable-core-do-not-import/http/**"
---
# maxBatchSize 제한 관련 경로 내 쉼표 개수 제한 규칙
- `opts.path.split(',')` 호출 전에 경로 내 쉼표 개수를 직접 세어 제한하는 검증 로직을 추가한다.
- 쉼표 개수 제한을 초과하는 경우 요청을 즉시 거부하거나 오류를 반환한다.
- 정적 분석 또는 유틸리티 함수를 활용하여 쉼표 개수 계산을 일관되게 수행한다.
- `maxBatchSize`와 별개로, 쉼표 개수 기반의 DoS 방어 로직을 명확히 문서화한다.
- 해당 규칙은 해당 경로 내에서만 적용하며, 다른 경로나 파일에는 영향을 주지 않는다.
```

### 이 대상을 선택한 이유

이 패턴은 특정 파일 경로 내에서 `maxBatchSize`와 관련된 DoS 방어를 위한 구체적인 구현 방식에 관한 반복적인 리뷰 코멘트로, 전체 레포지토리보다는 해당 경로에 국한된 규칙으로 관리하는 것이 적절하다.

### 고려된 대안

- `pr_only`: 특정 PR에만 국한된 문제로 보일 경우
- `agents`: 레포지토리 전반에 적용되는 일반적인 코딩 규칙일 경우
- `test`: 런타임에서 반드시 지켜져야 하는 불변식일 경우

### 실행 명령어

```bash
promote candidate_014 --target path_scoped_rule
promote ignore candidate_014 --reason "..."
promote snooze candidate_014 --days 30
```

---

## 7. 여러 패키지에서 동일한 이름의 실행 파일(`intent`)을 노출하면 `node_modules/.bin` 내에서 충돌이 발생할 수 있으므로, 고유한 네임스페이스가 있는 실행 파일 이름을 사용하는 것이 권장됩니다.

- **ID**: `candidate_016`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.95
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 7 comments

### 근거

- PR #7252 `packages/openapi/package.json` — "Adding an `intent` binary here is likely to create `node_modules/.bin/intent` co..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968259354)
- PR #7252 `packages/openapi/package.json` — "This package adds an `intent` binary. Since other @trpc/* packages in this PR al..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968125941)
- PR #7252 `packages/server/package.json` — "Exposing a generic `intent` binary from this package is likely to cause `node_mo..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968259331)
- PR #7252 `packages/client/package.json` — "Exposing a generic `intent` binary from this package is likely to cause `node_mo..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968259335)
- PR #7252 `packages/next/package.json` — "Exposing a generic `intent` binary from this package is likely to cause `node_mo..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968259301)
- ... and 2 more

### 제안된 패치

```markdown
## 실행 파일 네이밍 규칙

- 여러 패키지에서 동일한 이름(`intent`)의 실행 파일을 노출하는 경우, `node_modules/.bin` 내에서 충돌이 발생할 수 있으므로, 고유한 네임스페이스가 포함된 이름을 사용하세요.
- 예를 들어, `@trpc/` 관련 패키지에서는 `@trpc/intent`와 같이 네임스페이스를 명확히 구분하는 이름을 권장합니다.
- 충돌 방지를 위해, 패키지별로 고유한 실행 파일 이름을 정하고, `package.json`의 `bin` 필드에 명시하세요.
- 동일한 프로젝트 내에서 여러 패키지가 `intent`라는 이름의 실행 파일을 노출하는 것을 피하세요.
- 이 규칙은 저장소 전반에 걸쳐 일관되게 적용되어, 향후 개발자와 AI 에이전트가 참고할 수 있도록 문서에 명시하세요.
```

### 이 대상을 선택한 이유

이 패턴은 여러 패키지에 걸쳐 반복적으로 나타나는 네이밍 충돌 문제로, 저장소 전반에 적용되는 실행 파일 명명 규칙과 관련된 지침으로서, 향후 개발자와 AI 에이전트가 참고할 수 있도록 에이전트 문서에 저장하는 것이 적합합니다.

### 고려된 대안

- `pr_only`: 이 지침은 특정 PR에만 국한된 문제이지만, 반복적으로 나타나므로 저장소 전반에 도움이 된다.
- `path_scoped_rule`: 문제가 특정 경로에 국한되지 않고 여러 패키지에 걸쳐 발생한다.

### 실행 명령어

```bash
promote candidate_016 --target agents
promote ignore candidate_016 --reason "..."
promote snooze candidate_016 --days 30
```

---

## 8. packages/tanstack-react-query 테스트 파일에서는 React를 네임스페이스 import 방식으로 사용해야 한다는 규칙이 반복적으로 지적됨

- **ID**: `candidate_017`
- **Target**: `path_scoped_rule` → `packages/tanstack-react-query/IMPORT_CONVENTIONS.md`
- **Confidence**: 0.9500000000000001
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 3 comments
- **Path scope**: `packages/tanstack-react-query/test/**`

### 근거

- PR #7362 `packages/tanstack-react-query/test/client.test.tsx` — "⚠️ Potential issue | 🟠 Major

Use namespace React import in TanStack React Quer..." [link](https://github.com/trpc/trpc/pull/7362#discussion_r3144118971)
- PR #7362 `packages/tanstack-react-query/test/polymorphism.test.tsx` — "⚠️ Potential issue | 🟠 Major

Keep `import  as React` in TanStack React Query t..." [link](https://github.com/trpc/trpc/pull/7362#discussion_r3144118976)
- PR #7362 `packages/tanstack-react-query/test/infiniteQueryOptions.test.tsx` — "⚠️ Potential issue | 🟡 Minor

Restore namespace React import in TanStack test f..." [link](https://github.com/trpc/trpc/pull/7362#discussion_r3144118975)

### 사람 반응

- Agreed: **2** reviewer(s) (@Zelys-DFKH)
  - First agreement: *"Thanks for the review. There's no documented convention in `.coderabbit.yaml` or `CONTRIBUTING.md` requiring namespace i"*

### 제안된 패치

```markdown
---
applyTo: "packages/tanstack-react-query/test/**"
---
# React 네임스페이스 임포트 규칙
- `packages/tanstack-react-query/test/` 내 테스트 파일에서는 React를 네임스페이스 방식(`import * as React from 'react'`)으로 임포트해야 한다.
- 모든 테스트 파일에서 React 관련 임포트는 일관되게 네임스페이스 방식으로 유지한다.
- 기존의 개별 임포트(`import { useState } from 'react'`)는 네임스페이스 임포트로 변경한다.
- 임포트 구문과 사용 예시는 공식 문서 또는 기존 규칙에 따라 일관성 있게 작성한다.
- 이 규칙은 해당 경로 내 테스트 파일에 한정하며, 다른 경로나 파일에는 적용하지 않는다.
```

### 이 대상을 선택한 이유

이 패턴은 특정 경로(packages/tanstack-react-query/test/)에 한정된 import 스타일 규칙으로, 전체 저장소에 적용되는 일반 규칙(agents)보다는 경로 범위 규칙(path_scoped_rule)로 관리하는 것이 적절함. 또한, 기존에 문서화된 규칙이 없으므로 새로 저장할 가치가 있음.

### 고려된 대안

- `agents`: 만약 이 규칙이 저장소 전반에 걸친 React import 스타일이라면 agents가 적합하나, 현재는 특정 경로에 한정됨.
- `pr_only`: 한 번의 PR에서만 지적된 일회성 문제라면 pr_only가 적합하나, 여러 파일에서 반복되어 나타남.

### 실행 명령어

```bash
promote candidate_017 --target path_scoped_rule
promote ignore candidate_017 --reason "..."
promote snooze candidate_017 --days 30
```

---

## 9. `hasExpectedArtifacts()` 함수가 출력 디렉토리 존재 여부만 확인하여 내부 파일이 삭제되거나 손상된 경우 캐시 히트가 잘못 발생할 수 있음.

- **ID**: `candidate_020`
- **Target**: `path_scoped_rule` → `packages/openapi/test/scripts/codegen.ts`
- **Confidence**: 0.95
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/openapi/test/scripts/**`

### 근거

- PR #7296 `packages/openapi/test/scripts/codegen.ts` — "`hasExpectedArtifacts()` only checks that `docPath` and `outputDir` exist. If th..." [link](https://github.com/trpc/trpc/pull/7296#discussion_r3004785612)
- PR #7296 `packages/openapi/test/scripts/codegen.ts` — "`hasExpectedArtifacts()` only checks that `docPath` and `outputDir` exist. If `o..." [link](https://github.com/trpc/trpc/pull/7296#discussion_r3004736557)

### 제안된 패치

```markdown
---
applyTo: "packages/openapi/test/scripts/**"
---
# `hasExpectedArtifacts()` 함수 검증 강화 규칙
- `hasExpectedArtifacts()`는 출력 디렉토리의 존재 여부만 확인하므로, 내부 파일의 상태(존재 여부 및 무결성)도 함께 검사하도록 개선한다.
- 내부 파일이 삭제되거나 손상된 경우를 감지할 수 있도록, 파일 존재 여부뿐 아니라 파일 크기 또는 해시값 검증을 수행하는 유틸리티를 활용한다.
- 스크립트 내에서 `fs` 또는 `crypto` 모듈을 이용하여 파일 무결성 검사를 수행하는 함수를 추가한다.
- 이 규칙은 해당 경로 내 코드 생성 신뢰성을 높이기 위해서만 적용하며, 다른 경로나 범위에는 영향을 주지 않는다.
- 검증 로직은 테스트 또는 빌드 과정에서 호출되어, 캐시 히트 여부를 보다 정확하게 판단할 수 있도록 한다.
```

### 이 대상을 선택한 이유

이 패턴은 packages/openapi/test/scripts/codegen.ts 경로 내 코드 생성 관련 스크립트에 국한된 문제로, 해당 경로에 한정된 규칙으로 관리하는 것이 적절함. 여러 PR에서 반복적으로 지적되었으며, 향후 이 경로 내 코드 생성 신뢰성을 높이기 위해 파일 존재 여부뿐 아니라 내부 파일 상태도 검사하는 규칙이 필요함.

### 고려된 대안

- `pr_only`: 특정 PR에만 국한된 문제로 보일 경우
- `agents`: 전사적으로 적용되는 코드 생성 캐시 검증 규칙일 경우
- `test`: 파일 존재 및 무결성 검증을 테스트로 강제할 경우

### 실행 명령어

```bash
promote candidate_020 --target path_scoped_rule
promote ignore candidate_020 --reason "..."
promote snooze candidate_020 --days 30
```

---

## 10. `subscribe()` 호출 시 `onError` 핸들러 내에서 `subscription.unsubscribe()`를 호출할 때, `subscription` 변수가 `const`로 선언되어 초기화 중에 참조되는 문제를 방지하기 위해 `let`으로 선언하는 것을 권장한다.

- **ID**: `candidate_021`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments

### 근거

- PR #7279 `packages/tests/server/websockets.test.ts` — "`waitForWsSubscriptionError` unsubscribes via `subscription.unsubscribe()` insid..." [link](https://github.com/trpc/trpc/pull/7279#discussion_r2971303187)
- PR #7279 `packages/tests/server/httpSubscriptionLink.test.ts` — "`waitForSubscriptionError` references `subscription` inside the `onError` handle..." [link](https://github.com/trpc/trpc/pull/7279#discussion_r2971303176)

### 제안된 패치

```markdown
## 구독 취소 처리 방식 표준화

- `subscribe()` 호출 시 `onError` 핸들러 내에서 `subscription.unsubscribe()`를 호출할 때, `subscription` 변수를 `const` 대신 `let`으로 선언한다.
- 전역적으로 반복되는 구독 취소 패턴에 대해 일관된 코딩 관례를 유지한다.
- 특정 파일이나 PR에 국한하지 않고, 전체 저장소에 적용할 수 있도록 지침을 준수한다.
- `subscribe()` 호출과 `subscription` 선언을 별도로 분리하지 않고, `let` 선언 후 바로 `subscribe()`를 호출하는 방식을 권장한다.
- 구독 변수 선언 시, `const` 대신 `let`을 사용하여 `onError` 핸들러 내에서 안전하게 `unsubscribe()` 호출이 가능하도록 한다.
```

### 이 대상을 선택한 이유

이 패턴은 여러 테스트 파일에서 반복적으로 나타나는 구독 취소 처리 방식에 관한 전역적인 코딩 관례 문제로, 향후 코드 작성 시 일관된 방식으로 처리하는 것이 중요하다. 따라서 특정 PR이나 경로에 국한되지 않고, 전체 저장소에 적용할 수 있는 에이전트 지침으로 보존하는 것이 적절하다.

### 고려된 대안

- `pr_only`: 특정 PR에만 관련된 일회성 문제일 경우
- `none`: 이미 문서화되어 있거나 중요하지 않은 경우

### 실행 명령어

```bash
promote candidate_021 --target agents
promote ignore candidate_021 --reason "..."
promote snooze candidate_021 --days 30
```

---

## 11. maxBatchSize가 NaN, Infinity, 음수, 0 또는 비정수일 경우 제한 조건이 제대로 작동하지 않으므로, 이를 방지하기 위해 maxBatchSize가 유한한 양의 정수인지 검증해야 한다.

- **ID**: `candidate_025`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.95
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments

### 근거

- PR #7191 `packages/server/src/unstable-core-do-not-import/http/contentType.ts` — "`typeof maxBatchSize === 'number'` will treat `NaN`/`Infinity` as valid, but the..." [link](https://github.com/trpc/trpc/pull/7191#discussion_r2974473943)
- PR #7191 `packages/server/src/unstable-core-do-not-import/http/types.ts` — "`maxBatchSize` accepts any `number` at runtime (including `NaN`, `Infinity`, neg..." [link](https://github.com/trpc/trpc/pull/7191#discussion_r2867601942)

### 제안된 패치

```markdown
## maxBatchSize 검증 규칙

- `maxBatchSize`가 유한한 양의 정수인지 여부를 반드시 검증한다.
- `typeof maxBatchSize === 'number'`만으로는 `NaN`, `Infinity`, 음수, 0, 비정수 값을 걸러내지 않으므로, 추가로 `Number.isInteger(maxBatchSize)`와 `maxBatchSize > 0` 검증을 수행한다.
- 검증은 `/path/to/your/file.js` 또는 관련 파일 내에서 일관되게 적용한다.
- 검증 로직은 `maxBatchSize` 사용 전에 수행하며, 유효하지 않은 경우 예외를 던지거나 적절한 기본값으로 대체한다.
- 이 규칙은 여러 기능과 파일에 반복 적용되어 코드 품질과 안정성을 확보한다.
```

### 이 대상을 선택한 이유

이 패턴은 여러 파일에서 반복적으로 나타나며, maxBatchSize 값 검증에 대한 일관된 규칙을 제시한다. 이는 특정 경로나 한 PR에 국한되지 않고, 여러 기능에 걸쳐 적용될 수 있는 일반적인 코드 품질 및 안정성 지침이므로 agents에 저장하는 것이 적합하다.

### 고려된 대안

- `pr_only`: 특정 PR에만 관련된 일회성 코드 문제일 경우
- `path_scoped_rule`: 특정 경로에만 적용되는 규칙일 경우
- `none`: 이미 문서화되어 있거나 중요하지 않은 경우

### 실행 명령어

```bash
promote candidate_025 --target agents
promote ignore candidate_025 --reason "..."
promote snooze candidate_025 --days 30
```

---

## 12. `SKILL.md` 파일명이 md/mdx 파일의 camelCase 명명 규칙을 위반하므로, 예외를 명시하거나 파일명을 camelCase로 변경해야 한다.

- **ID**: `candidate_027`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 3 comments

### 근거

- PR #7252 `packages/tanstack-react-query/skills/react-query-setup/SKILL.md` — "⚠️ Potential issue | 🟡 Minor

`SKILL.md` filename breaks the md/mdx camelCase n..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937237747)
- PR #7252 `packages/server/skills/adapter-fetch/SKILL.md` — "⚠️ Potential issue | 🟡 Minor

Add `SKILL.md` as an explicit exception in the ca..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937237734)
- PR #7252 `packages/server/skills/trpc-router/SKILL.md` — "⚠️ Potential issue | 🟡 Minor

Rename `SKILL.md` to a camelCase filename to matc..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937237735)

### 제안된 패치

```markdown
## 파일명 규칙 및 예외 처리

- `SKILL.md`와 같은 파일명은 camelCase 규칙을 위반하므로, 해당 파일명을 camelCase로 변경하거나, 규칙 예외 목록에 명시적으로 추가해야 한다.
- 이 규칙은 전체 저장소에 적용되며, 여러 PR에서 반복적으로 나타나는 문제이므로 반드시 준수할 것.
- `SKILL.md`와 유사한 파일명에 대해서도 동일하게 적용하며, 예외 처리 시 명확한 문서화가 필요하다.
- 규칙 위반 시, 파일명을 적절히 수정하거나, 예외 목록에 추가하는 작업을 수행할 것.
- 관련 규칙 및 예외 처리 내용은 `AGENTS.md` 또는 저장소 전체 규칙 문서에 포함시켜 관리할 것.
```

### 이 대상을 선택한 이유

이 패턴은 여러 PR에서 반복적으로 나타나며, repo-wide 파일명 규칙과 예외 처리에 관한 내용으로, 전체 저장소에 적용되는 AI 지침 문서에 포함하는 것이 적절하다.

### 고려된 대안

- `pr_only`: 특정 PR에만 관련된 일회성 문제일 경우
- `path_scoped_rule`: 특정 경로에만 적용되는 규칙일 경우
- `none`: 이미 문서화되어 있거나 중요하지 않은 경우

### 실행 명령어

```bash
promote candidate_027 --target agents
promote ignore candidate_027 --reason "..."
promote snooze candidate_027 --days 30
```

---

## 13. `querySerializer`가 `undefined` 값을 처리할 때 `JSON.stringify`와 `URLSearchParams.append()`의 동작으로 인해 의도치 않은 문자열 "undefined"가 쿼리 파라미터에 포함되는 문제와 `responseTransformer`의 특정 필드 검사 방식에 대한 반복적인 지적입니다.

- **ID**: `candidate_028`
- **Target**: `path_scoped_rule` → `packages/openapi/README.md`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/openapi/src/heyapi/**`

### 근거

- PR #7231 `packages/openapi/src/heyapi/index.ts` — "`querySerializer` uses `JSON.stringify(value)` for every key; when `value` is `u..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2935826094)
- PR #7231 `packages/openapi/src/heyapi/index.ts` — "`querySerializer` uses `JSON.stringify(value)` for every key; when `value` is `u..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2935826096)

### 제안된 패치

```markdown
---
applyTo: "packages/openapi/src/heyapi/**"
---
# 패키지 내 쿼리 직렬화 및 응답 변환 규칙
- `querySerializer`에서 `JSON.stringify` 호출 시 `undefined` 값이 전달될 경우, 문자열 `"undefined"`가 쿼리 파라미터에 포함되지 않도록 조건문을 추가하세요.
- `URLSearchParams.append()` 호출 전에 값이 `undefined` 또는 `null`인지 체크하여, 해당 경우에는 파라미터에 추가하지 않도록 하세요.
- `querySerializer`에서 직렬화하는 값이 `undefined`인 경우 빈 문자열 또는 생략하는 방식을 명확히 하여, 의도치 않은 `"undefined"` 문자열이 쿼리 파라미터에 포함되지 않도록 하세요.
- `responseTransformer`에서 특정 필드 검사 시, `typeof` 또는 `null` 체크를 통해 안전하게 검증하며, `undefined` 또는 `null`인 경우 별도 처리 또는 무시하도록 수정하세요.
- 관련 유틸리티 또는 패턴을 활용하여, 직렬화 및 응답 변환 로직의 일관성과 안정성을 확보하세요.
```

### 이 대상을 선택한 이유

이 패턴은 `packages/openapi/src/heyapi/index.ts` 경로에 국한된 구체적인 직렬화 및 응답 변환 로직에 관한 문제로, 해당 경로에 적용되는 규칙으로 저장하는 것이 적절합니다. 이는 범용적인 에이전트 지침보다는 특정 경로에 한정된 규칙이기 때문입니다.

### 고려된 대안

- `agents`: 만약 이 패턴이 여러 경로에 걸쳐 공통적으로 적용되는 직렬화 규칙이라면 에이전트 지침에 포함할 수 있습니다.
- `pr_only`: 이 문제들이 특정 PR에만 국한된 일회성 문제라면 PR 전용 메모리에 저장할 수 있습니다.

### 실행 명령어

```bash
promote candidate_028 --target path_scoped_rule
promote ignore candidate_028 --reason "..."
promote snooze candidate_028 --days 30
```

---

## 14. 이 에러 핸들러에서 `resolveProcedureError` 호출 시 프로시저의 `declaredErrors` 등록 리스트가 누락되어, 등록된 선언된 에러가 잘못 처리되는 문제를 다룸

- **ID**: `candidate_030`
- **Target**: `path_scoped_rule` → `packages/server/src/adapters/ws.ts`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/server/src/adapters/ws.ts`

### 근거

- PR #7279 `packages/server/src/adapters/ws.ts` — "In this error handler, `resolveProcedureError(..., { path })` is called without ..." [link](https://github.com/trpc/trpc/pull/7279#discussion_r2971303185)
- PR #7279 `packages/server/src/adapters/ws.ts` — "`resolveProcedureError` is called without passing the procedure’s `declaredError..." [link](https://github.com/trpc/trpc/pull/7279#discussion_r2970571720)

### 제안된 패치

```markdown
---
applyTo: "packages/server/src/adapters/ws.ts"
---
# ws.ts 파일 내 에러 핸들러 규칙
- `resolveProcedureError` 호출 시, 해당 프로시저의 `declaredErrors` 등록 리스트를 반드시 포함시킬 것.
- `resolveRegisteredDeclaredErrorOrDowngrade` 함수 호출 전, `declaredErrors`가 정의되어 있는지 검증할 것.
- 에러 처리 시, 선언된 에러 목록이 누락되지 않도록 일관된 패턴을 유지할 것.
- 특정 파일(`packages/server/src/adapters/ws.ts`)에 한정된 규칙임을 명확히 할 것.
- 관련 유틸리티 또는 패턴을 참고하여 일관된 에러 처리 구조를 적용할 것.
```

### 이 대상을 선택한 이유

이 패턴은 특정 파일과 경로(`packages/server/src/adapters/ws.ts`)에 국한된 문제로, 해당 경로 내에서 일관된 에러 처리 규칙을 적용하는 것이 적절함. 이는 코드베이스 전반에 걸친 일반적인 규칙이라기보다는 특정 모듈의 구현 세부사항에 관한 것이므로 path_scoped_rule이 적합함.

### 고려된 대안

- `pr_only`: 이 문제는 특정 PR과 코드에만 해당되는 일회성 문제일 수 있음
- `agents`: 에러 처리 관련 일반 규칙이라면 전체 에이전트 지침에 포함될 수 있음
- `none`: 이미 문서화되었거나 중요하지 않은 문제일 수 있음

### 실행 명령어

```bash
promote candidate_030 --target path_scoped_rule
promote ignore candidate_030 --reason "..."
promote snooze candidate_030 --days 30
```

---

## 15. metadata-only 프레임에 대해 onSseEvent 호출을 건너뛰어야 하며, retry: 전용 청크가 data: undefined로 호출되는 문제를 방지해야 한다.

- **ID**: `candidate_033`
- **Target**: `path_scoped_rule` → `packages/openapi/test/routers/**/core/serverSentEvents.gen.ts`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/openapi/test/routers/**/core/serverSentEvents.gen.ts`

### 근거

- PR #7231 `packages/openapi/test/routers/appRouter-heyapi/core/serverSentEvents.gen.ts` — "⚠️ Potential issue | 🟠 Major

Skip `onSseEvent` for metadata-only frames.

Hear..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128579)
- PR #7231 `packages/openapi/test/routers/errorFormatterRouter-heyapi/core/serverSentEvents.gen.ts` — "⚠️ Potential issue | 🟠 Major

Skip `onSseEvent` for metadata-only frames.

Hear..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128594)

### 제안된 패치

```markdown
---
applyTo: "packages/openapi/test/routers/**/core/serverSentEvents.gen.ts"
---
# 서버 Sent 이벤트 처리 규칙
- 메타데이터 전용 프레임에 대해 `onSseEvent` 호출을 건너뜁니다.
- `retry:` 전용 청크는 `data: undefined`로 호출되지 않도록 합니다.
- `StreamEvent.data`가 필수임을 고려하여 `data: undefined` 호출을 방지합니다.
- 이 규칙은 해당 파일 내 특정 경로에 한정된 구현 세부사항임을 유념합니다.
- 반복적으로 발생하는 문제를 방지하기 위해 관련 호출 조건을 명확히 합니다.
```

### 이 대상을 선택한 이유

이 패턴은 serverSentEvents.gen.ts 파일들에서 반복적으로 나타나며, 특정 경로 내에서만 적용되는 규칙으로 보인다. 이는 cross-cutting convention이라기보다는 특정 경로에 국한된 구현 세부사항이므로 path_scoped_rule이 적합하다.

### 고려된 대안

- `agents`: 만약 이 규칙이 모든 이벤트 스트림 처리에 공통적으로 적용된다면 repo-wide AI instruction에 적합할 수 있다.
- `pr_only`: 특정 PR에만 국한된 문제라면 pr_only가 적합하다.

### 실행 명령어

```bash
promote candidate_033 --target path_scoped_rule
promote ignore candidate_033 --reason "..."
promote snooze candidate_033 --days 30
```

---

## 16. 서버 전송 이벤트 재시도 로직에서 초기 연결 시도에 재시도 예산을 사용하지 않고, AbortError 발생 시 재시도하지 않아야 한다는 규칙

- **ID**: `candidate_034`
- **Target**: `path_scoped_rule` → `packages/openapi/test/routers/**/core/serverSentEvents.gen.ts`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/openapi/test/routers/**/core/serverSentEvents.gen.ts`

### 근거

- PR #7231 `packages/openapi/test/routers/errorFormatterRouter-heyapi/core/serverSentEvents.gen.ts` — "⚠️ Potential issue | 🟠 Major

Don't spend the retry budget on the initial conne..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128592)
- PR #7231 `packages/openapi/test/routers/appRouter-heyapi/core/serverSentEvents.gen.ts` — "⚠️ Potential issue | 🟠 Major

Don't spend the retry budget on the initial conne..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128577)

### 제안된 패치

```markdown
---
applyTo: "packages/openapi/test/routers/**/core/serverSentEvents.gen.ts"
---
# 서버 전송 이벤트 재시도 로직 규칙
- 초기 연결 시도 시 재시도 예산을 사용하지 않도록 구현한다.
- AbortError 발생 시 재시도를 수행하지 않도록 한다.
- 재시도 횟수 계산은 최초 연결 시도를 제외하고 진행한다.
- `attempt` 값이 요청 전 증가하는 점을 고려하여 재시도 제한을 설정한다.
- 관련 유틸리티 또는 패턴을 활용하여 재시도 로직을 명확히 구현한다.
```

### 이 대상을 선택한 이유

이 패턴은 serverSentEvents 관련 특정 경로 내에서 재시도 로직에 대한 명확한 규칙을 반복적으로 지적하고 있어, 전체 저장소보다는 해당 경로에 국한된 규칙으로 관리하는 것이 적절하다. 이는 재시도 횟수 계산과 AbortError 처리 방식에 관한 구체적인 구현 세부사항에 관한 내용이다.

### 고려된 대안

- `agents`: 재시도 로직이 여러 경로에 걸쳐 공통적으로 적용된다면 저장소 전체 AI 지침에 포함할 수 있으나, 현재는 특정 경로에 한정됨
- `pr_only`: 특정 PR에만 해당되는 일회성 문제일 경우
- `none`: 이미 문서화되어 있거나 중요하지 않은 경우

### 실행 명령어

```bash
promote candidate_034 --target path_scoped_rule
promote ignore candidate_034 --reason "..."
promote snooze candidate_034 --days 30
```

---

## 17. GitHub Actions 워크플로우에서 lerna publish 명령어가 git 태깅 및 푸시를 수행하므로, permissions.contents 권한 설정과 lerna 옵션(--no-push, --no-git-tag-version) 사용에 주의해야 한다.

- **ID**: `candidate_035`
- **Target**: `path_scoped_rule` → `github_workflows_permissions.md`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `.github/workflows/**`

### 근거

- PR #7247 `.github/workflows/release-tmp.yml` — "Job now explicitly sets `permissions.contents: read`, but `lerna publish` typica..." [link](https://github.com/trpc/trpc/pull/7247#discussion_r2936535934)
- PR #7247 `.github/workflows/release-canary.yml` — "Job now explicitly sets `permissions.contents: read`, but `lerna publish` typica..." [link](https://github.com/trpc/trpc/pull/7247#discussion_r2936535922)

### 제안된 패치

```markdown
---
applyTo: ".github/workflows/**"
---
# GitHub Actions 워크플로우에서 lerna publish 사용 시 권한 및 옵션 규칙
- `permissions.contents`를 `read`로 명시하여 태그 및 푸시 권한을 제한한다.
- `lerna publish` 명령어에 `--no-push`와 `--no-git-tag-version` 옵션을 반드시 포함시켜 git 태깅 및 푸시를 방지한다.
- 워크플로우 내에서 `lerna publish` 호출 시 옵션이 누락되지 않도록 검증한다.
- 태그 및 푸시 작업이 필요 없는 경우에만 `lerna publish`를 실행하도록 조건을 설정한다.
- 관련 유틸리티 또는 스크립트에서 옵션 검증 및 권한 설정을 자동화한다.
```

### 이 대상을 선택한 이유

이 패턴은 GitHub Actions 워크플로우 내 특정 경로(.github/workflows/)에 국한된 권한 설정과 lerna publish 사용 관련 규칙으로, 다른 영역에는 적용되지 않으므로 path_scoped_rule이 적합하다.

### 고려된 대안

- `agents`: 만약 이 권한 설정과 lerna 사용 규칙이 모든 워크플로우에 공통적으로 적용된다면 repo-wide AI instruction에 포함할 수 있다.
- `pr_only`: 특정 PR에만 해당되는 일회성 권한 문제라면 pr_only가 적합하다.

### 실행 명령어

```bash
promote candidate_035 --target path_scoped_rule
promote ignore candidate_035 --reason "..."
promote snooze candidate_035 --days 30
```

---

## 18. 의존성 변경 시 package.json과 pnpm-lock.yaml 파일을 항상 동기화하여 CI 실패를 방지해야 합니다.

- **ID**: `candidate_036`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments

### 근거

- PR #7200 `packages/react-query/package.json` — "⚠️ Potential issue | 🔴 Critical

Sync lockfile for the Line 98 dependency bump...." [link](https://github.com/trpc/trpc/pull/7200#discussion_r2872430627)
- PR #7200 `package.json` — "⚠️ Potential issue | 🔴 Critical

Regenerate `pnpm-lock.yaml` for these dependen..." [link](https://github.com/trpc/trpc/pull/7200#discussion_r2872430610)

### 제안된 패치

```markdown
## 의존성 변경 시 패키지 파일 동기화 규칙

- 의존성을 변경한 경우, 반드시 `package.json`과 `pnpm-lock.yaml` 파일을 동시에 업데이트하고 커밋하세요.
- 의존성 변경 후 `pnpm install` 또는 `pnpm update` 명령어를 실행하여 lockfile을 재생성하세요.
- 변경된 `pnpm-lock.yaml` 파일이 `package.json`과 일치하는지 반드시 확인하세요.
- CI 실패 방지를 위해, PR 제출 전에 항상 lockfile이 최신 상태인지 검증하세요.
- 이 규칙은 전체 저장소에 적용되며, 개별 PR 또는 특정 경로에 한정되지 않습니다.
```

### 이 대상을 선택한 이유

이 패턴은 여러 PR에서 반복적으로 나타나는 전사적 관행으로, 의존성 변경 후 반드시 lockfile을 재생성하고 커밋해야 한다는 중요한 개발 프로세스 지침을 담고 있습니다. 이는 특정 경로나 개별 PR에 국한되지 않고, 전체 저장소에 적용되는 규칙이므로 agents 문서에 기록하는 것이 적합합니다.

### 고려된 대안

- `pr_only`: 이 지침이 특정 PR에만 국한되어 반복되지 않는 경우.
- `path_scoped_rule`: 이 규칙이 특정 경로에만 적용되는 경우.
- `none`: 이미 문서화되어 있거나 중요하지 않은 경우.

### 실행 명령어

```bash
promote candidate_036 --target agents
promote ignore candidate_036 --reason "..."
promote snooze candidate_036 --days 30
```

---

## 19. `pnpm add -g npm@latest`는 릴리스를 비재현 가능하게 만들고 npm의 새로운 메이저/마이너 버전이 나올 때 예기치 않게 깨질 수 있으므로, 알려진 안정된 npm 버전을 고정하거나 Node에 번들된 npm 버전을 사용하는 것이 좋습니다.

- **ID**: `candidate_037`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.95
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments

### 근거

- PR #7247 `.github/workflows/release-tmp.yml` — "`pnpm add -g npm@latest` makes releases non-reproducible and can break unexpecte..." [link](https://github.com/trpc/trpc/pull/7247#discussion_r2936535936)
- PR #7247 `.github/workflows/release-canary.yml` — "`pnpm add -g npm@latest` makes releases non-reproducible and can break unexpecte..." [link](https://github.com/trpc/trpc/pull/7247#discussion_r2936535931)

### 제안된 패치

```markdown
## 릴리스 관련 워크플로우에서 npm 버전 관리 지침

- `pnpm add -g npm@latest` 명령은 사용하지 않으며, 릴리스의 재현 가능성을 저해하고 예기치 않게 깨질 수 있으므로 피할 것.
- 안정적인 npm 버전은 특정 버전으로 고정하거나, Node.js에 번들된 npm 버전을 사용할 것.
- 전체 저장소의 릴리스 및 배포 관련 워크플로우 파일에서 일관되게 적용할 것.
- 버전 고정은 `package.json` 또는 관련 스크립트 내에서 명확히 지정할 것.
- 릴리스의 결정론적이고 안정적인 버전 관리를 위해 이 지침을 준수할 것.
```

### 이 대상을 선택한 이유

이 패턴은 여러 릴리스 관련 워크플로우 파일에서 반복되며, 릴리스 도구 체인의 결정론적이고 안정적인 버전 관리를 위한 전사적 권장사항으로 보입니다. 따라서 특정 PR이나 경로에 국한되지 않고, 전체 저장소의 에이전트 지침 문서에 포함하는 것이 적절합니다.

### 고려된 대안

- `pr_only`: 패턴이 특정 PR에만 국한된 경우
- `path_scoped_rule`: 특정 경로에만 적용되는 규칙인 경우
- `none`: 이미 문서화되어 있거나 중요하지 않은 경우

### 실행 명령어

```bash
promote candidate_037 --target agents
promote ignore candidate_037 --reason "..."
promote snooze candidate_037 --days 30
```

---

## 20. CI 워크플로우에서 사용하는 CLI 버전을 package.json에 명시된 버전과 일치하도록 고정해야 한다.

- **ID**: `candidate_041`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.95
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments

### 근거

- PR #7252 `.github/workflows/validate-skills.yml` — "⚠️ Potential issue | 🟠 Major

Pin the CLI version used by CI.

Line 29 installs..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937366756)
- PR #7252 `.github/workflows/check-skills.yml` — "⚠️ Potential issue | 🟠 Major

Pin the `@tanstack/intent` version to match the r..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937366751)

### 제안된 패치

```markdown
## CI 워크플로우에서 사용하는 CLI 버전 고정 규칙

- `/.github/workflows/` 내 모든 워크플로우 파일에서 CLI 버전이 `package.json`에 명시된 버전(`^0.0.20` 등)과 일치하도록 고정한다.
- `npm install` 또는 `npx` 명령어를 사용할 때, 버전 번호를 명시하여 최신 버전이 아닌 특정 버전이 설치되도록 한다.
- 버전 미고정 또는 최신 버전 설치가 발견될 경우 즉시 수정하여, 버전 고정을 강제한다.
- CLI 관련 명령어 또는 스크립트는 `/.github/scripts/` 또는 워크플로우 파일 내 명시된 위치에서 버전이 일치하는지 검증한다.
- 이 규칙을 위반하는 경우, 수정 요청 또는 자동 수정 스크립트 실행을 통해 버전 일치를 유지한다.
```

### 이 대상을 선택한 이유

여러 워크플로우 파일에서 동일한 문제인 CLI 버전 고정 누락이 반복적으로 지적되고 있으며, 이는 CI의 결정적 실행을 보장하기 위한 중요한 관행이다. 이 규칙은 특정 경로에 국한되지 않고, 여러 CI 관련 워크플로우에 적용되므로 repo-wide AI 지침 문서에 포함하는 것이 적절하다.

### 고려된 대안

- `pr_only`: 이 문제는 특정 PR에만 국한된 일회성 문제로 보기 어렵고, 반복적으로 나타나므로 pr_only는 부적절하다.
- `path_scoped_rule`: 문제가 특정 경로에 한정되지 않고 여러 워크플로우 파일에 걸쳐 발생하므로 path_scoped_rule은 부적절하다.

### 실행 명령어

```bash
promote candidate_041 --target agents
promote ignore candidate_041 --reason "..."
promote snooze candidate_041 --days 30
```

---

## 21. 마크다운 코드 블록에 언어 식별자를 추가하여 markdownlint MD040 경고를 방지하고 편집기 도구 지원을 향상시킵니다.

- **ID**: `candidate_044`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments

### 근거

- PR #7252 `packages/next/skills/nextjs-pages-router/SKILL.md` — "⚠️ Potential issue | 🟡 Minor

Add a language hint to the fenced tree block.

Th..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937237720)
- PR #7252 `packages/next/skills/nextjs-app-router/SKILL.md` — "⚠️ Potential issue | 🟡 Minor

Add a language to the fenced code block (MD040).
..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937303453)

### 제안된 패치

```markdown
## 마크다운 코드 블록 언어 식별자 규칙

- 모든 마크다운 문서 내 코드 블록에는 적절한 언어 식별자를 추가하세요. 예: ```python, ```javascript, ```bash 등.
- `docs/`, `src/`, `README.md` 등 특정 파일 또는 폴더에 상관없이 일관되게 적용하세요.
- 코드 블록이 언어 식별자를 누락된 경우, markdownlint MD040 경고가 발생하며 편집기 지원이 저하됩니다.
- 새 문서 또는 기존 문서 수정 시, 코드 블록 시작 부분에 언어 식별자를 반드시 포함하세요.
- 규칙 위반 시 경고 메시지(예: "Add a language hint to the fenced code block")를 참고하여 수정하세요.
```

### 이 대상을 선택한 이유

이 패턴은 여러 문서에서 반복적으로 나타나며, 마크다운 문서 작성 시 일관된 코드 블록 언어 지정 관행을 확립하는 데 도움이 됩니다. 이는 특정 경로에 국한되지 않고, 문서 작성 전반에 적용 가능한 규칙이므로 에이전트 지침 문서에 포함하는 것이 적절합니다.

### 고려된 대안

- `path_scoped_rule`: 만약 이 규칙이 특정 경로에만 적용된다면 경로 범위 규칙으로 분류할 수 있습니다.
- `pr_only`: 일회성 PR에만 관련된 경우에는 PR 전용으로 분류할 수 있습니다.

### 실행 명령어

```bash
promote candidate_044 --target agents
promote ignore candidate_044 --reason "..."
promote snooze candidate_044 --days 30
```

---

## 필터링된 항목

승격 후보에서 제외된 항목들. 임계치 튜닝이나 팀 리뷰의 엣지 케이스 확인에 유용합니다.

### 승격 불가 (target=none / invalid) (17)

- 이 패턴은 AI가 인간 대화 상황을 인지하고 대화에 개입하지 않겠다는 메시지를 반복하는 것이다. — target=`none`, confidence=0.95
  - 이 코멘트들은 특정 코드 리뷰 지침이나 규칙이 아니라 AI의 대화 중단 알림 메시지로, 저장할 가치가 있는 기술적 지식이나 규칙이 아니다.
- 디렉터리 읽기 오류 처리 시 모든 오류를 무시하지 말고 ENOENT(디렉터리 없음) 오류만 무시해야 한다. — target=`pr_only`, confidence=0.95
  - 이 패턴은 특정 파일(www/scripts/check-twoslash.ts) 내 특정 코드 라인에서 발생하는 오류 처리 방식에 관한 것으로, 범용적인 규칙이라기보다는 해당 PR에서 발견된 구체적 문제에 대한 지적이다. 따라서 전체 저장소에 적용할 일반 규칙보다는 PR 단위로 보존하는 것이 적절하다.
- 이 워크플로우의 경로 필터가 실제 문서 디렉토리 위치와 일치하지 않아 문서 변경 시 워크플로우가 트리거되지 않음. — target=`pr_only`, confidence=0.90
  - 이 문제는 특정 PR에서 발견된 워크플로우 설정의 경로 필터 오류로, 리포지토리 전반에 적용되는 규칙이나 아키텍처 결정과는 관련이 없으며, 단일 PR에 국한된 수정 사항임.
- 오타 'occured'를 'occurred'로 수정하여 사용자에게 노출되는 오류 메시지의 철자 오류를 방지한다. — target=`pr_only`, confidence=0.95
  - 이 패턴은 특정 PR 내에서 발생한 오타 수정에 관한 것으로, 일반적인 코딩 규칙이나 아키텍처 결정과는 무관하다. 또한, 오타 수정은 특정 코드와 테스트에 국한된 문제이므로 저장소 전체에 적용되는 규칙으로 승격할 필요가 없다.
- `@hey-api/openapi-ts` 패키지의 peerDependencies 최소 버전을 실제 코드에서 사용하는 Plugins 타입을 지원하는 가장 이른 버전으로 조정해야 한다는 반복된 리뷰 코멘트 — target=`pr_only`, confidence=0.95
  - 이 패턴은 특정 PR에서 패키지 버전 범위와 타입 의존성 문제를 다루는 구체적인 상황에 한정되며, 범용적인 규칙이나 아키텍처 결정이 아니므로 저장소 전체에 적용할 필요가 없다.
- pnpm-lock.yaml 파일에서 일부 워크스페이스가 여전히 vitest 3.x 버전을 참조하고 있어, 모노레포 전체를 vitest 4로 업그레이드하려면 해당 워크스페이스의 package.json도 함께 업데이트해야 한다. — target=`pr_only`, confidence=0.90
  - 이 패턴은 특정 PR에서 모노레포 내 vitest 버전 일관성을 맞추기 위한 구체적이고 일회성 작업에 관한 것으로, 범용적인 규칙이나 아키텍처 결정이 아니며, 다른 경로나 전반적인 에이전트 지침에 적용되지 않으므로 pr_only가 적합하다.
- 이 패턴은 여러 패키지에서 동일한 'intent' bin 엔트리를 선언했으나 실제 실행 파일이 누락되어 설치 시 충돌 및 오류가 발생할 수 있다는 문제를 반복적으로 지적합니다. — target=`pr_only`, confidence=0.90
  - 이 문제는 특정 PR 내 여러 패키지에 걸쳐 발생하는 구체적인 구현 누락 및 충돌 문제로, 범용적인 규칙이나 아키텍처 결정이라기보다는 해당 PR에 특화된 문제입니다. 따라서 저장소 전반에 적용할 일반 규칙이나 에이전트 지침으로 승격하기보다는 PR 단위로 관리하는 것이 적절합니다.
- 테스트에서 오타가 있는 메시지 'occured'를 사용하고 있으며, 런타임 메시지가 'occurred'로 수정되면 테스트도 함께 업데이트해야 한다. — target=`pr_only`, confidence=0.90
  - 이 패턴은 특정 테스트 파일 내의 오타 관련 테스트 기대값에 국한되며, 일반적인 코딩 규칙이나 아키텍처 결정과는 무관하다. 또한 테스트 자체의 동작 원칙보다는 특정 PR 내에서만 의미가 있으므로, 저장소 전반에 적용할 필요는 없다.
- 파일 헤더 주석에 명시된 경로와 실제 파일 경로가 일치하지 않아 혼란을 초래할 수 있으므로, 주석 또는 파일 위치를 일치시켜야 한다는 반복된 지적. — target=`pr_only`, confidence=0.90
  - 이 패턴은 특정 PR 내에서 파일 경로와 주석 불일치 문제에 국한된 구체적이고 일회성 문제로, 전사적 규칙이나 아키텍처 결정과는 거리가 멀다. 따라서 일반적인 규칙으로 승격하기보다는 해당 PR에 한정하여 기억하는 것이 적절하다.
- PR에서 문서화된 kebab-case import 경로와 package.json의 exports 키에 사용된 camelCase 경로가 일치하지 않아 소비자가 올바르게 import할 수 없는 문제가 반복적으로 지적됨. — target=`pr_only`, confidence=0.95
  - 이 문제는 특정 PR에서만 발생하는 구체적인 패키지 설정 오류로, 범용적인 규칙이나 아키텍처 결정과 관련이 없으며, 다른 경로에도 일반화되지 않음. 따라서 저장소 전체에 적용할 필요 없이 해당 PR에 한정된 지식으로 분류함.
- 여러 패키지의 package.json에서 존재하지 않는 ./bin/intent.js 파일을 bin.entry로 참조하여 실행 파일이 깨지는 문제가 반복적으로 지적됨. — target=`pr_only`, confidence=0.95
  - 이 문제는 특정 PR에서 여러 패키지에 걸쳐 발생한 구체적인 코드베이스 문제로, 일반적인 코딩 규칙이나 아키텍처 결정과는 거리가 있음. 또한, 이 문제는 특정 파일 경로와 패키지에 국한되어 있어 범용적인 에이전트 지침이나 경로 스코프 규칙으로 분류하기 어렵고, 테스트 가능한 런타임 불변 조건도 아님. 따라서 이 지식은 해당 PR에 한정하여 보존하는 것이 적절함.
- 도움말 및 주석에서 OpenAPI 버전이 3.0으로 표기되어 있으나 실제 생성기는 3.1.1 버전을 출력하므로 문서 일관성 문제를 수정해야 한다. — target=`pr_only`, confidence=0.90
  - 이 패턴은 특정 PR 내에서 문서 주석의 버전 표기 오류를 수정하는 구체적인 사례로, 범용적인 규칙이나 아키텍처 결정과는 거리가 있다. 따라서 저장소 전체에 적용할 만한 일반적인 규칙이 아니며, 특정 PR에 한정된 지식으로 보존하는 것이 적절하다.
- 이 클러스터는 스트리밍 응답의 content-type이 application/json에서 application/jsonl로 변경되어야 한다는 반복된 리뷰 코멘트를 포함합니다. — target=`pr_only`, confidence=0.90
  - 이 패턴은 특정 PR(#7345)과 관련된 구체적인 변경 사항에만 적용되며, 범용적인 규칙이나 아키텍처 결정이 아니므로 전체 저장소 지침에 포함시키기보다는 해당 PR에 한정하여 보존하는 것이 적절합니다.
- `getRouterInputsHash()` 함수가 여러 라우터마다 전체 파일을 반복적으로 읽고 해시를 계산하여 성능 저하를 유발함. — target=`pr_only`, confidence=0.95
  - 이 패턴은 특정 PR에서 발견된 성능 문제로, 구체적인 코드 구조와 함수 호출 방식에 국한되어 있어 범용적인 규칙이나 아키텍처 결정으로 확장하기 어렵다. 따라서 향후 참고를 위해 PR 단위로 보존하는 것이 적절하다.
- @ts-expect-error 주석이 호출되는 훅 이름과 일치하지 않아 혼동을 줄 수 있으므로 정확한 훅 이름으로 수정해야 한다. — target=`pr_only`, confidence=0.95
  - 이 패턴은 특정 테스트 파일 내에서 발생하는 주석의 정확성 문제로, 코드 리뷰 시점에서만 의미가 있으며, 전사적 규칙이나 아키텍처 결정과 관련이 없고, 테스트 동작 보장과도 무관하다. 따라서 PR 단위로 관리하는 것이 적절하다.
- 사용자에게 노출되는 오류 메시지에서 'occured' 오타를 'occurred'로 수정하는 반복된 리뷰 코멘트 — target=`pr_only`, confidence=0.95
  - 이 패턴은 특정 PR과 파일 내의 한정된 오타 수정에 관한 것으로, 범용적인 규칙이나 아키텍처적 결정이 아니며, 테스트 스냅샷과 연관되어 있어 PR 단위에서 관리하는 것이 적절하다. 따라서 저장소 전체에 적용할 필요가 없고, 경로 한정 규칙이나 에이전트 지침으로도 적합하지 않다.
- 문서 내 오타 및 철자 오류 수정에 관한 반복된 리뷰 코멘트 — target=`pr_only`, confidence=0.95
  - 이 패턴은 특정 PR 내 문서 파일에서 발생한 개별적인 오타 수정에 관한 것으로, 범용적인 규칙이나 아키텍처적 결정과는 거리가 멀다. 따라서 저장소 전반에 적용할 필요가 없고, 특정 PR에 한정된 지식으로 분류하는 것이 적절하다.

### 분류 단계에서 실패 (7)

- failed: No object generated: response did not match schema.
  - No object generated: response did not match schema.
- failed: No object generated: response did not match schema.
  - No object generated: response did not match schema.
- failed: No object generated: response did not match schema.
  - No object generated: response did not match schema.
- failed: No object generated: response did not match schema.
  - No object generated: response did not match schema.
- failed: No object generated: response did not match schema.
  - No object generated: response did not match schema.
- failed: No object generated: response did not match schema.
  - No object generated: response did not match schema.
- failed: No object generated: response did not match schema.
  - No object generated: response did not match schema.

---
