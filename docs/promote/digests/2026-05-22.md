# 메모리 승격 다이제스트 — trpc/trpc
> 생성일: 2026-05-22 · promote v0.6.0

## 설정

- Provider: `openai` (embeddings + HAC + llmRefine)
- Models: `gpt-4.1-mini` (classify), `gpt-4.1-nano` (draft), `text-embedding-3-small` (embed)
- Output language: `ko`
- Thresholds: similarity `0.8`, min confidence `0.75`, min occurrences `2`
- Window: `90` days (overridden via --since; config default is 60)
- Privacy: redactSecrets=`true`, sendDiffHunksToLLM=`false`

## 요약

- Scanned: **423** review comments across **62** PRs
- AI reviewer comments: **361**
- Clusters found: **274**
- Repeated clusters: **45**
- Promotion candidates: **23**
- Estimated cost: $0.061
- Timings: fetch 6.2s · normalize 28ms · cluster 44.6s · conv-fetch 7.3s · reply-ctx 6.0s · memory 1.1s · classify+draft 1m 1s · **total 2m 9s**

---

## 1. www/scripts/check-twoslash.ts 파일 내에서 인덱스 또는 캡처 접근 시 non-null assertion 연산자(!) 사용을 금지하고 명시적 null 체크 또는 optional chaining으로 대체해야 한다.

- **ID**: `candidate_001`
- **Target**: `path_scoped_rule` → `www/scripts/CODING_GUIDELINES.md`
- **Confidence**: 0.95
- **Scope**: 🌐 cross-PR (3 PRs)
- **Occurrences**: 3 comments
- **Path scope**: `www/scripts/check-twoslash.ts`

### 근거

- PR #7196 `www/scripts/check-twoslash.ts` — "⚠️ Potential issue | 🟠 Major

Remove non-null assertions from indexed/capture a..." [link](https://github.com/trpc/trpc/pull/7196#discussion_r2869759038)
- PR #7176 `www/scripts/check-twoslash.ts` — "⚠️ Potential issue | 🟠 Major

Replace non-null assertions with guards/optional ..." [link](https://github.com/trpc/trpc/pull/7176#discussion_r2876202469)
- PR #7195 `www/scripts/check-twoslash.ts` — "⚠️ Potential issue | 🟡 Minor

Remove non-null assertions in this file to mainta..." [link](https://github.com/trpc/trpc/pull/7195#discussion_r2869717508)

### 제안된 패치

```markdown
---
applyTo: "www/scripts/check-twoslash.ts"
---
# 경로별 코딩 가이드라인: non-null assertion 금지
- 인덱스 또는 캡처 접근 시 `!` 연산자를 사용하지 않도록 합니다.
- null 체크 또는 선택적 체이닝(`?.`)으로 명시적 null 처리를 수행하세요.
- 48, 58, 67, 148 라인에서 `!` 사용을 제거하고 적절한 null 체크 또는 선택적 체이닝으로 대체하세요.
- 해당 규칙 위반 시, 잠재적 null 참조 오류 방지 및 코드 안전성 향상을 위해 수정하세요.
- 이 규칙은 `www/scripts/check-twoslash.ts` 파일 내에서만 적용됩니다.
```

### 이 대상을 선택한 이유

이 패턴은 특정 파일(www/scripts/check-twoslash.ts)에 반복적으로 나타나는 코딩 가이드라인 위반으로, 해당 경로에 한정된 규칙으로 관리하는 것이 적절하다. 이는 범용적인 에이전트 지침보다는 특정 경로에 적용되는 규칙이므로 path_scoped_rule로 분류했다.

### 고려된 대안

- `agents`: 만약 이 규칙이 모든 파일에 걸쳐 적용된다면 에이전트 지침에 포함하는 것이 적절하다.
- `pr_only`: 특정 PR에만 국한된 문제라면 PR 전용으로 관리하는 것이 낫다.

### 실행 명령어

```bash
promote candidate_001 --target path_scoped_rule
promote ignore candidate_001 --reason "..."
promote snooze candidate_001 --days 30
```

---

## 2. `@ts-expect-error` 지시자에 대해 ESLint가 요구하는 3자 이상의 설명을 추가해야 한다는 규칙이 반복적으로 지적됨.

- **ID**: `candidate_003`
- **Target**: `path_scoped_rule` → `eslint.config.js`
- **Confidence**: 0.95
- **Scope**: 🌐 cross-PR (2 PRs)
- **Occurrences**: 4 comments
- **Path scope**: `packages/openapi/test/routers/**/client/client.gen.ts`

### 근거

- PR #7231 `packages/openapi/test/routers/superjsonRouter-heyapi/client/client.gen.ts` — "⚠️ Potential issue | 🟡 Minor

Add description to `@ts-expect-error` directive.
..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128597)
- PR #7283 `packages/openapi/test/routers/cyclicTypesRouter-heyapi/client/client.gen.ts` — "⚠️ Potential issue | 🟡 Minor

Add description to `@ts-expect-error` directive.
..." [link](https://github.com/trpc/trpc/pull/7283#discussion_r2977549460)
- PR #7231 `packages/openapi/test/routers/appRouter-heyapi/client/client.gen.ts` — "⚠️ Potential issue | 🟡 Minor

Add description to `@ts-expect-error` directive.
..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128573)
- PR #7231 `packages/openapi/test/routers/errorFormatterRouter-heyapi/client/client.gen.ts` — "⚠️ Potential issue | 🟡 Minor

Add a reason to the `@ts-expect-error` or remove ..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128580)

### 제안된 패치

```markdown
---
applyTo: "packages/openapi/test/routers/**/client/client.gen.ts"
---
# `@ts-expect-error` 지시자에 설명 추가 규칙
- `@ts-expect-error` 주석에는 최소 3자 이상의 설명을 반드시 포함해야 합니다.
- ESLint 규칙에 따라, 왜 타입 오류 무시가 필요한지 명확히 기술하세요.
- 반복되는 자동 생성 파일 내에서도 일관되게 적용하세요.
- 설명이 없는 경우 ESLint 경고 또는 오류가 발생할 수 있음을 유의하세요.
- 설명은 간결하면서도 무시 사유를 명확히 전달하는 내용을 포함해야 합니다.
```

### 이 대상을 선택한 이유

이 패턴은 여러 auto-generated 코드 파일 내에서 반복적으로 나타나며, ESLint 규칙에 따라 `@ts-expect-error` 주석에 설명을 반드시 포함해야 하는 구체적인 규칙으로, 특정 경로 내 코드에만 적용되므로 path_scoped_rule이 적합하다.

### 고려된 대안

- `agents`: 규칙이 여러 경로에 걸쳐 적용되지만, 주로 특정 auto-generated 코드 경로에 국한되어 있음.
- `pr_only`: 각 코멘트가 특정 PR과 파일에만 해당되어 일반화하기 어려움.

### 실행 명령어

```bash
promote candidate_003 --target path_scoped_rule
promote ignore candidate_003 --reason "..."
promote snooze candidate_003 --days 30
```

---

## 3. 레포지토리 내 pnpm 버전 선언이 package.json과 .tool-versions 파일 간에 불일치하여 asdf/rtx 환경에서 설치 불일치 문제를 일으킬 수 있으므로, 두 파일의 pnpm 버전을 일치시켜야 한다.

- **ID**: `candidate_005`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.95
- **Scope**: 🌐 cross-PR (2 PRs)
- **Occurrences**: 2 comments

### 근거

- PR #7194 `.tool-versions` — "The repo declares pnpm 9.x via package.json (engines/packageManager), but .tool-..." [link](https://github.com/trpc/trpc/pull/7194#discussion_r2869277485)
- PR #7200 `.tool-versions` — "`.tool-versions` still pins `pnpm 8.15.5`, but the repo’s root `package.json` de..." [link](https://github.com/trpc/trpc/pull/7200#discussion_r2872405266)

### 제안된 패치

```markdown
## 도구 체인 버전 일치 규칙

- `package.json`의 `engines.packageManager` 또는 `engines.pnpm`과 `.tool-versions` 파일 내 `pnpm` 버전이 일치하도록 유지한다.
- 두 파일 간 버전 불일치가 발견되면 즉시 수정하여 일관성을 확보한다.
- 버전 변경 시, 변경 사항을 커밋 메시지에 명확히 기록한다.
- 이 규칙은 레포지토리 전체에 적용되며, 특정 폴더 또는 PR에 한정하지 않는다.
- 도구 체인 관리에 관한 전사적 규칙으로, 모든 개발자가 준수하도록 한다.
```

### 이 대상을 선택한 이유

이 패턴은 레포지토리 전반에 걸쳐 일관된 개발 환경을 유지하기 위한 중요한 관행으로, 여러 PR에서 반복적으로 지적되었으며, 향후 개발자 및 AI 에이전트가 참고할 수 있도록 중앙 문서에 기록하는 것이 유용하다. 특정 경로나 PR에 국한되지 않고, 도구 체인 관리에 관한 전사적 규칙이기 때문에 agents 대상으로 분류하였다.

### 고려된 대안

- `pr_only`: 이 지적이 특정 PR에만 국한된 일회성 문제일 경우.
- `path_scoped_rule`: 만약 이 규칙이 특정 경로에만 적용된다면.

### 실행 명령어

```bash
promote candidate_005 --target agents
promote ignore candidate_005 --reason "..."
promote snooze candidate_005 --days 30
```

---

## 4. Node.js 런타임 버전과 @types/node 타입 정의 버전의 일치 필요성에 대한 반복된 권고

- **ID**: `candidate_006`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.95
- **Scope**: 🌐 cross-PR (2 PRs)
- **Occurrences**: 2 comments

### 근거

- PR #7200 `package.json` — "⚠️ Potential issue | 🟠 Major

Update `@types/node` to align with Node 24 runtim..." [link](https://github.com/trpc/trpc/pull/7200#discussion_r2872399545)
- PR #7210 `package.json` — "⚠️ Potential issue | 🟠 Major

Downgrade `@types/node` to `^24.x` or update `eng..." [link](https://github.com/trpc/trpc/pull/7210#discussion_r2884107893)

### 제안된 패치

```markdown
## Node.js 런타임 버전과 @types/node 타입 정의 버전 일치 규칙

- 프로젝트의 `package.json` 파일 내 `engines.node` 버전과 `@types/node` 버전이 일치하거나 호환되는 버전으로 유지하세요.
- Node.js 런타임 버전이 `^24.0.0`인 경우, `@types/node`도 `^24.x` 버전으로 업데이트하세요.
- 버전 불일치로 인한 빌드 또는 타입 오류를 방지하기 위해 정기적으로 버전 검증 및 업데이트를 수행하세요.
- `@types/node` 버전은 Node.js 런타임 버전의 주요 버전과 일치해야 하며, 예를 들어 Node.js 25를 사용하는 경우 `@types/node`도 `^25.x`로 설정하세요.
- 이 규칙을 준수하지 않으면 코드 품질 저하와 빌드 실패 가능성이 높아지므로 반드시 지침에 따라 관리하세요.
```

### 이 대상을 선택한 이유

이 패턴은 Node.js 런타임 버전과 타입 정의 버전 간의 일관성을 유지하는 것이 전사적으로 중요한 관행으로, 여러 PR에서 반복적으로 나타나며, 이는 코드 품질과 빌드 안정성에 영향을 미치므로 에이전트 지침 문서에 포함하는 것이 적절하다.

### 고려된 대안

- `pr_only`: 특정 PR에만 국한된 문제로, 일반화된 규칙으로 보기는 어렵다.
- `none`: 이미 충분히 문서화되어 있거나 중요하지 않은 문제이다.

### 실행 명령어

```bash
promote candidate_006 --target agents
promote ignore candidate_006 --reason "..."
promote snooze candidate_006 --days 30
```

---

## 5. 테스트 파일에서 beforeAll을 vitest에서 import하지 않으면 런타임 오류가 발생하므로, describe/expect/it와 함께 반드시 import해야 한다.

- **ID**: `candidate_007`
- **Target**: `path_scoped_rule` → `packages/openapi/test/README.md`
- **Confidence**: 0.95
- **Scope**: 🌐 cross-PR (2 PRs)
- **Occurrences**: 4 comments
- **Path scope**: `packages/openapi/test/**`

### 근거

- PR #7231 `packages/openapi/test/edgeCases.test.ts` — "`beforeAll` is used but not imported from Vitest, which will cause the test file..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2935826088)
- PR #7231 `packages/openapi/test/edgeCases.test.ts` — "`beforeAll` is used but not imported from Vitest, which will cause the test file..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2935826082)
- PR #7283 `packages/openapi/test/cyclicTypes.test.ts` — "`beforeAll` is used in this test but not imported from vitest, which will throw ..." [link](https://github.com/trpc/trpc/pull/7283#discussion_r2977455435)
- PR #7283 `packages/openapi/test/cyclicTypes.test.ts` — "`beforeAll` is used but not imported from vitest, so this test file will throw a..." [link](https://github.com/trpc/trpc/pull/7283#discussion_r2977503215)

### 제안된 패치

```markdown
---
applyTo: "packages/openapi/test/**"
---
# 패턴별 import 규칙
- `beforeAll`을 사용할 경우, 반드시 `vitest`에서 import해야 하며, `describe`, `expect`, `it`과 함께 import할 것.
- import 구문은 테스트 파일 상단에 위치시킬 것.
- `vitest`에서 제공하는 모든 테스트 관련 유틸리티를 일관되게 import하여 런타임 오류를 방지할 것.
- `beforeAll` 미사용 또는 미 import 시, 관련 테스트가 실패할 수 있음을 유의할 것.
- 규칙 위반 시, 코드 수정 또는 import 추가를 권장함.
```

### 이 대상을 선택한 이유

이 패턴은 vitest를 사용하는 테스트 파일 내에서 반복적으로 발생하는 구체적인 import 규칙으로, 특정 경로(테스트 파일)에 국한된 규칙이다. 따라서 전체 에이전트 지침보다는 path_scoped_rule로 관리하는 것이 적합하다.

### 고려된 대안

- `agents`: 만약 이 규칙이 모든 테스트 파일에 공통적으로 적용된다면 에이전트 지침에 포함할 수 있다.
- `pr_only`: 각 PR에만 국한된 문제라면 PR 전용으로 관리할 수 있다.

### 실행 명령어

```bash
promote candidate_007 --target path_scoped_rule
promote ignore candidate_007 --reason "..."
promote snooze candidate_007 --days 30
```

---

## 6. 패키지의 `bin` 실행 파일을 `files` 허용 목록에 포함하지 않으면, 배포 시 실행 파일이 누락되어 소비자에게 문제가 발생하므로 `bin` 디렉터리를 `files`에 반드시 추가해야 한다.

- **ID**: `candidate_011`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.95
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 9 comments

### 근거

- PR #7252 `packages/server/package.json` — "The package defines a `bin` entry (`./bin/intent.js`), but `bin/` is not include..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968125965)
- PR #7252 `packages/tanstack-react-query/package.json` — "The package exports an `intent` binary but `bin/` is not included in the `files`..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968125922)
- PR #7252 `packages/client/package.json` — "The package now exposes an `intent` binary, but `bin/` is not part of the `files..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968125972)
- PR #7252 `packages/next/package.json` — "The package declares `"bin": { "intent": "./bin/intent.js" }` but `bin/` is not ..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968125893)
- PR #7252 `packages/client/package.json` — "`bin.intent` points to `./bin/intent.js`, but there is no `packages/client/bin/`..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937362621)
- ... and 4 more

### 제안된 패치

```markdown
## 배포 시 `bin` 디렉터리 포함 규칙

- `bin` 디렉터리에 있는 실행 파일을 배포 패키지에 포함시키기 위해, 반드시 `package.json`의 `files` 배열에 `bin` 또는 `bin/` 경로를 추가한다.
- `files` 허용 목록에 `bin/`이 포함되지 않으면, 배포 시 실행 파일이 누락되어 소비자에게 문제가 발생하므로 반드시 포함시킨다.
- `bin` 디렉터리 내의 파일이 `package.json`의 `bin` 필드와 일치하는지 확인한다.
- `files` 배열에 `bin/`을 추가하는 방법은 예를 들어 `"files": ["lib", "bin"]`과 같이 작성한다.
- 이 규칙은 여러 패키지에 일관되게 적용하여 배포 품질을 유지하는 중요한 관행임을 유념한다.
```

### 이 대상을 선택한 이유

이 패턴은 여러 패키지에서 반복적으로 나타나는 배포 관련 규칙으로, 저장소 전반에 적용되는 중요한 관행이다. 따라서 특정 PR에 국한되지 않고, 여러 패키지에 걸쳐 일관된 배포 품질을 보장하기 위해 에이전트 지침 문서에 포함하는 것이 적절하다.

### 고려된 대안

- `pr_only`: 패키지별로 개별 PR에서만 의미가 있을 경우
- `path_scoped_rule`: 특정 경로에만 적용되는 규칙일 경우
- `none`: 이미 문서화되어 있거나 중요하지 않은 경우

### 실행 명령어

```bash
promote candidate_011 --target agents
promote ignore candidate_011 --reason "..."
promote snooze candidate_011 --days 30
```

---

## 7. `maxBatchSize` 설정이 있어도 `opts.path.split(',')`가 먼저 배열을 할당하여 DoS 공격에 취약하므로, 경로 내 쉼표 개수를 직접 세어 제한하는 방식으로 개선해야 한다.

- **ID**: `candidate_013`
- **Target**: `path_scoped_rule` → `packages/server/src/unstable-core-do-not-import/http/contentType.ts`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/server/src/unstable-core-do-not-import/http/**`

### 근거

- PR #7191 `packages/server/src/unstable-core-do-not-import/http/contentType.ts` — "Even with `maxBatchSize` set, `opts.path.split(',')` will allocate an array prop..." [link](https://github.com/trpc/trpc/pull/7191#discussion_r2867601933)
- PR #7191 `packages/server/src/unstable-core-do-not-import/http/contentType.ts` — "`opts.path.split(',')` allocates an array with one entry per comma before the si..." [link](https://github.com/trpc/trpc/pull/7191#discussion_r2974473905)

### 제안된 패치

```markdown
---
applyTo: "packages/server/src/unstable-core-do-not-import/http/**"
---
# 경로 내 쉼표 개수 제한 규칙
- `opts.path.split(',')` 호출 전에 경로 내 쉼표 개수를 직접 세어 제한하는 로직을 구현한다.
- 쉼표 개수 제한을 초과하는 경우 요청을 즉시 차단하거나 오류를 반환한다.
- 정규 표현식 또는 문자열 검색 방법을 활용하여 쉼표 개수 검사를 수행한다.
- `maxBatchSize`와 별개로, 경로 내 쉼표 개수에 따른 DoS 공격 방지 로직을 명확히 분리한다.
- 보안 강화를 위해 경로 길이와 쉼표 개수 모두를 검증하는 유틸리티 함수를 재사용한다.
```

### 이 대상을 선택한 이유

이 패턴은 특정 파일 경로 내에서 `maxBatchSize`를 효과적으로 적용하기 위한 구체적인 구현 방식에 관한 반복된 권고사항으로, 보안 관련 경로 처리 규칙으로서 path_scoped_rule에 적합하다.

### 고려된 대안

- `pr_only`: 이 내용이 특정 PR에만 국한된 구현 개선 사항일 경우.
- `agents`: 만약 이 규칙이 여러 경로에 걸쳐 일반적인 보안 관행이라면.

### 실행 명령어

```bash
promote candidate_013 --target path_scoped_rule
promote ignore candidate_013 --reason "..."
promote snooze candidate_013 --days 30
```

---

## 8. 여러 @trpc/* 패키지에서 동일한 'intent' 바이너리를 노출하면 node_modules/.bin/intent 충돌이 발생할 수 있으므로, 네임스페이스가 있는 고유한 바이너리 이름을 사용하는 것이 권장됩니다.

- **ID**: `candidate_016`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.95
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 7 comments

### 근거

- PR #7252 `packages/openapi/package.json` — "Adding an `intent` binary here is likely to create `node_modules/.bin/intent` co..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968259354)
- PR #7252 `packages/openapi/package.json` — "This package adds an `intent` binary. Since other @trpc/* packages in this PR al..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968125941)
- PR #7252 `packages/server/package.json` — "Exposing a generic `intent` binary from this package is likely to cause `node_mo..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968259331)
- PR #7252 `packages/client/package.json` — "Exposing a generic `intent` binary from this package is likely to cause `node_mo..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968259335)
- PR #7252 `packages/next/package.json` — "Exposing a generic `intent` binary from this package is likely to cause `node_mo..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968259301)
- ... and 2 more

### 제안된 패치

```markdown
## 바이너리 네이밍 규칙

- 여러 `@trpc/*` 패키지에서 동일한 `intent` 바이너리를 노출하지 않도록 하며, 네임스페이스가 포함된 고유한 이름을 사용하세요 (예: `@trpc/intent-cli`).
- 바이너리 이름은 패키지별로 고유하게 지정하여 `node_modules/.bin/` 내 충돌을 방지하세요.
- 바이너리 이름이 충돌 가능성이 있는 경우, 관련 패키지 전체에 일관된 네이밍 규칙을 적용하세요.
- 바이너리 이름을 변경할 때는 사용자 환경에서의 충돌 방지와 명확한 구분을 위해 명확한 네임스페이스를 포함하세요.
- 충돌 방지와 명확성을 위해, 모든 `@trpc/*` 패키지의 바이너리 이름은 고유하고 일관된 네이밍 규칙을 따르도록 문서화하세요.
```

### 이 대상을 선택한 이유

이 패턴은 여러 패키지에 걸쳐 반복적으로 나타나는 네이밍 충돌 문제로, 향후 모든 관련 패키지에서 일관된 네이밍 규칙을 적용하는 것이 중요하여 저장소 전반에 적용되는 AI 지침 문서에 포함하는 것이 적합합니다.

### 고려된 대안

- `pr_only`: 이 문제는 특정 PR에만 국한된 일회성 문제일 경우.
- `path_scoped_rule`: 만약 이 규칙이 특정 경로(예: packages/*)에만 적용된다면.

### 실행 명령어

```bash
promote candidate_016 --target agents
promote ignore candidate_016 --reason "..."
promote snooze candidate_016 --days 30
```

---

## 9. ServerSentEventsOptions 타입 파라미터 누락 문제를 수정하고, SSE 페이로드 타입 전파를 올바르게 적용하는 규칙

- **ID**: `candidate_017`
- **Target**: `path_scoped_rule` → `packages/openapi/test/routers/README.md`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/openapi/test/routers/**`

### 근거

- PR #7231 `packages/openapi/test/routers/errorFormatterRouter-heyapi/core/serverSentEvents.gen.ts` — "⚠️ Potential issue | 🟠 Major

Fix the SSE payload type propagation and apply th..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128590)
- PR #7231 `packages/openapi/test/routers/appRouter-heyapi/core/serverSentEvents.gen.ts` — "⚠️ Potential issue | 🟠 Major

Fix missing generic parameter and remove type ass..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128575)

### 제안된 패치

```markdown
---
applyTo: "packages/openapi/test/routers/**"
---
# SSE 타입 안전성 규칙
- `ServerSentEventsOptions` 타입 파라미터를 반드시 명시하여 타입 누락 문제를 방지한다.
- `createSseClient` 호출 시 적절한 제네릭 타입을 전달하도록 한다.
- 타입 단언(`as`) 사용을 피하고, 타입 안전성을 유지하는 패턴을 적용한다.
- SSE 페이로드 타입 전파를 명확하게 하여 타입 불일치를 방지한다.
- 관련 유틸리티 또는 패턴을 활용하여 일관된 타입 적용을 유지한다.
```

### 이 대상을 선택한 이유

이 패턴은 packages/openapi/test/routers/ 경로 내 SSE 관련 코드에서 반복적으로 발생하는 타입 파라미터 누락 및 부적절한 타입 단언 문제로, 해당 경로에 국한된 타입 안전성 규칙으로 분류하는 것이 적절하다. 이는 특정 경로에 한정된 문제이며, 범용적인 에이전트 지침이나 아키텍처 결정과는 거리가 있어 path_scoped_rule로 분류하였다.

### 고려된 대안

- `agents`: 만약 이 타입 파라미터 문제와 타입 안전성 규칙이 전 레포지토리에 걸쳐 SSE 관련 모든 코드에 적용된다면 에이전트 지침으로 분류할 수 있다.
- `pr_only`: 이 문제들이 특정 PR에만 국한된 일회성 문제라면 pr_only가 적절하다.

### 실행 명령어

```bash
promote candidate_017 --target path_scoped_rule
promote ignore candidate_017 --reason "..."
promote snooze candidate_017 --days 30
```

---

## 10. 여러 @trpc/* 패키지에서 동일한 'intent' bin 이름을 중복 선언하고, 해당 bin 파일이 누락되어 설치 시 충돌 및 실행 실패가 발생함.

- **ID**: `candidate_018`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.95
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 4 comments

### 근거

- PR #7252 `packages/next/package.json` — "This package exports a `bin` named `intent` pointing at `./bin/intent.js`, but t..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937220216)
- PR #7252 `packages/tanstack-react-query/package.json` — "This package declares a `bin` entry `intent: ./bin/intent.js`, but there is no `..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937220224)
- PR #7252 `packages/openapi/package.json` — "`bin.intent` points to `./bin/intent.js`, but there is no `packages/openapi/bin/..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937362632)
- PR #7252 `packages/server/package.json` — "`bin.intent` points to `./bin/intent.js`, but `packages/server/bin/` doesn't exi..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937362645)

### 제안된 패치

```markdown
## AGENTS.md: 패턴 일관성 및 bin 설정 가이드라인

- 여러 `@trpc/*` 패키지에서 동일한 `bin` 이름(예: `intent`)을 중복 선언하지 않도록 한다.
- `bin`으로 선언된 파일(`./bin/intent.js`)은 반드시 해당 경로에 존재하도록 하며, 누락되지 않도록 한다.
- `bin` 선언 시, 선언된 파일이 PR에 포함되어 있는지 반드시 확인한다.
- 동일한 `bin` 이름을 사용하는 경우, 충돌 방지 및 명확한 역할 분담을 위해 별도 가이드라인을 따른다.
- `bin` 관련 문제 발생 시, 즉시 수정하여 배포 전 검증을 완료한다.
```

### 이 대상을 선택한 이유

이 패턴은 여러 패키지에 걸쳐 반복적으로 나타나는 bin 설정 문제로, 동일한 bin 이름 중복 선언과 bin 파일 누락으로 인한 설치 및 실행 문제를 방지하기 위한 전사적 가이드라인으로 관리하는 것이 적절함.

### 고려된 대안

- `pr_only`: 특정 PR에만 해당하는 일회성 문제로 보기에는 여러 패키지에서 반복됨
- `path_scoped_rule`: 문제가 특정 경로에 국한되지 않고 여러 패키지에 걸쳐 발생함
- `none`: 중복 및 누락 문제는 향후 유사 문제 방지에 도움이 되는 중요한 패턴임

### 실행 명령어

```bash
promote candidate_018 --target agents
promote ignore candidate_018 --reason "..."
promote snooze candidate_018 --days 30
```

---

## 11. `hasExpectedArtifacts()` 함수가 출력 디렉터리 존재 여부만 확인하여 내부 파일 손상 시 캐시 오류가 발생할 수 있으므로, 출력 디렉터리 내 특정 파일 존재 여부를 추가로 검사해야 한다.

- **ID**: `candidate_019`
- **Target**: `path_scoped_rule` → `packages/openapi/test/scripts/CODEGEN_RULES.md`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/openapi/test/scripts/**`

### 근거

- PR #7296 `packages/openapi/test/scripts/codegen.ts` — "`hasExpectedArtifacts()` only checks that `docPath` and `outputDir` exist. If th..." [link](https://github.com/trpc/trpc/pull/7296#discussion_r3004785612)
- PR #7296 `packages/openapi/test/scripts/codegen.ts` — "`hasExpectedArtifacts()` only checks that `docPath` and `outputDir` exist. If `o..." [link](https://github.com/trpc/trpc/pull/7296#discussion_r3004736557)

### 제안된 패치

```markdown
---
applyTo: "packages/openapi/test/scripts/**"
---
# `hasExpectedArtifacts()` 함수 검사 강화 규칙
- 출력 디렉터리(`outputDir`) 내 특정 파일 존재 여부를 추가로 확인한다.
- `expectedFiles` 목록에 명시된 파일들이 모두 존재하는지 검증한다.
- 파일 손상 또는 삭제로 인한 캐시 오류를 방지하기 위해 파일 무결성 검사를 수행한다.
- `fs.existsSync()` 또는 유사한 파일 존재 검사 유틸리티를 활용한다.
- 검증 실패 시 캐시 무효화 또는 재생성 절차를 트리거한다.
```

### 이 대상을 선택한 이유

이 패턴은 packages/openapi/test/scripts/codegen.ts 경로에 국한된 코드 생성기 캐시 무효화 문제로, 해당 경로에만 적용되는 규칙으로 보인다. 범용적인 에이전트 지침보다는 특정 경로에 대한 검사 강화 규칙으로 저장하는 것이 적절하다.

### 고려된 대안

- `agents`: 만약 이 패턴이 여러 경로에 걸쳐 코드 생성 캐시 무효화 문제로 반복된다면 에이전트 지침으로 적합할 수 있다.
- `pr_only`: 특정 PR에만 국한된 문제라면 PR 전용 메모리에 저장할 수 있다.

### 실행 명령어

```bash
promote candidate_019 --target path_scoped_rule
promote ignore candidate_019 --reason "..."
promote snooze candidate_019 --days 30
```

---

## 12. `subscribe()` 호출과 동시에 `const`로 선언된 `subscription` 변수를 `onError` 핸들러 내에서 참조하는 것은 동기적 에러 발생 시 temporal dead zone 문제를 일으킬 수 있으므로, `let`으로 선언하거나 별도 초기화가 필요하다.

- **ID**: `candidate_020`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.95
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments

### 근거

- PR #7279 `packages/tests/server/websockets.test.ts` — "`waitForWsSubscriptionError` unsubscribes via `subscription.unsubscribe()` insid..." [link](https://github.com/trpc/trpc/pull/7279#discussion_r2971303187)
- PR #7279 `packages/tests/server/httpSubscriptionLink.test.ts` — "`waitForSubscriptionError` references `subscription` inside the `onError` handle..." [link](https://github.com/trpc/trpc/pull/7279#discussion_r2971303176)

### 제안된 패치

```markdown
## 구독 변수 선언 및 에러 핸들러 처리 규칙

- `subscribe()` 호출과 동시에 `const`로 선언된 `subscription` 변수를 `onError` 핸들러 내에서 참조하지 않도록 한다.
- `subscription` 변수는 `let`으로 선언하거나, 별도 초기화 후 참조하도록 수정한다.
- 구독 취소(`unsubscribe()`)는 `onError` 핸들러 내에서 `subscription`이 안전하게 참조된 후 수행한다.
- 이 규칙은 모든 테스트 파일에서 일관되게 적용하며, 전역적인 코드 안전성과 가독성을 위해 준수한다.
- `subscribe()` 호출과 `subscription` 선언, `unsubscribe()` 호출의 순서와 위치를 명확히 하여, 타이밍 문제를 방지한다.
```

### 이 대상을 선택한 이유

이 패턴은 여러 테스트 파일에서 반복적으로 나타나는 구독 취소 시점과 변수 선언 방식에 관한 전역적인 코드 스타일 및 안전성 문제로, 향후 코드 작성 시 참고할 수 있도록 에이전트 지침 문서에 포함하는 것이 적절하다.

### 고려된 대안

- `pr_only`: 패턴이 특정 PR에만 국한되어 있어 일반화하기 어렵다.
- `none`: 이미 문서화되어 있거나 중요하지 않은 문제일 경우.

### 실행 명령어

```bash
promote candidate_020 --target agents
promote ignore candidate_020 --reason "..."
promote snooze candidate_020 --days 30
```

---

## 13. 환경 변수로부터 절대 경로를 직접 사용하는 셸 명령어는 제어되지 않은 경로 의존성을 초래할 수 있다.

- **ID**: `candidate_022`
- **Target**: `path_scoped_rule` → `packages/**/generate.test.ts`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 3 comments
- **Path scope**: `packages/**/generate.test.ts`

### 근거

- PR #7226 `packages/server/src/openapi/generate.test.ts` — "Shell command built from environment values

This shell command depends on an un..." [link](https://github.com/trpc/trpc/pull/7226#discussion_r2900555615)
- PR #7226 `packages/openapi/src/generate.test.ts` — "Shell command built from environment values

This shell command depends on an un..." [link](https://github.com/trpc/trpc/pull/7226#discussion_r2900766446)
- PR #7226 `packages/openapi/src/generate.test.ts` — "Shell command built from environment values

This shell command depends on an un..." [link](https://github.com/trpc/trpc/pull/7226#discussion_r2900766444)

### 제안된 패치

```markdown
---
applyTo: "packages/**/generate.test.ts"
---
# 환경 변수로부터 절대 경로 사용 규칙
- 환경 변수로부터 가져온 절대 경로를 사용하는 셸 명령어는 제어되지 않은 경로 의존성을 초래할 수 있으므로, 경로를 검증하거나 정적 경로로 대체할 것
- 테스트 파일 내에서만 사용하는 경우, 경로를 하드코딩하거나 안전한 경로로 제한하여 사용하도록 유도할 것
- 환경 변수 값이 신뢰할 수 없는 경우, 절대 경로를 사용하는 셸 명령어를 실행하기 전에 검증하는 유틸리티를 활용할 것 (예: `realpath`, `test -d`)
- 경로 의존성을 최소화하기 위해, 환경 변수 대신 정적 또는 구성 파일 기반의 경로를 사용하는 방안을 고려할 것
- 관련 유틸리티 또는 패턴 예시: `readlink -f`, `test -d`, `pushd/popd` 등을 활용하여 안전한 경로 처리 방식을 적용할 것
```

### 이 대상을 선택한 이유

이 패턴은 특정 경로 내 테스트 파일들에서 반복적으로 나타나며, 환경 변수로부터 절대 경로를 직접 사용하는 셸 명령어에 대한 보안 및 안정성 문제를 다룬다. 이는 특정 경로에 국한된 규칙으로 관리하는 것이 적절하다. 전체 에이전트 지침으로 확장하기에는 범위가 좁고, 단일 PR에만 국한되지도 않는다.

### 고려된 대안

- `agents`: 만약 이 규칙이 모든 셸 명령어 사용에 적용된다면 에이전트 지침에 포함할 수 있다.
- `pr_only`: 만약 이 문제가 특정 PR에만 국한된다면 PR 전용으로 관리할 수 있다.

### 실행 명령어

```bash
promote candidate_022 --target path_scoped_rule
promote ignore candidate_022 --reason "..."
promote snooze candidate_022 --days 30
```

---

## 14. packages/tanstack-react-query 테스트 파일에서는 React를 네임스페이스 import 방식으로 사용해야 한다는 규칙이 반복적으로 지적됨

- **ID**: `candidate_023`
- **Target**: `path_scoped_rule` → `packages/tanstack-react-query/CONVENTIONS.md`
- **Confidence**: 0.9500000000000001
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 3 comments
- **Path scope**: `packages/tanstack-react-query/test/**/*.tsx`

### 근거

- PR #7362 `packages/tanstack-react-query/test/client.test.tsx` — "⚠️ Potential issue | 🟠 Major

Use namespace React import in TanStack React Quer..." [link](https://github.com/trpc/trpc/pull/7362#discussion_r3144118971)
- PR #7362 `packages/tanstack-react-query/test/polymorphism.test.tsx` — "⚠️ Potential issue | 🟠 Major

Keep `import  as React` in TanStack React Query t..." [link](https://github.com/trpc/trpc/pull/7362#discussion_r3144118976)
- PR #7362 `packages/tanstack-react-query/test/infiniteQueryOptions.test.tsx` — "⚠️ Potential issue | 🟡 Minor

Restore namespace React import in TanStack test f..." [link](https://github.com/trpc/trpc/pull/7362#discussion_r3144118975)

### 사람 반응

- Agreed: **2** reviewer(s) (@Zelys-DFKH)
  - First agreement: *"Thanks for the review. There's no documented convention in `.coderabbit.yaml` or `CONTRIBUTING.md` requiring namespace i"*

### 제안된 패치

```markdown
---
applyTo: "packages/tanstack-react-query/test/**/*.tsx"
---
# React 네임스페이스 임포트 규칙
- 모든 테스트 파일에서 React를 네임스페이스 방식(`import * as React from 'react'`)으로 임포트해야 합니다.
- `import React from 'react'` 또는 개별 훅 임포트 방식을 사용하지 않도록 합니다.
- 기존 임포트 구문이 있다면, 규칙에 맞게 수정하세요.
- React 관련 사용 시 항상 `React` 네임스페이스를 통해 접근하도록 일관성을 유지하세요.
- 이 규칙은 향후 테스트 파일 작성 시 일관성을 확보하기 위해서만 적용됩니다.
```

### 이 대상을 선택한 이유

이 패턴은 특정 경로(packages/tanstack-react-query/test/)에 한정된 import 스타일 규칙으로, 전체 저장소에 적용되는 일반 규칙이 아니며, 향후 해당 경로 내 테스트 파일 작성 시 일관성을 유지하는 데 도움이 되므로 path_scoped_rule로 분류함

### 고려된 대안

- `agents`: 만약 이 규칙이 저장소 전반에 걸쳐 모든 테스트 파일에 적용된다면 agents가 적합함
- `pr_only`: 한 번만 나타난 특정 PR 관련 규칙이라면 pr_only가 적합함

### 실행 명령어

```bash
promote candidate_023 --target path_scoped_rule
promote ignore candidate_023 --reason "..."
promote snooze candidate_023 --days 30
```

---

## 15. `SKILL.md` 파일명은 md/mdx camelCase 명명 규칙을 위반하지만, 이는 의도된 예외로서 명확히 문서화되어야 한다.

- **ID**: `candidate_027`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 3 comments

### 근거

- PR #7252 `packages/tanstack-react-query/skills/react-query-setup/SKILL.md` — "⚠️ Potential issue | 🟡 Minor

`SKILL.md` filename breaks the md/mdx camelCase n..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937237747)
- PR #7252 `packages/server/skills/adapter-fetch/SKILL.md` — "⚠️ Potential issue | 🟡 Minor

Add `SKILL.md` as an explicit exception in the ca..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937237734)
- PR #7252 `packages/server/skills/trpc-router/SKILL.md` — "⚠️ Potential issue | 🟡 Minor

Rename `SKILL.md` to a camelCase filename to matc..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937237735)

### 제안된 패치

```markdown
## AI 에이전트 지침 규칙

- `SKILL.md` 파일명은 md/mdx camelCase 명명 규칙을 위반하지만, 이는 의도된 예외이므로 `AGENTS.md`에 명확히 문서화한다.
- `SKILL.md`와 유사한 예외 파일명은 모두 `AGENTS.md`에 기록하여 일관성을 유지한다.
- 파일명 규칙 위반 시, 무조건 이름을 변경하지 말고, 예외로 처리할 경우 해당 내용을 명확히 문서화한다.
- `AGENTS.md`에 명시된 예외 규칙은 전사적 코딩 가이드라인에 반영하여 모든 팀원이 참고할 수 있도록 한다.
- 예외 처리 대상 파일은 패턴 또는 특정 파일명(`SKILL.md`)으로 한정하며, 다른 파일명은 규칙 준수 여부를 검토한다.
```

### 이 대상을 선택한 이유

이 패턴은 여러 경로에 걸쳐 반복되며, 파일명 명명 규칙과 예외 처리에 관한 전사적 코딩 가이드라인에 해당하므로 AGENTS.md와 같은 AI 에이전트 지침 문서에 저장하는 것이 적합하다.

### 고려된 대안

- `path_scoped_rule`: 파일명 규칙이 특정 경로에만 적용된다면 경로 범위 규칙으로 저장 가능하다.
- `pr_only`: 특정 PR에만 국한된 경우에만 해당한다.

### 실행 명령어

```bash
promote candidate_027 --target agents
promote ignore candidate_027 --reason "..."
promote snooze candidate_027 --days 30
```

---

## 16. `querySerializer`가 `undefined` 값을 처리할 때 `JSON.stringify(undefined)`가 `undefined`를 반환하여 `URLSearchParams.append()`가 문자열 "undefined"로 변환하는 문제와 `responseTransformer`의 특정 키 검사 문제는 packages/openapi/src/heyapi 경로에 국한된 규칙이다.

- **ID**: `candidate_028`
- **Target**: `path_scoped_rule` → `packages/openapi/README.md`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/openapi/src/heyapi/**`

### 근거

- PR #7231 `packages/openapi/src/heyapi/index.ts` — "`querySerializer` uses `JSON.stringify(value)` for every key; when `value` is `u..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2935826096)
- PR #7231 `packages/openapi/src/heyapi/index.ts` — "`querySerializer` uses `JSON.stringify(value)` for every key; when `value` is `u..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2935826094)

### 제안된 패치

```markdown
---
applyTo: "packages/openapi/src/heyapi/**"
---
# 패키지 내 특정 경로에 대한 규칙
- `querySerializer`에서는 `JSON.stringify`를 호출하기 전에 `value`가 `undefined`인지 확인하여 처리한다.
- `querySerializer`에서 `undefined` 값은 빈 문자열 또는 특정 값으로 대체하도록 수정한다.
- `responseTransformer`는 특정 키 검사 시, 키 존재 여부를 먼저 확인하는 조건문을 추가한다.
- `URLSearchParams.append()` 호출 전에 값이 `undefined`인 경우 문자열 "undefined"로 강제 변환하지 않도록 한다.
- 이 규칙은 `packages/openapi/src/heyapi/index.ts` 파일 내에서만 적용하며, 관련 유틸리티 함수에 대한 테스트를 수행한다.
```

### 이 대상을 선택한 이유

이 패턴은 `querySerializer`와 `responseTransformer`의 동작 방식에 관한 구체적인 문제로, 특정 경로인 packages/openapi/src/heyapi/index.ts에만 적용되므로 범용적인 에이전트 지침보다는 경로 범위 규칙으로 관리하는 것이 적절하다.

### 고려된 대안

- `agents`: 패턴이 여러 경로에 걸쳐 적용되는 일반적인 규칙일 경우
- `pr_only`: 특정 PR에만 국한된 문제일 경우
- `none`: 이미 문서화되어 있거나 중요하지 않은 경우

### 실행 명령어

```bash
promote candidate_028 --target path_scoped_rule
promote ignore candidate_028 --reason "..."
promote snooze candidate_028 --days 30
```

---

## 17. metadata-only 프레임에 대해 onSseEvent 호출을 건너뛰어야 하며, retry: 전용 청크가 data: undefined로 호출되는 문제를 방지해야 한다.

- **ID**: `candidate_032`
- **Target**: `path_scoped_rule` → `packages/openapi/test/routers/**/core/serverSentEvents.gen.ts`
- **Confidence**: 0.95
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/openapi/test/routers/**/core/serverSentEvents.gen.ts`

### 근거

- PR #7231 `packages/openapi/test/routers/appRouter-heyapi/core/serverSentEvents.gen.ts` — "⚠️ Potential issue | 🟠 Major

Skip `onSseEvent` for metadata-only frames.

Hear..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128579)
- PR #7231 `packages/openapi/test/routers/errorFormatterRouter-heyapi/core/serverSentEvents.gen.ts` — "⚠️ Potential issue | 🟠 Major

Skip `onSseEvent` for metadata-only frames.

Hear..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128594)

### 제안된 패치

```markdown
---
applyTo: "packages/openapi/test/routers/**/core/serverSentEvents.gen.ts"
---
# 서버 센트 이벤트 처리 규칙
- 메타데이터 전용 프레임에 대해 `onSseEvent` 호출을 건너뛰도록 합니다.
- `retry:` 전용 청크가 `data: undefined`로 호출되는 것을 방지합니다.
- `StreamEvent.data`가 반드시 정의된 상태로 호출되도록 검증합니다.
- 이 규칙은 서버 센트 이벤트 처리 관련 파일에만 적용됩니다.
- 호출 시점에서 조건에 맞지 않는 이벤트는 무시하거나 건너뛰도록 구현합니다.
```

### 이 대상을 선택한 이유

이 패턴은 serverSentEvents.gen.ts 파일들에서 반복적으로 나타나며, 특정 경로 내에서만 적용되는 규칙으로 보인다. 이는 cross-cutting convention이라기보다는 특정 도메인(서버 센트 이벤트 처리)에 국한된 문제이므로 path_scoped_rule이 적합하다.

### 고려된 대안

- `agents`: 만약 이 규칙이 모든 이벤트 스트림 처리에 걸쳐 일반적으로 적용된다면 agents가 적합할 수 있다.
- `pr_only`: 특정 PR에만 국한된 문제라면 pr_only가 적합하다.

### 실행 명령어

```bash
promote candidate_032 --target path_scoped_rule
promote ignore candidate_032 --reason "..."
promote snooze candidate_032 --days 30
```

---

## 18. 서버 전송 이벤트 재시도 로직에서 초기 연결 시도에 재시도 예산을 사용하지 않고, AbortError 발생 시 재시도하지 않아야 한다는 규칙

- **ID**: `candidate_033`
- **Target**: `path_scoped_rule` → `packages/openapi/test/routers/**/core/serverSentEvents.gen.ts`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/openapi/test/routers/**/core/serverSentEvents.gen.ts`

### 근거

- PR #7231 `packages/openapi/test/routers/errorFormatterRouter-heyapi/core/serverSentEvents.gen.ts` — "⚠️ Potential issue | 🟠 Major

Don't spend the retry budget on the initial conne..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128592)
- PR #7231 `packages/openapi/test/routers/appRouter-heyapi/core/serverSentEvents.gen.ts` — "⚠️ Potential issue | 🟠 Major

Don't spend the retry budget on the initial conne..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128577)

### 제안된 패치

```markdown
---
applyTo: "packages/openapi/test/routers/**/core/serverSentEvents.gen.ts"
---
# 서버 전송 이벤트 재시도 로직 규칙
- 초기 연결 시도 시 재시도 예산을 사용하지 않도록 구현한다.
- `AbortError` 발생 시 재시도를 수행하지 않는다.
- 재시도 시도(`attempt`)는 최초 요청 전에 증가하지 않도록 한다.
- `sseMaxRetryAttempts` 값이 1인 경우, 재시도는 최초 연결 실패에만 적용한다.
- 관련 로직에 주석 또는 문서화하여 유지보수 시 참고할 수 있도록 한다.
```

### 이 대상을 선택한 이유

이 패턴은 serverSentEvents 관련 특정 경로 내에서 재시도 로직의 동작 방식에 관한 구체적인 규칙으로, 여러 파일에서 반복적으로 나타나며 향후 유지보수 및 코드 일관성에 도움이 되므로 경로 범위 규칙으로 보존하는 것이 적절하다.

### 고려된 대안

- `pr_only`: 특정 PR에만 국한된 문제일 경우
- `agents`: 전사적으로 적용되는 일반적인 재시도 정책일 경우
- `none`: 이미 문서화되어 있거나 중요하지 않은 경우

### 실행 명령어

```bash
promote candidate_033 --target path_scoped_rule
promote ignore candidate_033 --reason "..."
promote snooze candidate_033 --days 30
```

---

## 19. GitHub Actions 워크플로우에서 lerna publish 명령어가 git 태깅 및 푸시를 수행하므로, permissions.contents 권한 설정과 lerna 옵션(--no-push, --no-git-tag-version) 간의 일관성을 유지해야 한다.

- **ID**: `candidate_035`
- **Target**: `path_scoped_rule` → `.github/workflows/README.md`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `.github/workflows/**`

### 근거

- PR #7247 `.github/workflows/release-tmp.yml` — "Job now explicitly sets `permissions.contents: read`, but `lerna publish` typica..." [link](https://github.com/trpc/trpc/pull/7247#discussion_r2936535934)
- PR #7247 `.github/workflows/release-canary.yml` — "Job now explicitly sets `permissions.contents: read`, but `lerna publish` typica..." [link](https://github.com/trpc/trpc/pull/7247#discussion_r2936535922)

### 제안된 패치

```markdown
---
applyTo: ".github/workflows/**"
---
# GitHub Actions 워크플로우에서 lerna publish 사용 시 권한 및 옵션 일관성 규칙
- `permissions.contents`를 `read`로 명시하였을 경우, `lerna publish` 명령어에 `--no-push` 및 `--no-git-tag-version` 옵션을 반드시 포함하여 git 태깅 및 푸시를 방지한다.
- `lerna publish`가 git 태깅 및 푸시를 수행하는 기본 동작을 고려하여, 워크플로우 내에서 해당 옵션을 일관되게 사용한다.
- 워크플로우 파일 내에서 `permissions.contents` 설정과 `lerna publish` 옵션이 서로 충돌하지 않도록 주의한다.
- 여러 워크플로우 파일에서 반복되는 규칙이므로, 공통 템플릿 또는 액션을 활용하여 일관성을 유지한다.
- `--no-push`, `--no-git-tag-version` 옵션을 명시하지 않으면, 권한 설정이 `read`로 되어 있어 태그 및 푸시가 실패할 수 있음을 문서화한다.
```

### 이 대상을 선택한 이유

이 패턴은 GitHub Actions 워크플로우 내 특정 경로(.github/workflows/)에 국한된 권한 설정과 lerna publish 사용법에 관한 규칙으로, 여러 워크플로우 파일에서 반복적으로 나타나므로 경로 범위 규칙으로 보존하는 것이 적절하다.

### 고려된 대안

- `pr_only`: 특정 PR에만 관련된 일회성 권한 설정 문제일 경우
- `agents`: 레포 전반에 걸친 권한 및 배포 관행에 관한 일반 규칙일 경우
- `none`: 이미 문서화되어 있거나 중요하지 않은 경우

### 실행 명령어

```bash
promote candidate_035 --target path_scoped_rule
promote ignore candidate_035 --reason "..."
promote snooze candidate_035 --days 30
```

---

## 20. 의존성 변경 시 package.json과 pnpm-lock.yaml 파일을 항상 동기화하여 CI 실패를 방지해야 합니다.

- **ID**: `candidate_037`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments

### 근거

- PR #7200 `packages/react-query/package.json` — "⚠️ Potential issue | 🔴 Critical

Sync lockfile for the Line 98 dependency bump...." [link](https://github.com/trpc/trpc/pull/7200#discussion_r2872430627)
- PR #7200 `package.json` — "⚠️ Potential issue | 🔴 Critical

Regenerate `pnpm-lock.yaml` for these dependen..." [link](https://github.com/trpc/trpc/pull/7200#discussion_r2872430610)

### 제안된 패치

```markdown
## 의존성 변경 시 파일 동기화 규칙

- 의존성을 변경할 때는 반드시 `package.json`과 `pnpm-lock.yaml` 파일을 동시에 업데이트하고 커밋하세요.
- 의존성 변경 후 `pnpm install` 또는 `pnpm update` 명령어를 실행하여 lockfile을 재생성하세요.
- 변경된 `pnpm-lock.yaml` 파일이 `package.json`과 일치하는지 반드시 확인하세요.
- CI 실패 방지를 위해, PR 제출 전에 `pnpm-lock.yaml`이 최신 상태인지 검증하세요.
- 모든 패키지 관리 작업은 프로젝트 루트 또는 관련 패키지 디렉터리에서 수행하며, 변경 사항을 잊지 말고 커밋하세요.
```

### 이 대상을 선택한 이유

이 패턴은 여러 PR에서 반복적으로 나타나는 전사적 관행으로, 의존성 변경 시 lockfile을 반드시 재생성하고 커밋해야 한다는 중요한 개발 규칙을 담고 있습니다. 이는 특정 경로나 개별 PR에 국한되지 않고, 모든 패키지 관리에 적용되므로 agents 문서에 기록하는 것이 적합합니다.

### 고려된 대안

- `pr_only`: 이 규칙이 특정 PR에만 국한되어 적용되는 경우.
- `path_scoped_rule`: 이 규칙이 특정 경로에만 적용되는 경우.

### 실행 명령어

```bash
promote candidate_037 --target agents
promote ignore candidate_037 --reason "..."
promote snooze candidate_037 --days 30
```

---

## 21. 릴리스 워크플로우에서 'pnpm add -g npm@latest' 사용은 비재현적이며, npm 버전을 고정하는 것이 안정적임.

- **ID**: `candidate_038`
- **Target**: `path_scoped_rule` → `docs/release-workflow-rules.md`
- **Confidence**: 0.95
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `.github/workflows/**`

### 근거

- PR #7247 `.github/workflows/release-tmp.yml` — "`pnpm add -g npm@latest` makes releases non-reproducible and can break unexpecte..." [link](https://github.com/trpc/trpc/pull/7247#discussion_r2936535936)
- PR #7247 `.github/workflows/release-canary.yml` — "`pnpm add -g npm@latest` makes releases non-reproducible and can break unexpecte..." [link](https://github.com/trpc/trpc/pull/7247#discussion_r2936535931)

### 제안된 패치

```markdown
---
applyTo: ".github/workflows/**"
---
# 릴리스 워크플로우에서 npm 버전 고정 규칙
- `pnpm add -g npm@latest` 대신, 특정 안정된 npm 버전(예: 8.19.3)으로 고정하여 사용한다.
- npm 버전은 `.npmrc` 또는 환경 변수로 명시적으로 지정한다.
- 릴리스 관련 워크플로우에서는 npm 버전 변경이 필요할 경우, 버전 업데이트 후 테스트를 반드시 수행한다.
- npm 버전 고정은 재현 가능성과 안정성을 위해 필수적임을 인지한다.
- 버전 변경 시, 변경 내역을 문서화하고 관련 테스트를 수행하여 릴리스 안정성을 확보한다.
```

### 이 대상을 선택한 이유

이 패턴은 특정 경로(.github/workflows/) 내 릴리스 관련 워크플로우 파일에 반복적으로 나타나며, npm 버전 고정이라는 구체적이고 실용적인 규칙을 제안하므로 경로 범위 규칙으로 보존하는 것이 적합하다.

### 고려된 대안

- `pr_only`: 특정 PR에만 국한된 지침이지만 반복되어 경로 범위 규칙이 더 적합하다.
- `agents`: 전체 저장소에 적용되는 일반적인 규칙이 아니며, 릴리스 워크플로우에 한정된다.

### 실행 명령어

```bash
promote candidate_038 --target path_scoped_rule
promote ignore candidate_038 --reason "..."
promote snooze candidate_038 --days 30
```

---

## 22. CI 워크플로우에서 사용하는 CLI 버전을 package.json에 명시된 버전과 일치하도록 고정해야 합니다.

- **ID**: `candidate_041`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.95
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments

### 근거

- PR #7252 `.github/workflows/validate-skills.yml` — "⚠️ Potential issue | 🟠 Major

Pin the CLI version used by CI.

Line 29 installs..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937366756)
- PR #7252 `.github/workflows/check-skills.yml` — "⚠️ Potential issue | 🟠 Major

Pin the `@tanstack/intent` version to match the r..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937366751)

### 제안된 패치

```markdown
## CI 워크플로우에서 사용하는 CLI 버전 고정 규칙

- 모든 CI 워크플로우 파일(예: `.github/workflows/*`)에서 사용하는 CLI 버전을 `package.json`에 명시된 버전(`^0.0.20` 등)과 일치하도록 고정하세요.
- `npm install` 또는 관련 명령어를 사용할 때, CLI 패키지의 버전이 최신이거나 미지정 상태로 설치되지 않도록 주의하세요.
- 버전이 일치하지 않는 경우, CI 스크립트 내에서 명시적으로 버전 번호를 지정하거나, `package.json`의 버전과 일치하는지 검증하는 절차를 포함하세요.
- 버전 고정이 누락된 경우, PR 검토 시 반드시 지적하고 수정 요청을 하세요.
- 이 규칙은 저장소 전체에 적용되며, 특정 PR에 한정하지 않고 일관된 버전 관리를 유지하세요.
```

### 이 대상을 선택한 이유

이 패턴은 여러 CI 워크플로우 파일에서 반복적으로 나타나며, CLI 버전 고정은 전체 저장소의 안정성과 일관성을 위해 중요한 관행입니다. 따라서 특정 PR에 국한되지 않고 저장소 전반에 적용되는 규칙으로 AI 에이전트 지침에 포함하는 것이 적절합니다.

### 고려된 대안

- `pr_only`: 이슈가 특정 PR에만 국한될 경우
- `path_scoped_rule`: 특정 경로에만 적용되는 규칙일 경우
- `none`: 이미 문서화되어 있거나 중요하지 않은 경우

### 실행 명령어

```bash
promote candidate_041 --target agents
promote ignore candidate_041 --reason "..."
promote snooze candidate_041 --days 30
```

---

## 23. 마크다운 코드 블록에 언어 식별자를 추가하여 markdownlint MD040 경고를 방지하는 규칙

- **ID**: `candidate_044`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments

### 근거

- PR #7252 `packages/next/skills/nextjs-pages-router/SKILL.md` — "⚠️ Potential issue | 🟡 Minor

Add a language hint to the fenced tree block.

Th..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937237720)
- PR #7252 `packages/next/skills/nextjs-app-router/SKILL.md` — "⚠️ Potential issue | 🟡 Minor

Add a language to the fenced code block (MD040).
..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937303453)

### 제안된 패치

```markdown
## 마크다운 코드 블록 언어 식별자 규칙

- 모든 마크다운 코드 블록(예: ` ``` `)에 적절한 언어 식별자(예: ` ```python `, ` ```javascript ` 등)를 명시하세요.
- `markdownlint MD040` 경고를 방지하기 위해, 코드 블록이 있는 모든 문서에서 언어 식별자를 빠짐없이 추가하세요.
- `docs/`, `src/`, `README.md` 등 주요 문서 파일 내의 코드 블록에 일관되게 적용하세요.
- 언어 식별자를 생략하거나 잘못된 언어를 지정하는 경우 수정하여 경고를 제거하세요.
- 에디터 지원과 가독성을 높이기 위해, 항상 명확한 언어 식별자를 사용하세요.
```

### 이 대상을 선택한 이유

여러 문서에서 반복적으로 마크다운 코드 블록에 언어 식별자를 명시하지 않아 발생하는 markdownlint MD040 경고를 해결하기 위한 일관된 코딩 관행으로, 이는 에디터 지원과 가독성을 높이기 때문에 저장할 가치가 있음

### 고려된 대안

- `pr_only`: 각 PR에만 국한된 문제로 보일 경우
- `none`: 이미 다른 문서에 충분히 문서화되어 있을 경우

### 실행 명령어

```bash
promote candidate_044 --target agents
promote ignore candidate_044 --reason "..."
promote snooze candidate_044 --days 30
```

---

## 필터링된 항목

승격 후보에서 제외된 항목들. 임계치 튜닝이나 팀 리뷰의 엣지 케이스 확인에 유용합니다.

### 승격 불가 (target=none / invalid) (17)

- AI가 대화 중인 인간을 인지하고 대기 상태로 전환하는 안내 메시지 반복 — target=`none`, confidence=0.95
  - 이 코멘트들은 특정 PR이나 코드 품질과 관련된 지식이 아니라 AI의 대화 상태 안내 메시지로, 저장할 가치가 없으며 재사용될 필요도 없음
- 디렉터리 읽기 오류 처리 시 모든 오류를 무시하지 말고, ENOENT(디렉터리 없음) 오류만 무시해야 한다는 반복된 권고. — target=`pr_only`, confidence=0.95
  - 이 패턴은 특정 파일(www/scripts/check-twoslash.ts) 내 특정 코드 라인에서 발생하는 오류 처리 방식에 관한 것으로, 범용적인 규칙이라기보다는 해당 PR들에 국한된 문제이다. 따라서 저장소 전체에 적용할 만한 일반적인 규칙이나 아키텍처 결정이 아니며, 테스트 불변 조건도 아니다. 기존에 문서화된 내용도 없으나, 특정 PR에만 관련된 구체적 문제이므로 pr_only가 적합하다.
- 이 워크플로우의 경로 필터가 실제 문서 디렉토리 위치와 일치하지 않아 문서 변경 시 워크플로우가 트리거되지 않는 문제를 수정해야 한다. — target=`pr_only`, confidence=0.90
  - 이 패턴은 특정 PR에서 발견된 워크플로우 경로 설정 오류에 관한 것으로, 일반적인 규칙이나 아키텍처 결정과는 거리가 있다. 따라서 저장소 전체에 적용할 만한 지침이나 규칙으로 승격하기보다는 해당 PR에 국한된 문제로 간주하는 것이 적절하다.
- `@hey-api/openapi-ts` 패키지의 peerDependencies 최소 버전을 실제 코드에서 사용하는 Plugins 타입을 지원하는 가장 이른 버전으로 엄격하게 조정해야 한다는 반복된 리뷰 코멘트 — target=`pr_only`, confidence=0.90
  - 이 패턴은 특정 PR에서만 나타난 구체적인 버전 호환성 문제에 관한 것으로, 범용적인 규칙이나 아키텍처 결정이 아니며, 다른 경로에도 적용되지 않아 저장소 전체 지침으로 승격할 필요가 없다.
- 오타 'occured'를 'occurred'로 수정하여 사용자에게 노출되는 오류 메시지의 철자 오류를 방지하는 반복된 리뷰 요청입니다. — target=`pr_only`, confidence=0.95
  - 이 패턴은 특정 PR 내에서 발생한 오타 수정에 관한 것으로, 범용적인 규칙이나 아키텍처적 결정이 아니며, 다른 PR이나 경로에 일반화하기 어렵습니다. 따라서 개별 PR에 한정된 지식으로 보존하는 것이 적절합니다.
- pnpm-lock.yaml 파일에서 일부 워크스페이스가 여전히 vitest 3.x 버전을 참조하고 있어, 모노레포 전체를 vitest 4로 업그레이드하려면 해당 워크스페이스의 package.json도 함께 업데이트해야 한다. — target=`pr_only`, confidence=0.90
  - 이 패턴은 특정 PR에서 모노레포 내 vitest 버전 일관성 문제를 지적하는 것으로, 일반적인 코딩 규칙이나 아키텍처 결정과는 다르며, 특정 PR 상황에 국한된 지식이다. 따라서 전체 저장소에 적용되는 규칙이나 테스트 불변성으로 보기는 어렵다.
- 테스트에서 오타가 있는 메시지 "occured"를 사용하고 있으며, 런타임 메시지가 "occurred"로 수정되면 테스트도 함께 업데이트해야 한다. — target=`pr_only`, confidence=0.90
  - 이 패턴은 특정 PR 내 여러 테스트 파일에서 동일한 오타 문제를 지적하는 것으로, 테스트 코드 내 하드코딩된 메시지와 런타임 메시지의 일치 여부에 관한 구체적인 수정 지침이다. 이는 특정 PR과 관련된 구체적이고 일회성 문제로, 전사적 규칙이나 아키텍처 결정과는 거리가 있어 pr_only로 분류하는 것이 적절하다.
- 파일 헤더 주석에 명시된 경로와 실제 파일 경로가 일치하지 않아 혼란을 초래할 수 있으므로 주석 또는 파일 위치를 일치시켜야 한다. — target=`pr_only`, confidence=0.90
  - 이 패턴은 특정 파일에 대한 주석과 실제 경로 불일치 문제로, 특정 PR 내에서만 의미가 있으며, 범용적인 규칙이나 아키텍처 결정과는 거리가 있다. 따라서 저장소 전체에 적용할 필요가 없고, 향후 유사한 문제 발생 시 해당 PR 문맥에서 참고하는 것이 적절하다.
- PR 설명과 실제 exports 키의 경로 명명 규칙 불일치로 인해 소비자가 문서화된 경로로 임포트할 수 없는 문제가 반복적으로 지적됨 — target=`pr_only`, confidence=0.90
  - 이 문제는 특정 PR에서만 발생하는 경로 명명 불일치에 관한 것으로, 범용적인 규칙이나 아키텍처 결정이 아니라 PR 단위의 구체적 수정 사항에 해당하므로 pr_only로 분류함
- 이 헬퍼 함수는 subscribe 호출 중 onError가 동기적으로 실행될 때 ReferenceError를 발생시킬 수 있으므로, let으로 변수를 미리 선언하고 할당 후 구독 해제해야 한다. — target=`pr_only`, confidence=0.90
  - 이 패턴은 특정 테스트 파일 내 subscribe 함수 사용 시 발생하는 일회성 코드 문제로, 범용적인 규칙이나 아키텍처 결정이 아니며, 다른 경로나 전역 규칙에 적용되지 않으므로 PR 단위로 관리하는 것이 적절하다.
- 여러 패키지의 package.json에서 존재하지 않는 ./bin/intent.js 파일을 bin.entry로 참조하여 실행 파일이 깨지는 문제가 반복적으로 지적됨. — target=`pr_only`, confidence=0.95
  - 이 문제는 특정 PR에서 여러 패키지의 package.json 설정에 한정된 구체적인 문제로, 범용적인 규칙이나 아키텍처 결정과는 거리가 있음. 또한, 이 문제는 각 패키지별로 개별적으로 해결되어야 하므로 저장소 전체에 적용할 일반 규칙으로 승격하기 부적절함.
- `getRouterInputsHash()` 함수가 여러 라우터마다 전체 파일을 반복적으로 읽고 해시를 계산하여 성능 저하를 유발함. — target=`pr_only`, confidence=0.95
  - 이 패턴은 특정 PR에서 발견된 성능 문제로, 구체적인 코드와 상황에 국한되어 있어 범용적인 규칙이나 아키텍처 결정으로 보기는 어렵다. 따라서 향후 참고를 위해 PR 단위로 보존하는 것이 적절하다.
- 도움말 및 주석에서 OpenAPI 버전이 3.0으로 명시되어 있으나 실제 생성기는 3.1.1 버전을 출력하므로 문서 불일치 문제를 수정해야 한다. — target=`pr_only`, confidence=0.90
  - 이 패턴은 특정 PR 내에서 문서 주석의 버전 불일치 문제를 정확히 지적하고 수정한 사례로, 범용적인 규칙이나 아키텍처적 결정이 아니라 특정 코드 변경에 국한된 내용이다. 따라서 저장소 전체에 적용할 만한 일반적인 규칙이나 지침으로 보기 어렵다.
- 이 패턴은 스트리밍 응답의 content-type이 application/json에서 application/jsonl로 변경되어야 한다는 반복된 리뷰 지적입니다. — target=`pr_only`, confidence=0.90
  - 이 지식은 특정 PR(#7345)과 관련된 구체적인 변경 사항에 국한되어 있으며, 범용적인 규칙이나 아키텍처 결정이 아니므로 전체 저장소 지침에 포함시키기보다는 해당 PR 문서에만 보존하는 것이 적절합니다.
- @ts-expect-error 주석이 호출되는 훅 이름과 일치하지 않아 혼동을 줄 수 있음 — target=`pr_only`, confidence=0.95
  - 이 패턴은 특정 PR 내 테스트 파일에서 발생한 주석 오류로, 일반적인 코딩 규칙이나 아키텍처 결정과는 무관하며, 향후 다른 코드에 일반화하기 어렵기 때문에 저장 대상은 PR 단위로 제한하는 것이 적절함.
- 사용자에게 노출되는 오류 메시지에서 'occured' 오타를 'occurred'로 수정하는 반복된 리뷰 코멘트 — target=`pr_only`, confidence=0.95
  - 이 패턴은 특정 PR과 파일 내의 한정된 오타 수정에 관한 것으로, 범용적인 규칙이나 아키텍처 결정이 아니며, 테스트 스냅샷과 연관되어 있어 PR 단위로 관리하는 것이 적절하다.
- 문서 내 오타 수정에 관한 반복된 리뷰 코멘트 패턴 — target=`pr_only`, confidence=0.95
  - 이 패턴은 특정 PR 내 문서 파일에서 발견된 개별 오타 수정에 관한 것으로, 범용적인 규칙이나 아키텍처적 결정과 관련이 없으며, 특정 PR에만 국한된 내용이므로 pr_only로 분류함.

### 분류 단계에서 실패 (5)

- failed: No object generated: response did not match schema.
  - No object generated: response did not match schema.
- failed: No object generated: response did not match schema.
  - No object generated: response did not match schema.
- failed: No object generated: response did not match schema.
  - No object generated: response did not match schema.
- failed: No object generated: response did not match schema.
  - No object generated: response did not match schema.
- failed: No object generated: response did not match schema.
  - No object generated: response did not match schema.

---
