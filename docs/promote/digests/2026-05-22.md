# 메모리 승격 다이제스트 — trpc/trpc
> 생성일: 2026-05-22 · promote v0.6.0

## 설정

- Provider: `openai` (embeddings + HAC + llmRefine)
- Models: `gpt-4.1-mini` (classify), `gpt-4.1-nano` (draft), `text-embedding-3-small` (embed)
- Output language: `ko`
- Thresholds: similarity `0.8`, min confidence `0.75`, min occurrences `2`
- Window: `90` days (overridden via --since; config default is 60)
- Privacy: redactSecrets=`true`, sendDiffHunksToLLM=`false`

## 요약

- Scanned: **423** review comments across **62** PRs
- AI reviewer comments: **361**
- Clusters found: **274**
- Repeated clusters: **45**
- Promotion candidates: **21**
- Estimated cost: $0.0603
- Timings: fetch 5.7s · normalize 26ms · cluster 44.7s · conv-fetch 5.8s · reply-ctx 11.6s · memory 958ms · classify+draft 1m 9s · **total 2m 18s**

---

## 1. 통합 테스트에서 서버와 클라이언트 리소스 관리를 위해 `testServerAndClientResource` 패턴을 일관되게 사용하도록 권장하는 리팩토링 제안입니다.

- **ID**: `candidate_001`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.9
- **Scope**: 🌐 cross-PR (3 PRs)
- **Occurrences**: 3 comments

### 근거

- PR #7231 `packages/openapi/test/generate.test.ts` — "🛠️ Refactor suggestion | 🟠 Major

Use `testServerAndClientResource` for these ..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902034597)
- PR #7207 `packages/tests/server/httpSubscriptionLink.test.ts` — "🛠️ Refactor suggestion | 🟠 Major

Use `testServerAndClientResource` directly w..." [link](https://github.com/trpc/trpc/pull/7207#discussion_r2879708223)
- PR #7304 `packages/tests/server/httpSubscriptionLink.fetch.test.ts` — "🛠️ Refactor suggestion | 🟠 Major

Use the standard `testServerAndClientResourc..." [link](https://github.com/trpc/trpc/pull/7304#discussion_r3012813221)

### 제안된 패치

```markdown
## 테스트 리소스 관리 패턴 안내

- 모든 통합 테스트에서 서버와 클라이언트 리소스 관리를 위해 `testServerAndClientResource` 패턴을 일관되게 사용하세요.
- 개별 테스트에서 서버와 클라이언트의 생명주기를 수동으로 관리하는 코드를 제거하고, `testServerAndClientResource`를 활용하세요.
- `await using` 구문과 함께 `testServerAndClientResource`를 적용하여 리소스 해제와 정리를 자동화하세요.
- `konn()` 또는 유사한 커스텀 래퍼 대신 표준 패턴인 `testServerAndClientResource`를 사용하여 일관성을 유지하세요.
- 특정 경로나 PR에 국한되지 않으며, 저장소 전체 테스트 코드에 이 패턴을 적용하여 유지보수성과 코드 품질을 향상시키세요.
```

### 이 대상을 선택한 이유

여러 PR에서 반복적으로 동일한 테스트 리소스 관리 패턴(`testServerAndClientResource`) 사용을 권장하고 있어, 이는 저장소 전반에 걸친 테스트 작성 관례로 볼 수 있습니다. 특정 경로나 개별 PR에 국한되지 않고, 테스트 작성 시 일관된 리소스 관리 방법을 안내하는 것이 향후 유지보수와 코드 품질 향상에 도움이 되므로 에이전트 지침 문서에 저장하는 것이 적합합니다.

### 고려된 대안

- `pr_only`: 각 코멘트가 특정 PR의 코드에만 적용되는 경우.
- `path_scoped_rule`: 특정 경로에만 적용되는 규칙인 경우.
- `none`: 이미 문서화되어 있거나 일회성 제안인 경우.

### 실행 명령어

```bash
promote candidate_001 --target agents
promote ignore candidate_001 --reason "..."
promote snooze candidate_001 --days 30
```

---

## 2. www/scripts/check-twoslash.ts 파일 내에서 인덱스 또는 캡처 접근 시 non-null assertion 연산자(!) 사용을 제거하고 명시적 null 체크 또는 optional chaining으로 대체해야 한다.

- **ID**: `candidate_002`
- **Target**: `path_scoped_rule` → `www/scripts/CODING_GUIDELINES.md`
- **Confidence**: 0.95
- **Scope**: 🌐 cross-PR (3 PRs)
- **Occurrences**: 3 comments
- **Path scope**: `www/scripts/**`

### 근거

- PR #7196 `www/scripts/check-twoslash.ts` — "⚠️ Potential issue | 🟠 Major

Remove non-null assertions from indexed/capture a..." [link](https://github.com/trpc/trpc/pull/7196#discussion_r2869759038)
- PR #7176 `www/scripts/check-twoslash.ts` — "⚠️ Potential issue | 🟠 Major

Replace non-null assertions with guards/optional ..." [link](https://github.com/trpc/trpc/pull/7176#discussion_r2876202469)
- PR #7195 `www/scripts/check-twoslash.ts` — "⚠️ Potential issue | 🟡 Minor

Remove non-null assertions in this file to mainta..." [link](https://github.com/trpc/trpc/pull/7195#discussion_r2869717508)

### 제안된 패치

```markdown
---
applyTo: "www/scripts/**"
---
# www/scripts/check-twoslash.ts 규칙 적용 가이드라인
- 인덱스 또는 캡처 접근 시 사용된 `!` 연산자를 제거하고, 명시적 null 체크 또는 옵셔널 체이닝(`?.`)으로 대체하세요.
- 48, 58, 67, 148, 149 라인에서의 `!` 사용을 모두 수정하세요.
- null 체크를 위해 조건문 또는 옵셔널 체이닝을 활용하여 안전성을 확보하세요.
- 해당 파일 내에서만 적용되며, 다른 파일에는 영향을 주지 않습니다.
- 일관된 코드 스타일 유지를 위해 반드시 수정 후 테스트를 수행하세요.
```

### 이 대상을 선택한 이유

이 패턴은 특정 파일(www/scripts/check-twoslash.ts)에 반복적으로 나타나는 코딩 가이드라인 위반으로, 해당 경로에 국한된 규칙으로 관리하는 것이 적절하다. 이는 범용적인 에이전트 지침보다는 특정 경로에 적용되는 규칙으로 분류하는 것이 맞으며, 아키텍처 결정(adr)이나 테스트(invariant)와도 거리가 있다.

### 고려된 대안

- `agents`: 만약 이 규칙이 모든 파일에 걸쳐 일관되게 적용된다면 에이전트 지침에 포함할 수 있다.
- `pr_only`: 특정 PR에만 국한된 문제라면 PR 전용으로 관리할 수 있다.

### 실행 명령어

```bash
promote candidate_002 --target path_scoped_rule
promote ignore candidate_002 --reason "..."
promote snooze candidate_002 --days 30
```

---

## 3. `@ts-expect-error` 지시문에 ESLint 요구사항에 따라 3자 이상의 설명을 추가해야 한다는 규칙이 반복적으로 지적됨.

- **ID**: `candidate_003`
- **Target**: `path_scoped_rule` → `eslint.config.js`
- **Confidence**: 0.95
- **Scope**: 🌐 cross-PR (2 PRs)
- **Occurrences**: 4 comments
- **Path scope**: `packages/openapi/test/routers/**/client/client.gen.ts`

### 근거

- PR #7231 `packages/openapi/test/routers/superjsonRouter-heyapi/client/client.gen.ts` — "⚠️ Potential issue | 🟡 Minor

Add description to `@ts-expect-error` directive.
..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128597)
- PR #7283 `packages/openapi/test/routers/cyclicTypesRouter-heyapi/client/client.gen.ts` — "⚠️ Potential issue | 🟡 Minor

Add description to `@ts-expect-error` directive.
..." [link](https://github.com/trpc/trpc/pull/7283#discussion_r2977549460)
- PR #7231 `packages/openapi/test/routers/appRouter-heyapi/client/client.gen.ts` — "⚠️ Potential issue | 🟡 Minor

Add description to `@ts-expect-error` directive.
..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128573)
- PR #7231 `packages/openapi/test/routers/errorFormatterRouter-heyapi/client/client.gen.ts` — "⚠️ Potential issue | 🟡 Minor

Add a reason to the `@ts-expect-error` or remove ..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128580)

### 제안된 패치

```markdown
---
applyTo: "packages/openapi/test/routers/**/client/client.gen.ts"
---
# @ts-expect-error 지시문에 설명 추가 규칙
- `@ts-expect-error` 지시문에는 최소 3자 이상의 설명을 반드시 포함시킨다.
- ESLint 규칙에 따라, 각 지시문에 대한 이유를 명확히 기재한다.
- 설명은 간결하면서도 해당 오류 무시의 필요성을 설명해야 한다.
- 자동 생성된 클라이언트 코드 내에서 일관된 주석 작성 방식을 유지한다.
- 규칙 위반 시 ESLint 경고가 발생하므로, 정기적으로 검토한다.
```

### 이 대상을 선택한 이유

이 패턴은 여러 auto-generated client 코드 파일 내에서 반복적으로 나타나며, ESLint 규칙에 따른 일관된 주석 작성 요구사항으로 특정 경로 내 코드에 적용되는 규칙임. 따라서 전체 레포가 아닌 특정 경로에 국한된 규칙으로 분류하는 것이 적절함.

### 고려된 대안

- `agents`: 레포 전반에 걸친 코딩 컨벤션으로 볼 수 있으나, 이 경우는 특정 auto-generated 코드 경로에 한정됨.
- `pr_only`: 여러 PR에서 반복되었으므로 일회성 이슈가 아님.

### 실행 명령어

```bash
promote candidate_003 --target path_scoped_rule
promote ignore candidate_003 --reason "..."
promote snooze candidate_003 --days 30
```

---

## 4. `@types/node` 버전은 Node.js 주요 버전과 일치시켜야 하며, `engines.node` 설정과 맞지 않으면 버전 불일치 문제가 발생한다.

- **ID**: `candidate_005`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.95
- **Scope**: 🌐 cross-PR (2 PRs)
- **Occurrences**: 2 comments

### 근거

- PR #7210 `package.json` — "⚠️ Potential issue | 🟠 Major

Downgrade `@types/node` to `^24.x` or update `eng..." [link](https://github.com/trpc/trpc/pull/7210#discussion_r2884107893)
- PR #7200 `package.json` — "⚠️ Potential issue | 🟠 Major

Update `@types/node` to align with Node 24 runtim..." [link](https://github.com/trpc/trpc/pull/7200#discussion_r2872399545)

### 제안된 패치

```markdown
## Node.js 버전 및 타입 정의 일관성 규칙

- `@types/node` 버전은 반드시 프로젝트의 Node.js 주요 버전과 일치시켜야 하며, `package.json`의 `engines.node` 설정과 일치하도록 유지한다.
- `@types/node` 버전은 Node.js 버전이 24.x인 경우 `^24.x`로, 25.x인 경우 `^25.x`로 업데이트한다.
- `@types/node`와 `engines.node` 간 버전 불일치가 발견되면 즉시 수정하거나 관련 PR에 적절한 수정 내용을 반영한다.
- 이 규칙은 `packages/*` 또는 전체 프로젝트 내의 모든 관련 파일(`package.json`, `tsconfig.json` 등)에 적용하며, 특정 경로나 PR에 한정하지 않는다.
- 버전 일관성 유지에 관한 내용은 `AGENTS.md` 또는 관련 문서에 명확히 기록하여 개발자와 AI 에이전트가 참고할 수 있도록 한다.
```

### 이 대상을 선택한 이유

이 패턴은 여러 PR에서 반복적으로 나타나는 Node.js 런타임 버전과 타입 정의 버전 간의 일관성 유지에 관한 전사적 규칙으로, 향후 개발자와 AI 에이전트가 참고할 수 있도록 저장하는 것이 유용하다. 특정 경로나 개별 PR에 국한되지 않고, Node.js 버전 관리에 관한 일반적인 관행이므로 agents에 적합하다.

### 고려된 대안

- `pr_only`: 각 PR에만 국한된 일회성 문제로 볼 수도 있으나, 반복적이고 전사적 규칙임.
- `path_scoped_rule`: 특정 경로에 한정된 규칙이 아니므로 부적합.

### 실행 명령어

```bash
promote candidate_005 --target agents
promote ignore candidate_005 --reason "..."
promote snooze candidate_005 --days 30
```

---

## 5. 레포지토리 내 pnpm 버전 불일치 문제로 인해 asdf/rtx 환경에서 설치 재현성이 깨질 수 있으므로, .tool-versions와 package.json의 pnpm 버전을 일치시켜야 한다.

- **ID**: `candidate_006`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.95
- **Scope**: 🌐 cross-PR (2 PRs)
- **Occurrences**: 2 comments
- **Path scope**: `.tool-versions`

### 근거

- PR #7194 `.tool-versions` — "The repo declares pnpm 9.x via package.json (engines/packageManager), but .tool-..." [link](https://github.com/trpc/trpc/pull/7194#discussion_r2869277485)
- PR #7200 `.tool-versions` — "`.tool-versions` still pins `pnpm 8.15.5`, but the repo’s root `package.json` de..." [link](https://github.com/trpc/trpc/pull/7200#discussion_r2872405266)

### 제안된 패치

```markdown
## pnpm 버전 일치 규칙

- `.tool-versions` 파일 내 pnpm 버전과 `package.json`의 `engines.pnpm` 또는 `packageManager` 필드의 버전을 반드시 일치시킨다.
- `.tool-versions`에 pnpm 8.15.5로 명시되어 있다면, `package.json`의 `packageManager`와 `engines.pnpm`도 동일한 버전(예: pnpm@8.15.5 또는 ^8.15.5)으로 수정한다.
- 버전 불일치가 발견될 경우, 즉시 `.tool-versions` 또는 `package.json`을 수정하여 일치시킨다.
- 이 규칙은 레포지토리 전반에 적용하며, 새로 PR을 생성하거나 수정할 때 반드시 준수한다.
- 규칙 위반 시, AI 에이전트는 수정 요청 또는 알림을 통해 일치시킬 것을 권고한다.
```

### 이 대상을 선택한 이유

이 패턴은 레포지토리 전반에 걸쳐 적용되는 도구 버전 관리 규칙으로, 여러 PR에서 반복적으로 지적되고 있어 향후 개발자 및 AI 에이전트가 일관된 환경을 유지하는 데 도움이 되므로 에이전트 지침 문서에 저장하는 것이 적합하다.

### 고려된 대안

- `pr_only`: 이 지적은 특정 PR에만 국한된 일회성 문제이므로 저장할 필요가 없다.
- `path_scoped_rule`: 비록 특정 경로(.tool-versions)에 관련된 내용이지만, 이 규칙은 도구 버전 관리라는 전사적 관행에 해당한다.

### 실행 명령어

```bash
promote candidate_006 --target agents
promote ignore candidate_006 --reason "..."
promote snooze candidate_006 --days 30
```

---

## 6. 테스트 파일에서 beforeAll을 vitest에서 import하지 않으면 런타임 에러가 발생하므로, vitest에서 describe/it/expect와 함께 beforeAll을 반드시 import해야 한다.

- **ID**: `candidate_007`
- **Target**: `path_scoped_rule` → `packages/openapi/test/README.md`
- **Confidence**: 0.95
- **Scope**: 🌐 cross-PR (2 PRs)
- **Occurrences**: 4 comments
- **Path scope**: `packages/openapi/test/**`

### 근거

- PR #7231 `packages/openapi/test/edgeCases.test.ts` — "`beforeAll` is used but not imported from Vitest, which will cause the test file..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2935826088)
- PR #7231 `packages/openapi/test/edgeCases.test.ts` — "`beforeAll` is used but not imported from Vitest, which will cause the test file..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2935826082)
- PR #7283 `packages/openapi/test/cyclicTypes.test.ts` — "`beforeAll` is used in this test but not imported from vitest, which will throw ..." [link](https://github.com/trpc/trpc/pull/7283#discussion_r2977455435)
- PR #7283 `packages/openapi/test/cyclicTypes.test.ts` — "`beforeAll` is used but not imported from vitest, so this test file will throw a..." [link](https://github.com/trpc/trpc/pull/7283#discussion_r2977503215)

### 제안된 패치

```markdown
---
applyTo: "packages/openapi/test/**"
---
# 패키지 openapi 테스트 파일에 대한 import 규칙
- `beforeAll`을 사용할 경우, 반드시 `vitest`에서 `describe`, `it`, `expect`와 함께 `import`해야 합니다.
- `beforeAll`을 import하지 않으면 런타임 에러(ReferenceError)가 발생하므로, 누락된 경우 즉시 수정하세요.
- `import { beforeAll } from 'vitest'` 구문을 테스트 파일 상단에 추가하세요.
- 이미 `describe`, `it`, `expect`를 import하는 경우, `beforeAll`도 함께 추가하세요.
- 불필요한 `beforeAll` 사용 시, import 없이 제거하는 것도 고려하세요.
```

### 이 대상을 선택한 이유

이 패턴은 vitest를 사용하는 테스트 파일 내에서 반복적으로 발생하는 구체적인 import 규칙으로, 특정 경로(테스트 파일들)에 국한된 규칙이다. 따라서 전체 에이전트 지침보다는 path_scoped_rule로 관리하는 것이 적합하다.

### 고려된 대안

- `agents`: 만약 이 규칙이 모든 테스트 파일에 공통적으로 적용된다면 에이전트 지침에 포함할 수 있다.
- `pr_only`: 각 PR에만 국한된 문제라면 PR 전용으로 관리할 수 있다.

### 실행 명령어

```bash
promote candidate_007 --target path_scoped_rule
promote ignore candidate_007 --reason "..."
promote snooze candidate_007 --days 30
```

---

## 7. 인터셉터 루프 내에서 변환된 에러를 체인으로 전달해야 한다는 반복된 지적

- **ID**: `candidate_008`
- **Target**: `path_scoped_rule` → `packages/openapi/test/routers/**`
- **Confidence**: 0.95
- **Scope**: 🌐 cross-PR (2 PRs)
- **Occurrences**: 2 comments
- **Path scope**: `packages/openapi/test/routers/**`

### 근거

- PR #7283 `packages/openapi/test/routers/cyclicTypesRouter-heyapi/client/client.gen.ts` — "⚠️ Potential issue | 🟠 Major

Chain the transformed error through the intercept..." [link](https://github.com/trpc/trpc/pull/7283#discussion_r2977503548)
- PR #7231 `packages/openapi/test/routers/errorFormatterRouter-heyapi/client/client.gen.ts` — "⚠️ Potential issue | 🟠 Major

Chain transformed errors through the interceptor ..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128585)

### 제안된 패치

```markdown
---
applyTo: "packages/openapi/test/routers/**"
---
# 인터셉터 에러 체인 전달 규칙
- 인터셉터 내에서 변환된 에러는 반드시 다음 인터셉터에 전달하기 위해 체인으로 연결해야 합니다.
- 각 인터셉터는 이전 단계에서 변환된 에러를 인수로 받아야 하며, 원본 에러 대신 변환된 에러를 전달해야 합니다.
- 에러 체인 전달 시, `next(error)` 또는 유사한 패턴을 사용하여 변환된 에러를 넘겨야 합니다.
- 에러 변환 후, 원본 에러 대신 변환된 에러를 계속해서 전달하는 일관된 패턴을 유지하세요.
- 이 규칙은 오류 처리의 일관성과 디버깅 용이성을 위해 필수적입니다.
```

### 이 대상을 선택한 이유

두 코멘트 모두 특정 경로 내에서 인터셉터가 원본 에러만을 사용하여 변환된 에러가 다음 인터셉터에 전달되지 않는 문제를 지적하고 있어, 이 규칙은 해당 경로에 국한된 코드 품질 및 오류 처리 규칙으로 판단됨

### 고려된 대안

- `pr_only`: 특정 PR에만 해당되는 일회성 문제일 경우
- `agents`: 전체 저장소에 적용되는 일반적인 에러 처리 규칙일 경우
- `test`: 런타임에서 반드시 검증되어야 하는 불변 조건일 경우

### 실행 명령어

```bash
promote candidate_008 --target path_scoped_rule
promote ignore candidate_008 --reason "..."
promote snooze candidate_008 --days 30
```

---

## 8. `maxBatchSize` 설정에도 불구하고 `opts.path.split(',')`가 먼저 배열을 할당하여 DoS 공격에 취약할 수 있으므로, 배열 할당 전에 경로를 스캔하여 크기를 제한하는 방법을 적용해야 한다.

- **ID**: `candidate_013`
- **Target**: `path_scoped_rule` → `packages/server/src/unstable-core-do-not-import/http/CONTENT_TYPE_BATCH_LIMIT.md`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/server/src/unstable-core-do-not-import/http/**`

### 근거

- PR #7191 `packages/server/src/unstable-core-do-not-import/http/contentType.ts` — "Even with `maxBatchSize` set, `opts.path.split(',')` will allocate an array prop..." [link](https://github.com/trpc/trpc/pull/7191#discussion_r2867601933)
- PR #7191 `packages/server/src/unstable-core-do-not-import/http/contentType.ts` — "`opts.path.split(',')` allocates an array with one entry per comma before the si..." [link](https://github.com/trpc/trpc/pull/7191#discussion_r2974473905)

### 제안된 패치

```markdown
---
applyTo: "packages/server/src/unstable-core-do-not-import/http/**"
---
# 경로 크기 제한 및 DoS 방어 가이드
- `opts.path.split(',')` 호출 전에 `opts.path`의 길이 또는 콤마 개수를 검사하여, 배열 할당 크기를 제한한다.
- 정규 표현식 또는 문자열 길이 검사를 통해 허용된 경로 길이 또는 콤마 개수 범위 내인지 검증한다.
- `maxBatchSize`와 별개로, 경로 내 콤마 개수에 따른 배열 크기 증가를 방지하는 로직을 추가한다.
- 경로 검증 후, 안전한 크기 범위 내에서만 `split(',')`을 수행하도록 한다.
- `path.split(',')` 호출 전에 `String.prototype.match()` 또는 `String.prototype.length`를 활용하여 크기 제한을 적용한다.
```

### 이 대상을 선택한 이유

이 패턴은 특정 파일 경로 내에서 `maxBatchSize`와 관련된 DoS 방어를 위한 배열 할당 최적화 규칙에 관한 것으로, 범용적인 에이전트 지침보다는 해당 경로에 국한된 규칙으로 관리하는 것이 적절하다.

### 고려된 대안

- `agents`: 만약 이 규칙이 여러 경로에 걸쳐 적용된다면 에이전트 지침에 포함할 수 있으나, 현재는 특정 경로에 한정됨.
- `pr_only`: 일회성 PR 관련 문제라면 PR 전용으로 관리할 수 있으나, 반복적으로 나타나는 보안 관련 규칙임.

### 실행 명령어

```bash
promote candidate_013 --target path_scoped_rule
promote ignore candidate_013 --reason "..."
promote snooze candidate_013 --days 30
```

---

## 9. 패키지의 bin 실행 파일을 올바르게 배포하기 위해 package.json의 files 배열에 bin 디렉토리를 포함해야 한다.

- **ID**: `candidate_014`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.95
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 10 comments

### 근거

- PR #7252 `packages/server/package.json` — "The package defines a `bin` entry (`./bin/intent.js`), but `bin/` is not include..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968125965)
- PR #7252 `packages/tanstack-react-query/package.json` — "The package exports an `intent` binary but `bin/` is not included in the `files`..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968125922)
- PR #7252 `packages/client/package.json` — "The package now exposes an `intent` binary, but `bin/` is not part of the `files..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968125972)
- PR #7252 `packages/next/package.json` — "The package declares `"bin": { "intent": "./bin/intent.js" }` but `bin/` is not ..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968125893)
- PR #7252 `packages/client/package.json` — "`bin.intent` points to `./bin/intent.js`, but there is no `packages/client/bin/`..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937362621)
- ... and 5 more

### 제안된 패치

```markdown
## 패키지 배포 규칙: bin 실행 파일 포함

- `package.json`의 `files` 배열에 반드시 `bin` 디렉토리(`bin/`)를 포함시킨다.
- `bin/` 디렉토리 내의 실행 파일들이 배포 시 누락되지 않도록 한다.
- `bin`이 누락될 경우, `bin` 엔트리로 지정된 실행 파일이 배포되지 않아 바이너리 작동에 문제가 발생한다.
- 이 규칙은 여러 패키지에 공통적으로 적용되며, repo 전체에 일관성을 유지한다.
- `files` 배열에 `bin`을 추가하는 변경 사항을 반드시 커밋하여 배포 전에 반영한다.
```

### 이 대상을 선택한 이유

여러 패키지에서 반복적으로 발생하는 문제로, bin 실행 파일이 files allowlist에 포함되지 않아 배포 시 누락되는 현상을 방지하기 위한 공통적인 패키지 관리 규칙이다. 이는 특정 경로에 국한되지 않고 여러 패키지에 적용되므로 repo-wide AI 지침에 포함하는 것이 적절하다.

### 고려된 대안

- `pr_only`: 특정 PR에만 해당되는 일회성 문제로 보기에는 여러 패키지에서 반복되어 일반화 가능하다.
- `path_scoped_rule`: 문제가 특정 경로에 국한되지 않고 여러 패키지에 걸쳐 발생한다.

### 실행 명령어

```bash
promote candidate_014 --target agents
promote ignore candidate_014 --reason "..."
promote snooze candidate_014 --days 30
```

---

## 10. 여러 @trpc/* 패키지에서 동일한 'intent' 바이너리를 노출하면 node_modules/.bin/intent 충돌이 발생할 수 있으므로, 고유한 네임스페이스가 있는 바이너리 이름을 사용해야 한다.

- **ID**: `candidate_016`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.95
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 7 comments

### 근거

- PR #7252 `packages/openapi/package.json` — "Adding an `intent` binary here is likely to create `node_modules/.bin/intent` co..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968259354)
- PR #7252 `packages/openapi/package.json` — "This package adds an `intent` binary. Since other @trpc/* packages in this PR al..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968125941)
- PR #7252 `packages/server/package.json` — "Exposing a generic `intent` binary from this package is likely to cause `node_mo..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968259331)
- PR #7252 `packages/client/package.json` — "Exposing a generic `intent` binary from this package is likely to cause `node_mo..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968259335)
- PR #7252 `packages/next/package.json` — "Exposing a generic `intent` binary from this package is likely to cause `node_mo..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968259301)
- ... and 2 more

### 제안된 패치

```markdown
## 바이너리 네이밍 규칙

- 여러 `@trpc/*` 패키지에서 동일한 바이너리 이름(예: `intent`)을 노출하지 않도록 하며, 고유한 네임스페이스가 포함된 이름을 사용한다. 예: `@trpc/intent-cli`, `@trpc/intent-tool`.
- 바이너리 이름은 `package.json`의 `bin` 필드에 명확히 정의하며, 충돌 방지를 위해 일관된 네이밍 규칙을 따른다.
- 전체 저장소에 걸쳐 일관된 네이밍 규칙을 적용하여, 여러 패키지 간 바이너리 충돌 가능성을 방지한다.
- 바이너리 이름이 충돌할 경우, 사용자 환경에서 `node_modules/.bin/` 내에서 충돌이 발생할 수 있음을 유념한다.
- 이 규칙은 특정 PR이나 경로에 국한되지 않고, 전체 저장소의 패키지에 적용한다.
```

### 이 대상을 선택한 이유

이 패턴은 여러 패키지에 걸쳐 반복적으로 나타나는 네이밍 충돌 문제로, 향후 모든 관련 패키지에서 일관된 네임스페이스 규칙을 적용하는 것이 중요하다. 따라서 특정 PR이나 경로에 국한되지 않고, 전체 저장소에 적용할 수 있는 에이전트 지침으로 보존하는 것이 적절하다.

### 고려된 대안

- `pr_only`: 이 문제는 특정 PR에만 국한된 일회성 문제일 수 있다.
- `path_scoped_rule`: 만약 이 규칙이 특정 경로에만 적용된다면 경로 범위 규칙이 적합할 수 있다.

### 실행 명령어

```bash
promote candidate_016 --target agents
promote ignore candidate_016 --reason "..."
promote snooze candidate_016 --days 30
```

---

## 11. ServerSentEventsOptions 타입 파라미터 누락 문제를 수정하고, SSE 페이로드 타입 전파를 올바르게 적용하는 규칙

- **ID**: `candidate_017`
- **Target**: `path_scoped_rule` → `packages/openapi/test/routers/README.md`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/openapi/test/routers/**`

### 근거

- PR #7231 `packages/openapi/test/routers/errorFormatterRouter-heyapi/core/serverSentEvents.gen.ts` — "⚠️ Potential issue | 🟠 Major

Fix the SSE payload type propagation and apply th..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128590)
- PR #7231 `packages/openapi/test/routers/appRouter-heyapi/core/serverSentEvents.gen.ts` — "⚠️ Potential issue | 🟠 Major

Fix missing generic parameter and remove type ass..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128575)

### 제안된 패치

```markdown
---
applyTo: "packages/openapi/test/routers/**"
---
# SSE 타입 안전성 규칙
- `ServerSentEventsOptions` 타입 파라미터를 반드시 명시하여 타입 안전성을 확보할 것.
- `createSseClient` 호출 시 적절한 제네릭 타입을 전달하여 타입 추론이 올바르게 이루어지도록 할 것.
- 타입 단언(`as`) 사용을 지양하고, 타입 미스매치를 컴파일 타임에 잡을 수 있도록 타입 정의를 명확히 할 것.
- 반복적으로 발생하는 타입 누락 문제를 방지하기 위해, 관련 함수와 변수 선언에 타입 명시를 강화할 것.
- 관련 유틸리티 또는 패턴을 활용하여 SSE 페이로드 타입 전파를 일관되게 적용할 것.
```

### 이 대상을 선택한 이유

이 패턴은 packages/openapi/test/routers 경로 내 SSE 관련 코드에서 반복적으로 발생하는 타입 파라미터 누락 및 부적절한 타입 단언 문제로, 특정 경로에 국한된 타입 안전성 규칙으로 판단됨. 전체 에이전트 지침보다는 경로 스코프 규칙으로 관리하는 것이 적절함.

### 고려된 대안

- `agents`: 만약 이 타입 파라미터 규칙이 전 레포에 걸쳐 SSE 관련 모든 코드에 적용된다면 에이전트 지침으로 적합할 수 있음
- `pr_only`: 특정 PR에만 국한된 문제라면 PR 전용 메모리에 보관하는 것이 적절함

### 실행 명령어

```bash
promote candidate_017 --target path_scoped_rule
promote ignore candidate_017 --reason "..."
promote snooze candidate_017 --days 30
```

---

## 12. `hasExpectedArtifacts()` 함수가 출력 디렉토리 존재 여부만 확인하여 내부 파일이 삭제되거나 손상된 경우 캐시 히트가 잘못 발생할 수 있음.

- **ID**: `candidate_019`
- **Target**: `path_scoped_rule` → `packages/openapi/test/scripts/codegen.ts`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/openapi/test/scripts/**`

### 근거

- PR #7296 `packages/openapi/test/scripts/codegen.ts` — "`hasExpectedArtifacts()` only checks that `docPath` and `outputDir` exist. If th..." [link](https://github.com/trpc/trpc/pull/7296#discussion_r3004785612)
- PR #7296 `packages/openapi/test/scripts/codegen.ts` — "`hasExpectedArtifacts()` only checks that `docPath` and `outputDir` exist. If `o..." [link](https://github.com/trpc/trpc/pull/7296#discussion_r3004736557)

### 제안된 패치

```markdown
---
applyTo: "packages/openapi/test/scripts/**"
---
# 경로 범위 규칙: hasExpectedArtifacts() 함수 검증 강화
- `hasExpectedArtifacts()`는 출력 디렉토리 존재 여부만 확인하므로, 내부 파일의 무결성 검증을 추가하세요.
- 출력 디렉토리 내 파일이 삭제되거나 손상된 경우를 감지할 수 있도록 파일 존재 및 무결성 체크를 구현하세요.
- 구체적인 파일 검증 방법으로는 파일 크기 또는 해시값 비교를 활용하세요.
- 검증 로직을 테스트하여, 손상된 파일이 있을 때 캐시 히트가 방지되도록 하세요.
- 이 규칙은 `packages/openapi/test/scripts/` 경로 내에서만 적용됩니다.
```

### 이 대상을 선택한 이유

이 패턴은 packages/openapi/test/scripts/codegen.ts 경로 내에서만 반복적으로 나타나며, 출력 디렉토리와 생성된 파일의 무결성 검증에 관한 구체적인 코드 생성 관련 규칙이므로 경로 범위 규칙(path_scoped_rule)로 분류하는 것이 적절함.

### 고려된 대안

- `pr_only`: 특정 PR에만 국한된 문제로, 일반화된 규칙으로 만들기 어려움
- `agents`: 전사적 코드 생성 규칙이 아니며 특정 경로에 한정됨
- `test`: 런타임 불변 조건이라기보다는 코드 생성 무결성 검증 권고사항임

### 실행 명령어

```bash
promote candidate_019 --target path_scoped_rule
promote ignore candidate_019 --reason "..."
promote snooze candidate_019 --days 30
```

---

## 13. `subscribe()` 호출 시 `onError` 핸들러 내에서 `subscription.unsubscribe()`를 호출할 때, `subscription`이 `const`로 선언되어 동기 호출 시 temporal dead zone 문제가 발생할 수 있으므로 `let`으로 선언하는 것을 권장한다.

- **ID**: `candidate_020`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments

### 근거

- PR #7279 `packages/tests/server/websockets.test.ts` — "`waitForWsSubscriptionError` unsubscribes via `subscription.unsubscribe()` insid..." [link](https://github.com/trpc/trpc/pull/7279#discussion_r2971303187)
- PR #7279 `packages/tests/server/httpSubscriptionLink.test.ts` — "`waitForSubscriptionError` references `subscription` inside the `onError` handle..." [link](https://github.com/trpc/trpc/pull/7279#discussion_r2971303176)

### 제안된 패치

```markdown
## AGENTS.md 규칙

- `subscribe()` 호출 시 `onError` 핸들러 내에서 `subscription.unsubscribe()`를 호출할 때, `subscription` 변수를 `const` 대신 `let`으로 선언하도록 한다.
- `subscription` 변수는 여러 위치에서 재할당 가능하도록 `let`으로 선언하며, `subscribe()` 호출과 동시에 선언한다.
- 이 규칙은 여러 테스트 파일과 코드 전반에 일관성을 유지하기 위해 적용하며, 특정 경로나 PR에 국한하지 않는다.
- `subscription` 변수 선언과 `unsubscribe()` 호출이 같은 코드 블록 내에 위치하도록 하며, 명확한 구독 취소 시점을 확보한다.
- 이 규칙을 위반하는 코드는 수정하여 `subscription`을 `let`으로 선언하고, `unsubscribe()` 호출 시점에 재할당 가능하게 한다.
```

### 이 대상을 선택한 이유

이 패턴은 여러 테스트 파일에서 반복적으로 나타나며, 구독 취소 시점과 변수 선언 방식에 관한 일관된 코딩 관례로, 향후 코드 작성 시 참고할 수 있는 전사적 규칙으로 보인다. 특정 경로나 PR에 국한되지 않고, 여러 위치에서 동일한 문제를 예방하기 위한 지침이므로 agents에 저장하는 것이 적합하다.

### 고려된 대안

- `pr_only`: 특정 PR에만 국한된 문제일 경우
- `path_scoped_rule`: 특정 경로에만 적용되는 규칙일 경우
- `none`: 이미 문서화되어 있거나 중요하지 않은 경우

### 실행 명령어

```bash
promote candidate_020 --target agents
promote ignore candidate_020 --reason "..."
promote snooze candidate_020 --days 30
```

---

## 14. packages/tanstack-react-query 테스트 파일에서는 React를 네임스페이스 import 방식으로 사용해야 한다는 규칙이 반복적으로 지적됨

- **ID**: `candidate_023`
- **Target**: `path_scoped_rule` → `packages/tanstack-react-query/README.md`
- **Confidence**: 0.9500000000000001
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 3 comments
- **Path scope**: `packages/tanstack-react-query/test/**`

### 근거

- PR #7362 `packages/tanstack-react-query/test/client.test.tsx` — "⚠️ Potential issue | 🟠 Major

Use namespace React import in TanStack React Quer..." [link](https://github.com/trpc/trpc/pull/7362#discussion_r3144118971)
- PR #7362 `packages/tanstack-react-query/test/polymorphism.test.tsx` — "⚠️ Potential issue | 🟠 Major

Keep `import  as React` in TanStack React Query t..." [link](https://github.com/trpc/trpc/pull/7362#discussion_r3144118976)
- PR #7362 `packages/tanstack-react-query/test/infiniteQueryOptions.test.tsx` — "⚠️ Potential issue | 🟡 Minor

Restore namespace React import in TanStack test f..." [link](https://github.com/trpc/trpc/pull/7362#discussion_r3144118975)

### 사람 반응

- Agreed: **2** reviewer(s) (@Zelys-DFKH)
  - First agreement: *"Thanks for the review. There's no documented convention in `.coderabbit.yaml` or `CONTRIBUTING.md` requiring namespace i"*

### 제안된 패치

```markdown
---
applyTo: "packages/tanstack-react-query/test/**"
---
# React 네임스페이스 임포트 규칙
- `packages/tanstack-react-query/test/` 내 테스트 파일에서는 React를 네임스페이스 임포트(`import * as React from 'react'`) 방식으로 사용해야 한다.
- 모든 테스트 파일에서 React 관련 임포트는 일관되게 네임스페이스 방식으로 유지한다.
- 기존의 개별 임포트(`import { useState } from 'react'`)는 네임스페이스 임포트로 변경한다.
- 임포트 구문과 사용 예시는 프로젝트 내 테스트 컨벤션에 따라 일관성 있게 작성한다.
- 이 규칙은 해당 경로 내 모든 테스트 파일에 적용하며, 문서화된 규칙이 없으므로 신규 규칙으로 관리한다.
```

### 이 대상을 선택한 이유

이 패턴은 특정 경로(packages/tanstack-react-query/test/)에 한정된 import 스타일 규칙으로, 전체 저장소에 적용되는 일반 규칙(agents)보다는 경로 범위 규칙(path_scoped_rule)로 관리하는 것이 적합함. 또한, 기존에 문서화된 규칙이 없으므로 신규 규칙으로 저장할 가치가 있음.

### 고려된 대안

- `agents`: 만약 이 규칙이 모든 테스트 파일에 적용되는 일반적인 규칙이라면 agents가 적합하나, 현재는 특정 패키지 테스트 파일에 한정됨.
- `pr_only`: 일회성 PR 관련 지적이라면 pr_only가 적합하나, 여러 파일에서 반복적으로 나타남.

### 실행 명령어

```bash
promote candidate_023 --target path_scoped_rule
promote ignore candidate_023 --reason "..."
promote snooze candidate_023 --days 30
```

---

## 15. 마크다운 코드 블록에 언어 식별자를 추가하여 markdownlint MD040 경고를 방지하고 편집기 도구 지원을 향상시킵니다.

- **ID**: `candidate_025`
- **Target**: `path_scoped_rule` → `packages/next/skills/MD040-code-block-language-rule.md`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/next/skills/**`

### 근거

- PR #7252 `packages/next/skills/nextjs-app-router/SKILL.md` — "⚠️ Potential issue | 🟡 Minor

Add a language to the fenced code block (MD040).
..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937303453)
- PR #7252 `packages/next/skills/nextjs-pages-router/SKILL.md` — "⚠️ Potential issue | 🟡 Minor

Add a language hint to the fenced tree block.

Th..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937237720)

### 제안된 패치

```markdown
---
applyTo: "packages/next/skills/**"
---
# 마크다운 코드 블록 언어 식별자 규칙
- `packages/next/skills/` 내 모든 마크다운 파일에서 코드 블록에 언어 식별자를 반드시 추가하세요.
- 코드 블록 시작 시 백틱(```) 뒤에 언어 이름을 명시하여 markdownlint MD040 경고를 방지하세요.
- 예시: ```javascript, ```python, ```bash 등 구체적인 언어를 지정하세요.
- 편집기 지원 향상을 위해 일관된 스타일로 언어 식별자를 사용하는 것을 권장합니다.
- 코드 블록이 언어 식별자 없이 작성된 경우 수정하여 명확한 언어를 지정하세요.
```

### 이 대상을 선택한 이유

두 개의 문서 모두 packages/next/skills/ 경로 내에서 마크다운 코드 블록에 언어 식별자를 추가하는 동일한 규칙을 반복적으로 지적하고 있어, 특정 경로에 적용되는 규칙으로 분류하는 것이 적절합니다. 이는 코드 품질과 편집기 지원을 위한 일관된 스타일 가이드로서 향후에도 유용하기 때문입니다.

### 고려된 대안

- `pr_only`: 이 규칙은 특정 PR에만 국한된 문제로 보이지 않고 여러 문서에 반복적으로 나타납니다.
- `agents`: 이 규칙은 전체 저장소에 걸친 일반적인 마크다운 스타일 규칙이지만, 현재는 특정 경로 내 문서에만 반복되어 적용됩니다.

### 실행 명령어

```bash
promote candidate_025 --target path_scoped_rule
promote ignore candidate_025 --reason "..."
promote snooze candidate_025 --days 30
```

---

## 16. `SKILL.md` 파일명은 md/mdx camelCase 명명 규칙을 위반하지만, 이는 의도된 예외로 명확히 문서화되어야 한다.

- **ID**: `candidate_027`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.95
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 3 comments

### 근거

- PR #7252 `packages/tanstack-react-query/skills/react-query-setup/SKILL.md` — "⚠️ Potential issue | 🟡 Minor

`SKILL.md` filename breaks the md/mdx camelCase n..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937237747)
- PR #7252 `packages/server/skills/adapter-fetch/SKILL.md` — "⚠️ Potential issue | 🟡 Minor

Add `SKILL.md` as an explicit exception in the ca..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937237734)
- PR #7252 `packages/server/skills/trpc-router/SKILL.md` — "⚠️ Potential issue | 🟡 Minor

Rename `SKILL.md` to a camelCase filename to matc..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937237735)

### 제안된 패치

```markdown
## 파일명 명명 규칙 예외 처리

- `SKILL.md` 파일명은 md/mdx camelCase 명명 규칙을 위반하더라도, 의도된 예외로 명확히 문서화한다.
- 이 예외는 여러 경로에 걸쳐 반복되며, 전사적 코딩 가이드라인에 따라 `AGENTS.md`와 같은 AI 에이전트 지침 문서에 포함한다.
- `SKILL.md` 파일명을 camelCase 규칙에 맞게 변경하지 않으며, 예외로 처리한다.
- `SKILL.md`를 규칙 예외로 명시할 경우, 관련 가이드라인에 반드시 추가한다.
- 이 규칙은 프로젝트 전반에 걸쳐 일관되게 적용하며, 예외 처리에 대한 명확한 문서화를 유지한다.
```

### 이 대상을 선택한 이유

이 패턴은 여러 경로에 걸쳐 반복되며, 파일명 명명 규칙과 예외 처리에 관한 전사적 코딩 가이드라인에 해당하므로 AGENTS.md와 같은 AI 에이전트 지침 문서에 저장하는 것이 적합하다.

### 고려된 대안

- `path_scoped_rule`: 파일명 규칙이 특정 경로에만 적용된다면 경로 범위 규칙으로 저장 가능하다.
- `pr_only`: 특정 PR에만 국한된 경우 PR 전용으로 저장 가능하다.

### 실행 명령어

```bash
promote candidate_027 --target agents
promote ignore candidate_027 --reason "..."
promote snooze candidate_027 --days 30
```

---

## 17. `querySerializer`가 `undefined` 값을 `JSON.stringify`로 직렬화할 때, `URLSearchParams.append()`가 이를 문자열 "undefined"로 변환하는 문제와 `responseTransformer`의 특정 필드 검사 문제는 packages/openapi/src/heyapi 경로에 국한된 규칙이다.

- **ID**: `candidate_028`
- **Target**: `path_scoped_rule` → `packages/openapi/README.md`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/openapi/src/heyapi/**`

### 근거

- PR #7231 `packages/openapi/src/heyapi/index.ts` — "`querySerializer` uses `JSON.stringify(value)` for every key; when `value` is `u..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2935826096)
- PR #7231 `packages/openapi/src/heyapi/index.ts` — "`querySerializer` uses `JSON.stringify(value)` for every key; when `value` is `u..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2935826094)

### 제안된 패치

```markdown
---
applyTo: "packages/openapi/src/heyapi/**"
---
# 패턴별 규칙: querySerializer 및 responseTransformer 문제 해결
- `querySerializer`가 `undefined` 값을 `JSON.stringify`로 직렬화할 때, 반환값이 `undefined`인 경우를 처리하여 `URLSearchParams.append()`가 "undefined" 문자열로 변환하는 문제를 방지한다.
- `querySerializer`에서 `JSON.stringify` 호출 전에 값이 `undefined`인지 체크하고, 필요시 빈 문자열 또는 적절한 기본값으로 대체한다.
- `responseTransformer`에서 특정 필드가 존재하는지 여부를 검사할 때, `null`과 `undefined`를 명확히 구분하여 조건문을 작성한다.
- 해당 규칙은 `packages/openapi/src/heyapi/index.ts` 파일 내에서만 적용하며, 다른 경로에는 영향을 미치지 않는다.
- 변경 사항을 적용한 후, 관련 테스트 케이스를 실행하여 문제 해결 여부를 검증한다.
```

### 이 대상을 선택한 이유

이 패턴은 `querySerializer`와 `responseTransformer`의 동작 방식에 관한 구체적이고 반복적인 문제로, 특정 경로인 packages/openapi/src/heyapi/index.ts에만 적용된다. 따라서 전역 에이전트 지침보다는 경로 범위 규칙으로 관리하는 것이 적절하다.

### 고려된 대안

- `agents`: 패턴이 여러 경로에 걸쳐 적용되는 전역 규칙일 경우
- `pr_only`: 특정 PR에만 국한된 문제일 경우
- `none`: 이미 문서화되어 있거나 중요하지 않은 경우

### 실행 명령어

```bash
promote candidate_028 --target path_scoped_rule
promote ignore candidate_028 --reason "..."
promote snooze candidate_028 --days 30
```

---

## 18. 이 에러 핸들러에서 `resolveProcedureError` 호출 시 프로시저의 `declaredErrors` 등록 리스트가 누락되어, 등록된 선언된 에러가 잘못 처리되는 문제가 반복적으로 지적됨.

- **ID**: `candidate_031`
- **Target**: `path_scoped_rule` → `packages/server/src/adapters/ws.ts`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/server/src/adapters/ws.ts`

### 근거

- PR #7279 `packages/server/src/adapters/ws.ts` — "In this error handler, `resolveProcedureError(..., { path })` is called without ..." [link](https://github.com/trpc/trpc/pull/7279#discussion_r2971303185)
- PR #7279 `packages/server/src/adapters/ws.ts` — "`resolveProcedureError` is called without passing the procedure’s `declaredError..." [link](https://github.com/trpc/trpc/pull/7279#discussion_r2970571720)

### 제안된 패치

```markdown
---
applyTo: "packages/server/src/adapters/ws.ts"
---
# ws.ts 파일 내 에러 핸들러 규칙
- `resolveProcedureError` 호출 시, 해당 프로시저의 `declaredErrors` 등록 리스트를 반드시 포함시킬 것.
- `resolveRegisteredDeclaredErrorOrDowngrade` 함수 호출 전, 프로시저의 `declaredErrors`가 정의되어 있는지 검증할 것.
- 에러 처리 로직에서 `declaredErrors` 누락 시, 명확한 경고 또는 예외를 발생시켜 누락 방지.
- 반복적으로 발생하는 문제이므로, 관련 유틸리티 또는 미들웨어에서 일관된 에러 등록 패턴을 적용할 것.
- 코드 리뷰 시, `resolveProcedureError` 호출 부분에 `declaredErrors` 전달 여부를 반드시 확인할 것.
```

### 이 대상을 선택한 이유

이 패턴은 특정 파일 경로(`packages/server/src/adapters/ws.ts`) 내에서 반복적으로 발생하는 구체적인 구현 문제로, 해당 경로에 한정된 규칙으로 관리하는 것이 적절함. 이는 단일 PR에 국한되지 않고, 전체 해당 경로 코드의 일관성 유지에 도움이 됨.

### 고려된 대안

- `pr_only`: 문제가 특정 PR에만 국한되어 재사용 가능성이 낮음
- `agents`: 전사적 규칙이라기보다는 특정 경로에 한정된 문제임
- `none`: 중복된 문제지만 저장할 가치가 낮음

### 실행 명령어

```bash
promote candidate_031 --target path_scoped_rule
promote ignore candidate_031 --reason "..."
promote snooze candidate_031 --days 30
```

---

## 19. metadata-only 프레임에 대해 onSseEvent 호출을 건너뛰어야 하며, retry: 전용 청크가 data: undefined로 호출되는 문제를 방지해야 한다.

- **ID**: `candidate_033`
- **Target**: `path_scoped_rule` → `packages/openapi/test/routers/**/core/serverSentEvents.gen.ts`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/openapi/test/routers/**/core/serverSentEvents.gen.ts`

### 근거

- PR #7231 `packages/openapi/test/routers/appRouter-heyapi/core/serverSentEvents.gen.ts` — "⚠️ Potential issue | 🟠 Major

Skip `onSseEvent` for metadata-only frames.

Hear..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128579)
- PR #7231 `packages/openapi/test/routers/errorFormatterRouter-heyapi/core/serverSentEvents.gen.ts` — "⚠️ Potential issue | 🟠 Major

Skip `onSseEvent` for metadata-only frames.

Hear..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128594)

### 제안된 패치

```markdown
---
applyTo: "packages/openapi/test/routers/**/core/serverSentEvents.gen.ts"
---
# serverSentEvents.gen.ts에서의 onSseEvent 호출 규칙
- 메타데이터 전용 프레임에 대해서는 `onSseEvent` 호출을 건너뛰도록 합니다.
- `retry:`-전용 청크의 경우 `data: undefined`로 호출되는 것을 방지합니다.
- Heartbeat 또는 `retry:`-전용 청크에서도 `data`가 `undefined`인 `onSseEvent` 호출을 피합니다.
- 이 규칙은 해당 파일들 내에서만 적용하며, 다른 경로나 파일에는 영향을 주지 않습니다.
- 관련 유틸리티 또는 패턴을 참고하여 조건문을 추가하거나 수정하세요.
```

### 이 대상을 선택한 이유

이 패턴은 serverSentEvents.gen.ts 파일들에서 반복적으로 나타나는 특정 경로에 국한된 규칙으로, metadata-only 프레임에 대한 onSseEvent 호출을 건너뛰는 것이 중요하다. 이는 여러 파일에서 동일한 문제로 지적되었으며, repo-wide 규칙보다는 특정 경로에 적용되는 규칙으로 관리하는 것이 적절하다.

### 고려된 대안

- `agents`: repo-wide AI 지침으로 관리할 수도 있으나, 이 문제는 특정 경로와 파일에 국한되어 있어 path_scoped_rule이 더 적합하다.
- `pr_only`: 특정 PR에만 해당하는 문제일 수 있으나, 여러 파일에서 반복되어 나타나므로 pr_only는 부적절하다.

### 실행 명령어

```bash
promote candidate_033 --target path_scoped_rule
promote ignore candidate_033 --reason "..."
promote snooze candidate_033 --days 30
```

---

## 20. 서버 전송 이벤트 재시도 로직에서 초기 연결 시도에 재시도 예산을 사용하지 않고, AbortError 발생 시 재시도하지 않아야 한다는 규칙

- **ID**: `candidate_034`
- **Target**: `path_scoped_rule` → `packages/openapi/test/routers/**/core/serverSentEvents.gen.ts`
- **Confidence**: 0.9
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/openapi/test/routers/**/core/serverSentEvents.gen.ts`

### 근거

- PR #7231 `packages/openapi/test/routers/errorFormatterRouter-heyapi/core/serverSentEvents.gen.ts` — "⚠️ Potential issue | 🟠 Major

Don't spend the retry budget on the initial conne..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128592)
- PR #7231 `packages/openapi/test/routers/appRouter-heyapi/core/serverSentEvents.gen.ts` — "⚠️ Potential issue | 🟠 Major

Don't spend the retry budget on the initial conne..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128577)

### 제안된 패치

```markdown
---
applyTo: "packages/openapi/test/routers/**/core/serverSentEvents.gen.ts"
---
# 서버 전송 이벤트 재시도 로직 규칙
- 초기 연결 시도 시 재시도 예산을 사용하지 않도록 구현한다.
- AbortError 발생 시 재시도를 수행하지 않도록 한다.
- 재시도 횟수(`sseMaxRetryAttempts`)는 최초 연결 시도 후에만 적용한다.
- `attempt` 값이 최초 요청 전에 증가하는 점을 고려하여 재시도 조건을 설정한다.
- 관련 유틸리티 또는 패턴을 활용하여 일관된 재시도 로직을 유지한다.
```

### 이 대상을 선택한 이유

이 패턴은 serverSentEvents 관련 특정 경로 내에서 재시도 로직에 대한 일관된 규칙을 다루고 있어, 전체 레포가 아닌 해당 경로에 국한된 규칙으로 관리하는 것이 적절하다.

### 고려된 대안

- `agents`: 재시도 로직이 여러 기능에 걸쳐 적용된다면 전체 에이전트 지침에 포함할 수 있으나, 현재는 특정 경로에 한정됨.
- `pr_only`: 특정 PR에만 해당하는 일회성 문제일 경우.

### 실행 명령어

```bash
promote candidate_034 --target path_scoped_rule
promote ignore candidate_034 --reason "..."
promote snooze candidate_034 --days 30
```

---

## 21. 릴리스 워크플로우에서 'pnpm add -g npm@latest' 사용은 비재현적이며, npm 버전을 고정하는 것이 안정적임.

- **ID**: `candidate_038`
- **Target**: `path_scoped_rule` → `docs/release-workflow-guidelines.md`
- **Confidence**: 0.95
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `.github/workflows/**`

### 근거

- PR #7247 `.github/workflows/release-tmp.yml` — "`pnpm add -g npm@latest` makes releases non-reproducible and can break unexpecte..." [link](https://github.com/trpc/trpc/pull/7247#discussion_r2936535936)
- PR #7247 `.github/workflows/release-canary.yml` — "`pnpm add -g npm@latest` makes releases non-reproducible and can break unexpecte..." [link](https://github.com/trpc/trpc/pull/7247#discussion_r2936535931)

### 제안된 패치

```markdown
---
applyTo: ".github/workflows/**"
---
# 릴리스 워크플로우에서 npm 버전 관리 규칙
- `pnpm add -g npm@latest` 대신, 안정적인 특정 npm 버전(예: 8.19.3)을 명시적으로 사용하거나, 프로젝트에 번들된 npm 버전을 활용하세요.
- npm 버전 고정을 위해 `.npmrc` 또는 `package.json`의 `engines` 필드를 활용하여 일관성을 유지하세요.
- 릴리스 관련 GitHub 워크플로우에서는 npm 버전 변경이 재현 가능성을 해치지 않도록 주의하세요.
- npm 버전 업데이트 시, 사전에 충분한 테스트를 수행하여 예기치 않은 문제를 방지하세요.
- 버전 고정된 npm을 사용하는 경우, 정기적으로 버전 업데이트 후 호환성 검증을 수행하세요.
```

### 이 대상을 선택한 이유

이 패턴은 릴리스 관련 GitHub 워크플로우 파일에 국한된 규칙으로, 특정 경로(.github/workflows/)에 적용되는 명확한 규칙이므로 path_scoped_rule로 분류함.

### 고려된 대안

- `agents`: 만약 이 규칙이 모든 코드베이스에 걸쳐 npm 버전 관리 정책으로 확장된다면 agents가 적합할 수 있음
- `pr_only`: 특정 PR에만 국한된 경우 pr_only가 적합함

### 실행 명령어

```bash
promote candidate_038 --target path_scoped_rule
promote ignore candidate_038 --reason "..."
promote snooze candidate_038 --days 30
```

---

## 필터링된 항목

승격 후보에서 제외된 항목들. 임계치 튜닝이나 팀 리뷰의 엣지 케이스 확인에 유용합니다.

### 승격 불가 (target=none / invalid) (18)

- 이 코멘트는 AI가 대화 중임을 인지하고 대기 상태로 전환한다는 안내 메시지입니다. — target=`none`, confidence=0.95
  - 이 코멘트는 특정 PR이나 코드 변경과 관련된 기술적 지식이 아니라, AI의 대화 상태를 알리는 안내 메시지로서 저장할 가치가 낮습니다. 따라서 저장 대상이 아닙니다.
- 디렉터리 읽기 오류 처리 시 모든 오류를 무시하지 말고, 존재하지 않는 디렉터리(ENOENT) 오류만 무시해야 한다. — target=`pr_only`, confidence=0.95
  - 이 패턴은 특정 파일(www/scripts/check-twoslash.ts) 내에서 디렉터리 읽기 오류 처리 방식에 관한 구체적인 문제로, 범용적인 규칙이라기보다는 해당 PR에서만 적용되는 개선 사항이다. 따라서 전체 에이전트 지침이나 경로 범위 규칙으로 확장하기보다는 PR 단위로 보존하는 것이 적절하다.
- 이 워크플로우의 경로 필터가 실제 문서 디렉토리 위치와 일치하지 않아 문서 변경 시 워크플로우가 트리거되지 않음. — target=`pr_only`, confidence=0.90
  - 이 문제는 특정 워크플로우 파일 내 경로 설정 오류에 관한 것으로, 리포지토리 전반에 적용되는 규칙이나 아키텍처 결정이 아니며, 특정 PR에서만 수정이 필요한 구체적 문제임.
- 오타 수정: 'occured'를 'occurred'로 변경하여 사용자에게 노출되는 메시지의 철자 오류를 방지함. — target=`pr_only`, confidence=0.95
  - 이 패턴은 특정 PR 내에서 발생한 오타 수정에 관한 것으로, 일반적인 코딩 규칙이나 아키텍처 결정과는 무관하며, 다른 PR이나 경로에 걸쳐 반복되는 규칙이 아니므로 저장할 필요가 없음.
- `@hey-api/openapi-ts` 패키지의 peerDependencies 최소 버전을 실제 코드에서 사용하는 Plugins 타입을 지원하는 가장 이른 버전으로 조정해야 한다는 반복된 리뷰 코멘트 — target=`pr_only`, confidence=0.95
  - 이 패턴은 특정 PR에서 발견된 구체적인 버전 호환성 문제에 관한 것으로, 범용적인 규칙이나 아키텍처 결정이 아니며, 특정 코드와 패키지 버전에 한정된 내용이므로 저장소 전체에 적용되는 지침으로 승격하기에는 적합하지 않다.
- pnpm-lock.yaml 파일에서 일부 워크스페이스가 여전히 vitest 3.x 버전을 참조하고 있어, 모노레포 전체를 vitest 4로 업그레이드하려면 해당 워크스페이스의 package.json도 함께 업데이트해야 한다. — target=`pr_only`, confidence=0.90
  - 이 패턴은 특정 PR에서 모노레포 내 vitest 버전 일관성을 맞추기 위한 구체적이고 일회성 작업에 관한 것으로, 범용적인 규칙이나 아키텍처 결정이 아니며, 다른 경로나 전반적인 에이전트 지침에 적용되지 않으므로 pr_only가 적합하다.
- 이 패턴은 여러 패키지에서 동일한 'intent' bin 엔트리를 선언했으나 실제 실행 파일이 누락되어 설치 시 충돌 및 오류가 발생할 수 있다는 문제를 지적합니다. — target=`pr_only`, confidence=0.90
  - 이 문제는 특정 PR 내 여러 패키지에서 발생한 bin 실행 파일 누락과 이름 충돌에 관한 것으로, 일반적인 코딩 규칙이나 아키텍처 결정이 아니라 PR 단위의 구체적 문제입니다. 따라서 저장소 전반에 적용할 규칙이나 에이전트 지침으로 승격하기보다는 PR 관련 메모로 남기는 것이 적절합니다.
- 테스트에서 오타가 있는 메시지 'occured'를 사용하고 있으며, 런타임 메시지가 'occurred'로 수정되면 테스트도 함께 업데이트해야 한다. — target=`pr_only`, confidence=0.90
  - 이 패턴은 특정 PR 내 여러 테스트 파일에서 동일한 오타 문제를 지적하는 것으로, 테스트 코드 내 하드코딩된 메시지와 런타임 메시지의 일치 여부에 관한 구체적인 수정 지침이다. 이는 특정 PR에 국한된 문제이며, 전사적 규칙이나 아키텍처 결정, 테스트 불변 조건과는 거리가 있어 pr_only가 적합하다.
- 파일 헤더 주석에 명시된 경로와 실제 파일 경로가 일치하지 않아 혼란을 초래할 수 있으므로 주석이나 파일 위치를 일치시켜야 한다. — target=`pr_only`, confidence=0.90
  - 이 패턴은 특정 파일에 대한 주석과 실제 경로 불일치 문제로, 특정 PR 내에서만 의미가 있으며 범용적인 규칙이나 아키텍처 결정과는 거리가 있다. 따라서 저장소 전체에 적용할 필요가 없고, 향후 참고를 위해 PR 단위로 보존하는 것이 적절하다.
- 여러 패키지의 package.json에서 bin.intent가 존재하지 않는 ./bin/intent.js 파일을 참조하여 실행 파일이 깨지는 문제가 반복적으로 지적됨. — target=`pr_only`, confidence=0.95
  - 이 문제는 특정 PR에서 여러 패키지에 걸쳐 동일한 잘못된 bin.entry 설정이 발견된 사례로, 구체적인 파일 경로와 패키지에 한정된 문제임. 이는 전사적 규칙이나 아키텍처 결정과 관련된 내용이 아니며, 테스트 가능한 런타임 불변식도 아님. 따라서 향후 참고용으로는 PR 단위의 기록에 적합하며, 전사적 문서나 경로 범위 규칙으로 승격할 필요는 없음.
- PR 설명과 실제 exports 키의 경로 명명 규칙 불일치로 인해 소비자가 문서화된 경로로 임포트할 수 없는 문제 반복 지적 — target=`pr_only`, confidence=0.90
  - 이 패턴은 특정 PR에서만 발생하는 경로 명명 불일치 문제로, 범용적인 규칙이나 아키텍처 결정과 관련이 없으며, 다른 PR이나 경로에 일반화하기 어렵기 때문에 pr_only로 분류함
- 이 헬퍼 함수는 subscribe 호출 중 onError가 동기적으로 실행될 때 ReferenceError를 발생시킬 수 있으므로, let으로 변수를 미리 선언하고 할당 후 구독 해제해야 한다. — target=`pr_only`, confidence=0.90
  - 이 패턴은 특정 테스트 파일 내 subscribe 함수 사용 시 발생하는 일회성 코드 문제로, 범용적인 규칙이나 아키텍처 결정이 아니며, 다른 경로에도 일반화하기 어렵다. 따라서 PR 단위로 관리하는 것이 적절하다.
- `getRouterInputsHash()` 함수가 각 라우터마다 전체 파일을 재탐색하고 재읽기 때문에 성능 저하가 발생한다는 반복된 지적. — target=`pr_only`, confidence=0.95
  - 이 패턴은 특정 PR(#7296) 내에서 동일한 성능 문제에 대해 반복적으로 지적된 것으로, 구체적인 코드와 상황에 한정되어 있어 범용적인 규칙이나 아키텍처 결정으로 확장하기 어렵다. 따라서 향후 참고를 위해 PR 단위로 보존하는 것이 적절하다.
- 도움말 및 주석에서 OpenAPI 버전이 3.0으로 명시되어 있으나 실제 생성기는 3.1.1 버전을 출력하므로 문서 불일치 문제를 수정해야 한다. — target=`pr_only`, confidence=0.90
  - 이 패턴은 특정 파일 내 문서 주석의 버전 불일치 문제로, 코드베이스 전반에 걸친 규칙이나 아키텍처 결정과 관련된 내용이 아니며, 단일 PR 내에서 수정된 구체적 사례에 해당한다. 따라서 저장소 전체에 적용할 일반 규칙으로 승격할 필요가 없고, 특정 PR 문맥에 한정된 정보로 보존하는 것이 적절하다.
- 이 클러스터는 스트리밍 응답의 content-type이 application/json에서 application/jsonl로 변경되어야 한다는 반복된 리뷰 코멘트를 포함합니다. — target=`pr_only`, confidence=0.90
  - 이 패턴은 특정 PR(#7345)과 관련된 구체적인 코드 및 문서 수정 사항에 국한되어 있으며, 범용적인 규칙이나 아키텍처 결정이 아니므로 저장소 전체에 적용할 필요가 없습니다.
- @ts-expect-error 주석이 호출되는 훅 이름과 일치하지 않아 정확한 API 명칭으로 수정이 필요함 — target=`pr_only`, confidence=0.95
  - 이 패턴은 특정 테스트 파일 내에서 발생하는 주석의 정확성 문제로, 범용적인 규칙이나 아키텍처 결정과는 거리가 있음. 따라서 특정 PR이나 코드 위치에 한정된 개선 사항으로 판단됨.
- 사용자에게 노출되는 오류 메시지에서 'occured' 오타를 'occurred'로 수정해야 하며, 이 메시지는 테스트 스냅샷에도 반영되어야 한다. — target=`pr_only`, confidence=0.95
  - 이 패턴은 특정 PR과 파일 내의 오타 수정에 관한 것으로, 범용적인 규칙이나 아키텍처 결정이 아니며, 다른 경로에도 적용되지 않는다. 또한 테스트 인바리언트라기보다는 PR 단위의 수정 사항이므로 pr_only가 적합하다.
- 문서 내 오타 수정에 관한 반복된 리뷰 코멘트 패턴입니다. — target=`pr_only`, confidence=0.95
  - 이 패턴은 특정 PR 내 문서 파일에서 발견된 개별적인 오타 수정에 관한 것으로, 범용적인 규칙이나 아키텍처적 결정과는 거리가 멉니다. 따라서 저장소 전체에 적용할 만한 지침보다는 해당 PR에 한정된 정보로 보존하는 것이 적절합니다.

### 분류 단계에서 실패 (6)

- failed: No object generated: response did not match schema.
  - No object generated: response did not match schema.
- failed: No object generated: response did not match schema.
  - No object generated: response did not match schema.
- failed: No object generated: response did not match schema.
  - No object generated: response did not match schema.
- failed: No object generated: response did not match schema.
  - No object generated: response did not match schema.
- failed: No object generated: response did not match schema.
  - No object generated: response did not match schema.
- failed: No object generated: response did not match schema.
  - No object generated: response did not match schema.

---
