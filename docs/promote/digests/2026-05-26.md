# Memory Promotion Digest — trpc/trpc
> Generated on 2026-05-26 · promote v0.7.1

## Configuration

- Provider: `anthropic` (LLM-direct (no embedding API, llmRefine inactive))
- Models: `claude-haiku-4-5` (classify), `claude-haiku-4-5` (draft)
- Output language: `en`
- Thresholds: similarity `0.8`, min confidence `0.75`, min occurrences `2`
- Window: `90` days (overridden via --since; config default is 60)
- Privacy: redactSecrets=`true`, sendDiffHunksToLLM=`false`

## Summary

- Scanned: **430** review comments across **65** PRs
- AI reviewer comments: **368**
- Clusters found: **149**
- Repeated clusters: **77**
- Promotion candidates: **11**
- Estimated cost: $0.4231
- Timings: fetch 7.4s · normalize 25ms · cluster 5m 13s · conv-fetch 8.2s · reply-ctx 12.5s · memory 1.1s · classify+draft 2m 3s · **total 7m 46s**

---

## 1. Test files must import all Vitest functions (beforeAll, describe, expect, it) that are used to avoid runtime errors.

- **ID**: `candidate_057`
- **Target**: `test` → `(test file)`
- **Confidence**: 0.95
- **Scope**: 🌐 cross-PR (4 PRs)
- **Occurrences**: 6 comments

### Evidence

- PR #7283 `packages/openapi/test/cyclicTypes.test.ts` — "⚠️ Potential issue | 🔴 Critical

Missing `beforeAll` import.

The `beforeAll` f..." [link](https://github.com/trpc/trpc/pull/7283#discussion_r2977503544)
- PR #7252 `packages/client/skills/links/references/link-options.md` — "⚠️ Potential issue | 🟡 Minor

Missing `httpSubscriptionLink` import in the `spl..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937228658)
- PR #7231 `packages/openapi/test/edgeCases.test.ts` — "`beforeAll` is used but not imported from Vitest, which will cause the test file..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2935826088)
- PR #7231 `packages/openapi/test/edgeCases.test.ts` — "`beforeAll` is used but not imported from Vitest, which will cause the test file..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2935826082)
- PR #7283 `packages/openapi/test/cyclicTypes.test.ts` — "`beforeAll` is used but not imported from vitest, so this test file will throw a..." [link](https://github.com/trpc/trpc/pull/7283#discussion_r2977503215)
- ... and 1 more

### Suggested patch

```markdown
# Test Import Validation Invariant

## Test File Location
`tests/invariants/vitest-imports.test.ts`

## What the Test Should Assert

The test should verify that all Vitest utility functions used within a test file are explicitly imported from the `vitest` package. Specifically:
- Scan test files for usage of Vitest functions (`describe`, `it`, `expect`, `beforeAll`, `beforeEach`, `afterAll`, `afterEach`, `test`, `vi`)
- Verify that each used function appears in the import statement from `vitest`
- Fail if any Vitest function is used without being imported
- Report the line number and function name for each missing import

## Code Sketch

```typescript
import { describe, it, expect } from 'vitest';
import fs from 'fs';
import path from 'path';

describe('Vitest Import Validation', () => {
  it('should ensure all used Vitest functions are imported', () => {
    const testFilesPattern = '**/*.test.ts';
    const vitestFunctions = [
      'describe', 'it', 'test', 'expect', 
      'beforeAll', 'beforeEach', 'afterAll', 'afterEach', 'vi'
    ];
    
    // Scan test files
    // Extract import statements
    // Extract function usages
    // Assert: usages ⊆ imports
    
    expect(missingImports).toEqual([]);
  });
});
```
```

### Why this target

This is a testable runtime invariant: test files that use Vitest functions without importing them will fail at runtime. This is a mechanical check that should be enforced across all test files in the repository. The pattern appears consistently across multiple test files (cyclicTypes.test.ts, edgeCases.test.ts, next-app-dir.test.ts) and represents a class of errors that can be prevented through linting or test execution validation.

### Alternatives considered

- `agents`: Could be documented as a coding convention for test file setup, but the runtime failure aspect makes it more of a testable invariant than a style guide.
- `path_scoped_rule`: Could apply specifically to test files (*.test.ts), but the issue is fundamental to how Vitest works across all test files, not specific to a particular domain.

### Actions

```bash
promote candidate_057 --target test
promote ignore candidate_057 --reason "..."
promote snooze candidate_057 --days 30
```

---

## 2. Test files should use the standard `testServerAndClientResource` pattern with `await using` for server and client setup instead of manual lifecycle management or helper wrapping.

- **ID**: `candidate_059`
- **Target**: `path_scoped_rule` → `packages/tests/README.md or a new TEST_PATTERNS.md`
- **Confidence**: 0.85
- **Scope**: 🌐 cross-PR (3 PRs)
- **Occurrences**: 3 comments
- **Path scope**: `**/*.test.ts`

### Evidence

- PR #7304 `packages/tests/server/httpSubscriptionLink.fetch.test.ts` — "🛠️ Refactor suggestion | 🟠 Major

Use the standard `testServerAndClientResourc..." [link](https://github.com/trpc/trpc/pull/7304#discussion_r3012813221)
- PR #7231 `packages/openapi/test/generate.test.ts` — "🛠️ Refactor suggestion | 🟠 Major

Use `testServerAndClientResource` for these ..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902034597)
- PR #7207 `packages/tests/server/httpSubscriptionLink.test.ts` — "🛠️ Refactor suggestion | 🟠 Major

Use `testServerAndClientResource` directly w..." [link](https://github.com/trpc/trpc/pull/7207#discussion_r2879708223)

### Suggested patch

```markdown
---
applyTo: "**/*.test.ts"
---
# Test Resource Lifecycle Management

- Use `testServerAndClientResource` pattern with `await using` for all server and client setup instead of manual lifecycle management
- Replace `konn()` wrapper patterns and hand-rolled server initialization with the standard `testServerAndClientResource` utility
- Avoid mutating client singletons directly; let the resource pattern handle client instantiation and cleanup
- Apply `await using` syntax to ensure proper async resource disposal in test blocks
- Migrate from `run(...)` initialization patterns to `testServerAndClientResource` for consistency across integration tests
```

### Why this target

This is a repeated refactoring suggestion across multiple test files in the packages/ directory. The pattern is specific to test infrastructure and applies consistently to **/*.test.ts files. It describes a standardized testing convention that should be enforced across the test suite to maintain consistency and proper resource lifecycle management.

### Alternatives considered

- `agents`: Could be documented as a cross-cutting testing convention for all AI agents to follow when reviewing test code
- `pr_only`: If this is a temporary refactoring initiative specific to these PRs rather than a permanent testing standard

### Actions

```bash
promote candidate_059 --target path_scoped_rule
promote ignore candidate_059 --reason "..."
promote snooze candidate_059 --days 30
```

---

## 3. Documentation code examples must be complete and runnable, with all referenced functions/options defined or explicitly marked as omitted.

- **ID**: `candidate_072`
- **Target**: `path_scoped_rule` → `www/docs/DOCUMENTATION_STANDARDS.md`
- **Confidence**: 0.85
- **Scope**: 🌐 cross-PR (3 PRs)
- **Occurrences**: 3 comments
- **Path scope**: `www/docs/**/*.md`

### Evidence

- PR #7191 `www/docs/client/links/httpBatchLink.md` — "The `createHTTPServer` snippet is missing required options (at least `router`). ..." [link](https://github.com/trpc/trpc/pull/7191#discussion_r2974473963)
- PR #7299 `www/docs/client/openapi.md` — "The example paths reference repo-internal test fixtures (`packages/openapi/test/..." [link](https://github.com/trpc/trpc/pull/7299#discussion_r3005021467)
- PR #7252 `packages/server/skills/server-side-calls/SKILL.md` — "⚠️ Potential issue | 🟡 Minor

`byId` is referenced but not defined in the docum..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937228678)

### Suggested patch

```markdown
---
applyTo: "www/docs/**/*.md"
---
# Documentation Code Examples

- All code examples must be complete and runnable; include all required function definitions, options, and imports needed for copy-paste execution
- Define or explicitly mark as omitted any referenced functions, routes, or options (e.g., `// router.post.byId defined elsewhere` or include full definition)
- Replace repository-internal paths (e.g., `packages/openapi/test/routers/...`) with generic, universally applicable filenames or placeholder names
- Ensure consistency between example setup (e.g., router definitions) and usage (e.g., function calls) throughout the snippet
- Use code block comments to clarify required vs. optional parameters when full context cannot be shown
```

### Why this target

All three examples involve incomplete or non-runnable code snippets in documentation files across different paths. The pattern is specific to documentation (www/docs and SKILL.md files) where readers expect copy-paste-ready examples. This is a documentation quality rule that should apply consistently across all doc files.

### Alternatives considered

- `agents`: Could be a general AI instruction to always provide complete, runnable examples in documentation, but the pattern is more specific to documentation files than a cross-cutting coding convention.
- `none`: If treating each as a one-off documentation issue rather than a systematic pattern requiring enforcement.

### Actions

```bash
promote candidate_072 --target path_scoped_rule
promote ignore candidate_072 --reason "..."
promote snooze candidate_072 --days 30
```

---

## 4. Avoid non-null assertions (!) in TypeScript code; use explicit type guards or optional chaining instead.

- **ID**: `candidate_079`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.85
- **Scope**: 🌐 cross-PR (2 PRs)
- **Occurrences**: 2 comments

### Evidence

- PR #7176 `packages/client/src/createTRPCClient.ts` — "⚠️ Potential issue | 🟡 Minor

Remove the non-null assertion and unnecessary `an..." [link](https://github.com/trpc/trpc/pull/7176#discussion_r2876202463)
- PR #7195 `www/scripts/check-twoslash.ts` — "⚠️ Potential issue | 🟡 Minor

Remove non-null assertions in this file to mainta..." [link](https://github.com/trpc/trpc/pull/7195#discussion_r2869717508)

### Suggested patch

```markdown
## TypeScript Non-Null Assertions

- **Never use non-null assertions (`!`)** in TypeScript code; they bypass type safety and hide potential runtime errors
- **Use explicit type guards** instead: check with `if (value !== null && value !== undefined)` or `if (value)` before accessing properties
- **Use optional chaining (`?.`)** for safe property access on potentially null/undefined values (e.g., `obj?.property?.nested`)
- **Use nullish coalescing (`??`)** to provide default values instead of asserting non-null (e.g., `value ?? defaultValue`)
- **Apply this rule across all TypeScript files** including `src/`, `tests/`, and configuration files; no exceptions for "obvious" cases
```

### Why this target

This is a repeated cross-cutting coding convention appearing in multiple files and PRs. The pattern reflects a deliberate choice to enforce strict TypeScript safety practices repo-wide, not a one-off issue. Documenting this in AGENTS.md will help future code reviewers and AI agents consistently apply the same standard.

### Alternatives considered

- `path_scoped_rule`: Could be scoped to packages/client and www/scripts, but the principle applies broadly across the repo
- `none`: If the repo already has TypeScript strict mode enabled, this might be redundant

### Actions

```bash
promote candidate_079 --target agents
promote ignore candidate_079 --reason "..."
promote snooze candidate_079 --days 30
```

---

## 5. Avoid destructuring directly in function parameter declarations; use property access instead.

- **ID**: `candidate_094`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.85
- **Scope**: 🌐 cross-PR (2 PRs)
- **Occurrences**: 2 comments

### Evidence

- PR #7283 `packages/openapi/test/types.ts` — "🛠️ Refactor suggestion | 🟠 Major

Avoid destructuring directly in function par..." [link](https://github.com/trpc/trpc/pull/7283#discussion_r2978047825)
- PR #7199 `packages/tests/server/transformer.test.ts` — "⚠️ Potential issue | 🟡 Minor

Use direct property access instead of parameter d..." [link](https://github.com/trpc/trpc/pull/7199#discussion_r2871680407)

### Suggested patch

```markdown
## Parameter Destructuring

- Do not destructure objects directly in function parameter declarations; use property access instead
- Access object properties within the function body using dot notation (e.g., `options.property`) rather than destructuring in the function signature
- Apply this rule consistently across all function types: callbacks, handlers, utility functions, and test helpers
- This includes destructuring in arrow functions, regular functions, and callback parameters
- Refactor existing code that uses parameter destructuring to follow this pattern for consistency across the codebase
```

### Why this target

This is a repeated cross-cutting coding convention that appears in multiple packages and test files. It represents a consistent style guideline that should be enforced repo-wide to maintain code consistency. The pattern is clear and actionable, and documenting it in AGENTS.md will help both human reviewers and AI agents apply this rule consistently across the codebase.

### Alternatives considered

- `path_scoped_rule`: Could be scoped to packages/openapi/** and packages/tests/** if this convention only applies to test files, but the pattern suggests it is a repo-wide preference.
- `none`: If this is already documented in existing style guides or linting rules, it may not need additional preservation.

### Actions

```bash
promote candidate_094 --target agents
promote ignore candidate_094 --reason "..."
promote snooze candidate_094 --days 30
```

---

## 6. Avoid broad eslint-disable comments and console usage in packages/** modules; use explicit logging mechanisms instead.

- **ID**: `candidate_099`
- **Target**: `path_scoped_rule` → `eslint.config.js or a LINTING.md file scoped to packages/**`
- **Confidence**: 0.85
- **Scope**: 🌐 cross-PR (2 PRs)
- **Occurrences**: 2 comments
- **Path scope**: `packages/**/*`

### Evidence

- PR #7296 `packages/openapi/test/scripts/codegen.ts` — "The file-level `/ eslint-disable no-console /` is broad, but this file only uses..." [link](https://github.com/trpc/trpc/pull/7296#discussion_r3004736560)
- PR #7242 `packages/openapi/src/generate.ts` — "`packages//` is linted with `no-console: 'error'` (see eslint.config.js). Creati..." [link](https://github.com/trpc/trpc/pull/7242#discussion_r2936017762)

### Suggested patch

```markdown
---
applyTo: "packages/**"
---
# Logging and Console Usage

- Do not use `eslint-disable` comments to suppress `no-console` rule; this rule is enforced across all package modules
- Replace any `console.*` calls with an explicit logging utility (e.g., `logger.log()`, `debug()`) or remove them entirely
- Avoid creating aliases like `const log = console.log` to circumvent the no-console rule
- If logging is necessary for debugging, use a dedicated logging library configured for the package (e.g., `debug` module)
- For line-specific suppressions that cannot be avoided, use `// eslint-disable-next-line no-console` on the exact line rather than file-level disables
```

### Why this target

Both examples occur in packages/** paths where no-console is enforced. The pattern is about enforcing a specific linting rule across a path scope rather than a one-off code issue. This is a path-scoped convention that should be documented to prevent future violations.

### Alternatives considered

- `agents`: Could be a repo-wide convention if console restrictions apply beyond packages/**, but evidence suggests it's path-specific.
- `adr`: Could document the architectural decision to restrict console logging in library modules, but the primary value is the enforcement rule itself.

### Actions

```bash
promote candidate_099 --target path_scoped_rule
promote ignore candidate_099 --reason "..."
promote snooze candidate_099 --days 30
```

---

## 7. Core error metadata must be kept separate from user-declared payload data to prevent runtime desynchronization in tRPC error handling.

- **ID**: `candidate_107`
- **Target**: `adr` → `docs/adr/error-metadata-isolation.md`
- **Confidence**: 0.85
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 3 comments

### Evidence

- PR #7279 `packages/server/src/unstable-core-do-not-import/error/TRPCDeclaredError.ts` — "⚠️ Potential issue | 🟠 Major

Keep core error metadata separate from declared p..." [link](https://github.com/trpc/trpc/pull/7279#discussion_r2970588769)
- PR #7279 `packages/server/src/unstable-core-do-not-import/error/TRPCDeclaredError.ts` — "`Object.assign(this, instanceFields)` can overwrite core `TRPCError` fields (not..." [link](https://github.com/trpc/trpc/pull/7279#discussion_r2971303184)
- PR #7279 `packages/server/src/adapters/ws.ts` — "In this error handler, `resolveProcedureError(..., { path })` is called without ..." [link](https://github.com/trpc/trpc/pull/7279#discussion_r2971303185)

### Suggested patch

```markdown
# ADR-001: Separate Core Error Metadata from User-Declared Payload Data

## Status
Proposed

## Context
tRPC error handling requires maintaining a clear boundary between framework-managed error metadata and user-provided payload data. When core error properties (code, cause, name, stack) can be overwritten by user-declared data, runtime error state becomes desynchronized from the declared error shape, leading to unpredictable behavior and difficult-to-debug failures in error handling pipelines.

## Decision
Core error metadata (code, cause, name, stack) must be kept separate and protected from user-declared payload data. Error builders and handlers must not use shallow merge operations (Object.assign) that allow user data to overwrite framework properties. Instead, user payload must be explicitly nested or validated to prevent collision with reserved error fields.

## Consequences
- User-declared error data is isolated in a dedicated payload property, preventing accidental overwrites of core metadata
- Error handlers can safely rely on code, cause, name, and stack properties maintaining their runtime values
- Declared error shapes remain predictable and match their registered definitions throughout the error lifecycle
- Additional validation is required when constructing errors to ensure user data doesn't include reserved field names
- Error serialization and deserialization logic must explicitly handle the separation of metadata and payload layers
```

### Why this target

This pattern describes the architectural rationale behind a critical design decision: why error metadata (code, cause, name, stack) must not be overwritten by user payload. The comments explain the 'why' (preventing desync between runtime error state and declared shape) rather than just the 'what' (don't use Object.assign). This is a trade-off decision about error object design that future maintainers need to understand to avoid regressions.

### Alternatives considered

- `path_scoped_rule`: Could be scoped to packages/server/src/unstable-core-do-not-import/error/ since it's specific to TRPCDeclaredError implementation
- `agents`: Could be a general coding convention about not mutating error objects, but the pattern is too specific to tRPC's error architecture

### Actions

```bash
promote candidate_107 --target adr
promote ignore candidate_107 --reason "..."
promote snooze candidate_107 --days 30
```

---

## 8. Package.json bin entries must include bin/ directory in files array and ensure the referenced script exists.

- **ID**: `candidate_108`
- **Target**: `path_scoped_rule` → `PACKAGE_RULES.md`
- **Confidence**: 0.85
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/*/package.json`

### Evidence

- PR #7252 `packages/next/package.json` — "⚠️ Potential issue | 🔴 Critical

Critical: Broken CLI entrypoint across 4 packa..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937228660)
- PR #7252 `packages/server/package.json` — "The package defines a `bin` entry (`./bin/intent.js`), but `bin/` is not include..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968125965)

### Suggested patch

```markdown
---
applyTo: "packages/*/package.json"
---
# Binary Entrypoint Configuration

- Declare `bin` entries only for scripts that exist in the `bin/` directory and verify the file path is correct
- Always include `"bin/"` in the `files` array when declaring `bin` entries to ensure CLI scripts are published
- Use relative paths consistently in `bin` entries (e.g., `"./bin/script-name.js"`) matching the actual file location
- Validate that referenced bin scripts have proper shebang headers (`#!/usr/bin/env node`) and executable permissions
- Test package installation locally with `npm pack` and `npm install <tarball>` to verify bin entrypoints work after publishing
```

### Why this target

This is a repeated structural issue across multiple packages in the monorepo where bin entries are declared but the bin/ directory is not included in the files allowlist, causing broken CLI entrypoints on installation. It's specific to package.json configuration patterns and applies to all packages that declare bin entries.

### Alternatives considered

- `agents`: Could be a cross-cutting convention if all packages should follow this pattern, but it's more of a structural validation rule specific to package.json files.
- `test`: Could be enforced as a test that validates bin entries exist and are included in files array, but the pattern is about configuration correctness rather than runtime behavior.

### Actions

```bash
promote candidate_108 --target path_scoped_rule
promote ignore candidate_108 --reason "..."
promote snooze candidate_108 --days 30
```

---

## 9. Multiple @trpc/* packages should not expose generic binary names like 'intent' in their bin field, as this causes node_modules/.bin collisions and breaks installations; use namespaced names (e.g., trpc-intent) or avoid re-exporting.

- **ID**: `candidate_113`
- **Target**: `adr` → `docs/adr/bin-naming-convention.md`
- **Confidence**: 0.85
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 5 comments

### Evidence

- PR #7252 `packages/server/package.json` — "`bin.intent` points to `./bin/intent.js`, but `packages/server/bin/` doesn't exi..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937362645)
- PR #7252 `packages/server/package.json` — "`bin.intent` references `./bin/intent.js`, but there’s no `bin/intent.js` in thi..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937220221)
- PR #7252 `packages/server/package.json` — "Exposing a generic `intent` binary from this package is likely to cause `node_mo..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968259331)
- PR #7252 `packages/client/package.json` — "Exposing a generic `intent` binary from this package is likely to cause `node_mo..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968259335)
- PR #7252 `packages/next/package.json` — "Exposing a generic `intent` binary from this package is likely to cause `node_mo..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968259301)

### Suggested patch

```markdown
# ADR-001: Namespaced CLI Entry Points in @trpc/* Packages

## Status
Proposed

## Context
The @trpc monorepo contains multiple packages that may expose CLI tools via the `bin` field in package.json. Using generic binary names (e.g., `intent`, `server`) creates collisions in `node_modules/.bin/` when users install multiple @trpc packages, causing installation failures and shadowing real system binaries. This architectural decision establishes a pattern for all current and future CLI entry points across the monorepo.

## Decision
All @trpc packages that expose CLI tools must use namespaced binary names in the format `trpc-<tool-name>` (e.g., `trpc-intent`, `trpc-server`) rather than generic names. Alternatively, packages should avoid re-exporting CLI entry points entirely if the functionality is better served through a dedicated CLI package. Each bin entry must reference an actual file that exists in the package at publish time.

## Consequences
- Eliminates `node_modules/.bin/` collisions across @trpc packages
- Prevents shadowing of real system binaries and tools
- Ensures consistent naming convention across the monorepo
- Requires validation that all bin references point to existing files before publishing
- Improves discoverability by making CLI tools clearly associated with the @trpc namespace
- May require documentation updates to guide users on the new binary naming scheme
- Future package additions must follow this pattern during code review
```

### Why this target

This is fundamentally an architectural decision about how to structure CLI entry points across a monorepo. The comments explain both the immediate problem (missing files, collisions) and the underlying rationale (namespace pollution, shadowing real binaries). This is a design pattern that should be documented as a decision for future package additions to the monorepo, not just a one-off fix.

### Alternatives considered

- `path_scoped_rule`: Could be a rule scoped to packages/*/package.json to enforce namespaced binary names, but the decision rationale (why namespacing matters) is more important than the mechanical rule.
- `agents`: Could be added to coding guidelines if this is a cross-cutting convention, but it's specific enough to package.json structure that an ADR better captures the trade-offs.

### Actions

```bash
promote candidate_113 --target adr
promote ignore candidate_113 --reason "..."
promote snooze candidate_113 --days 30
```

---

## 10. Test helpers must avoid temporal dead zone errors by declaring subscription variables before subscribe() calls to handle synchronous onError callbacks safely.

- **ID**: `candidate_114`
- **Target**: `test` → `TEST_PATTERNS.md`
- **Confidence**: 0.85
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 3 comments

### Evidence

- PR #7279 `packages/tests/server/websockets.test.ts` — "This helper can throw a ReferenceError if the subscription calls `onError` synch..." [link](https://github.com/trpc/trpc/pull/7279#discussion_r2970571732)
- PR #7279 `packages/tests/server/httpSubscriptionLink.test.ts` — "This helper can throw a ReferenceError if `subscribe(...)` invokes `onError` syn..." [link](https://github.com/trpc/trpc/pull/7279#discussion_r2970571739)
- PR #7279 `packages/tests/server/websockets.test.ts` — "⚠️ Potential issue | 🟡 Minor

Avoid capturing `subscription` before it exists.
..." [link](https://github.com/trpc/trpc/pull/7279#discussion_r2970588775)

### Suggested patch

```markdown
# Test Helper Temporal Dead Zone Prevention

## Test File Location
`tests/helpers/subscription-helpers.test.ts`

## What the Test Should Assert

The test should verify that subscription test helpers safely handle synchronous `onError` callbacks without triggering ReferenceError due to temporal dead zone violations. Specifically:

1. When a source observable calls `onError` synchronously during the `subscribe()` call, the error callback should execute without referencing uninitialized variables
2. The subscription variable must be declared (with `let`) before the `subscribe()` call, ensuring it exists in scope even if the callback executes immediately
3. Error handling should complete successfully regardless of whether the error occurs synchronously or asynchronously

## Code Sketch

```typescript
describe('Subscription helpers - temporal dead zone safety', () => {
  it('should handle synchronous onError without ReferenceError', () => {
    const source = new Observable(subscriber => {
      subscriber.error(new Error('sync error'));
    });

    let subscription; // Declare BEFORE subscribe
    const errors: Error[] = [];

    expect(() => {
      subscription = source.subscribe({
        next: () => {},
        error: (err) => {
          errors.push(err);
          subscription?.unsubscribe(); // Safe to reference
        }
      });
    }).not.toThrow();

    expect(errors).toHaveLength(1);
    expect(errors[0].message).toBe('sync error');
  });
});
```
```

### Why this target

This describes a testable runtime invariant specific to subscription test helpers: synchronous error callbacks must not reference uninitialized variables. The pattern appears across multiple test files in the same PR and describes behavior that causes flaky tests when not handled correctly. This is a mechanical invariant that should be enforced in test code.

### Alternatives considered

- `path_scoped_rule`: Could be scoped to packages/tests/** since it only affects test helpers, but the underlying issue (TDZ with async callbacks) is a general JavaScript pattern worth documenting broadly.
- `agents`: Could be a general coding convention, but the issue is specific enough to subscription testing patterns that test-scoped documentation is more appropriate.

### Actions

```bash
promote candidate_114 --target test
promote ignore candidate_114 --reason "..."
promote snooze candidate_114 --days 30
```

---

## 11. Package.json bin entries must reference executable files that actually exist in the package directory, or publishing will create broken executables on install.

- **ID**: `candidate_118`
- **Target**: `path_scoped_rule` → `PACKAGE_VALIDATION.md`
- **Confidence**: 0.85
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/*/package.json`

### Evidence

- PR #7252 `packages/tanstack-react-query/package.json` — "`bin.intent` points to `./bin/intent.js`, but `packages/tanstack-react-query/bin..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937362635)
- PR #7252 `packages/next/package.json` — "`bin.intent` points to `./bin/intent.js`, but there is no `packages/next/bin/` d..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937362630)

### Suggested patch

```markdown
---
applyTo: "packages/*/package.json"
---
# Bin Entry Validation

- Verify all `bin` field entries reference executable files that exist in the package directory
- Ensure the directory structure matches bin paths (e.g., `./bin/intent.js` requires `packages/[name]/bin/` directory)
- Test package installation locally to confirm executables are created correctly
- Remove or update bin entries if corresponding executable files are not included in the PR
- Check that executable files have proper permissions and shebang headers for CLI functionality
```

### Why this target

This is a repeated validation rule specific to package.json files in monorepo packages. The pattern appears in multiple packages (tanstack-react-query, next) and describes a concrete, testable constraint that should be enforced across all package directories to prevent broken installations.

### Alternatives considered

- `test`: This could be a runtime/installation test that verifies all bin entries reference existing files before publishing
- `agents`: Could be a general instruction for all package.json modifications, though it's more specific to bin field validation

### Actions

```bash
promote candidate_118 --target path_scoped_rule
promote ignore candidate_118 --reason "..."
promote snooze candidate_118 --days 30
```

---

## Filtered out

Items that were excluded from promotion candidates. Useful for tuning thresholds or for team review of edge cases.

### Already promoted (3)

- 패키지의 package.json에서 bin 필드에 명시된 실행 파일 경로가 files 배열에 포함되어 있지 않으면, 해당 실행 파일이 배포 시 누락되어 소비자에게 깨진 바이너리가 전달된다. 따라서 bin 디렉토리를 files에 반드시 포함시켜야 한다.
- GitHub Actions 워크플로우에서 lerna publish 명령어가 git 태깅 및 푸시를 수행하므로, permissions.contents 권한 설정과 lerna 옵션(--no-push, --no-git-tag-version) 사용에 주의해야 한다.
- metadata-only 프레임에 대해 onSseEvent 호출을 건너뛰어야 하며, retry: 전용 청크가 data: undefined로 호출되는 문제를 방지해야 한다.

### Not promotable (target=none / invalid) (63)

- Multiple unrelated package.json configuration issues across different packages and concerns. — target=`none`, confidence=0.30
  - These comments address fundamentally different problems: (1) peer dependency version constraints for @hey-api/openapi-ts, (2) missing bin/ directory entries and devDependencies, (3) files allowlist not including bin/, and (4) script pinning peerDependencies to exact versions. While they all involve package.json, they are distinct issues with different root causes and solutions. The dismissal context ('You've done something very wrong') and 2 reviewer dismissals suggest these were false positives or misunderstandings. The cluster lacks genuine pattern repetition—it's a collection of surface-level similar-looking comments about different underlying problems.
- Various unrelated code quality issues across different files and technologies — target=`none`, confidence=0.20
  - These are fundamentally different issues: missing imports in test files, unused imports in MDX, test convention violations, missing dependencies in package.json, and incorrect .prettierignore paths. They share only surface-level similarity (all are 'missing/unused' concerns) but address distinct problems in different contexts with different solutions. This is not a repeated pattern but rather a collection of one-off code review observations.
- Various defensive programming issues across different modules — target=`none`, confidence=0.20
  - These are fundamentally different issues: (1) unsafe type casting and property access on unknown types, (2) null handling in stream processing, (3) OpenAPI schema reference handling, (4) OpenAPI reference key misalignment, and (5) DoS mitigation via array allocation. They share only surface-level similarity (defensive checks) but address distinct technical problems in different domains. No unified pattern or principle applies across all five.
- Multiple unrelated inconsistencies across different packages and artifacts — target=`none`, confidence=0.30
  - These are distinct issues affecting different systems (version mismatches, documentation inconsistencies, missing CLI files, artifact count errors) rather than a single repeated pattern. Each comment addresses a different underlying problem in different files/packages.
- Various documentation and comment accuracy issues across different files and domains — target=`none`, confidence=0.30
  - These are distinct, one-off issues affecting different parts of the codebase (OpenAPI docs, build scripts, skill references, JSDoc comments). They share only surface-level similarity (documentation/comment inaccuracy) but address fundamentally different problems: grammar fixes, outdated comments, misleading log messages, version mismatches, and conflicting API documentation. None represent a repeated pattern that would benefit from centralized documentation or enforcement.
- Various unrelated bugs in different scripts and workflows — target=`none`, confidence=0.20
  - These are four distinct bugs in completely different codebases (codegen script, documentation checker, GitHub Actions workflows) with no shared underlying pattern. Example 1 is about comma operator misuse in JavaScript, Example 2 is about regex pattern matching in a documentation tool, Example 3 is about bash pipeline error handling, and Example 4 is about error suppression in shell scripts. They happen to all be bugs, but they don't represent a repeated coding convention, architectural decision, or cross-cutting concern that should be documented in repository memory.
- Various type mismatches and missing schema discriminators across different packages — target=`none`, confidence=0.20
  - These are fundamentally different issues: (1) missing Zod discriminator in schema, (2) type annotation mismatch in TRPC link assignment, (3) undefined imports in documentation examples, (4) content-type default mismatch, and (5) abort handling logic. They share only surface-level similarity of being 'potential issues' but address unrelated domains (validation schemas, type systems, documentation, HTTP headers, stream handling). No actionable pattern emerges that would benefit future reviews.
- Multiple distinct TypeScript type system and API design issues across different packages — target=`none`, confidence=0.30
  - These comments address fundamentally different problems: (1) TypeScript intersection type logic in ProtectedIntersection, (2) JSON schema $ref placeholder handling, (3) test coverage for schema generation, (4) method chaining in proxy objects, and (5) special method handling in proxy traps. While they span related packages, they are not instances of the same repeated pattern—they are separate bugs/design issues that happen to appear in the same review cycle.
- Various ESLint and linting configuration issues across different files and concerns. — target=`none`, confidence=0.40
  - These comments address fundamentally different problems: (1) incorrect ESLint flat config API usage for react-hooks plugin, (2) inconsistent formatting of reportUnusedDisableDirectives across configs, (3) missing rule migrations from ESLint to Oxlint, and (4) stale ESLint suppression comments in Oxlint-managed code. While all relate to linting infrastructure, they are distinct issues requiring different fixes, not a repeated pattern. The first is a v7 API misuse, the second is a style consistency issue, the third is a migration tracking problem, and the fourth is a comment cleanup task. None represent a generalizable pattern worth preserving in repository memory.
- Mixed unrelated issues about GitHub Actions workflows, package.json scripts, and pnpm argument passing. — target=`none`, confidence=0.20
  - These four comments address fundamentally different problems: (1) security risk from workflow_dispatch with secrets, (2) job concurrency/serialization in CI, (3) concurrent package manager operations causing race conditions, and (4) pnpm CLI argument syntax. Despite surface similarity in being CI/workflow-related, they describe distinct technical issues with different root causes and solutions. They are not instances of the same repeated pattern.
- Various unrelated TypeScript and package configuration issues across different packages — target=`none`, confidence=0.20
  - These are fundamentally different issues: (1) missing generic parameters in generated code, (2) type guard for object spread, (3) dependency classification, (4-5) binary entry point conflicts. They share only surface-level similarity (all are 'potential issues') but address distinct problems in different domains with different solutions. Not a repeated pattern.
- Mixed documentation and code quality issues across unrelated files — target=`none`, confidence=0.30
  - These five comments address fundamentally different problems: missing required schema fields, unsafe type assertions, incomplete fixes, unsafe JSON parsing, and invalid TypeScript syntax. They are not instances of the same repeated pattern—they happen to appear in documentation and test files but target distinct code issues. No cross-cutting convention or architectural principle unites them.
- CodeRabbit bot's meta-commentary about withdrawing from review conversations — target=`none`, confidence=0.15
  - These are not substantive code review comments but rather bot meta-commentary about its own participation in PR discussions. The cluster mixes bot withdrawal messages with unrelated severity warnings that appear to be incomplete or placeholder text. There is no underlying coding pattern, convention, or architectural decision to preserve.
- Multiple distinct OpenAPI schema generation issues across different files and concerns. — target=`none`, confidence=0.30
  - These are five separate, unrelated issues: (1) OpenAPI 3.1 nullable syntax violation, (2) empty Zod recursive schemas, (3) nested description application with $ref, (4) symbol-based property name instability, (5) self-referential type generation. They share only the domain (OpenAPI generation) but address fundamentally different problems requiring different fixes. No single pattern or principle unifies them.
- Three unrelated documentation issues across different files with different underlying problems. — target=`none`, confidence=0.20
  - These are three distinct issues: (1) shell examples using repo-specific paths instead of generic placeholders, (2) incomplete redirect configuration in vercel.json, and (3) incorrect function signature in documentation. They share only surface-level similarity (documentation problems) but address fundamentally different concerns in different files with no cross-cutting pattern that would benefit future reviews.
- Various unrelated bug fixes across different modules — target=`none`, confidence=0.35
  - These three comments address fundamentally different issues: (1) error logging leaking sensitive data, (2) query cache configuration being overwritten, and (3) HTTP caching headers being incomplete. While all are labeled as potential issues, they involve different code paths, different modules, and different underlying problems. They share only surface-level similarity in being bug reports, not a genuine repeated pattern that would benefit from centralized documentation.
- Various unrelated code quality issues across different packages and domains. — target=`none`, confidence=0.20
  - These four comments address fundamentally different problems: (1) missing URLSearchParams handling in a serializer, (2) overly broad computed property matching in TypeScript symbol filtering, (3) language parsing in a documentation checker, and (4) missing MDX support in a markdown processor. Despite surface similarity in being 'potential issues', they involve different codebases, different root causes, and different solutions. They are not instances of a repeated pattern but rather isolated bugs in separate systems.
- Three unrelated code review issues: missing error parameter passing, interceptor function signature mismatch, and React hooks called in async functions. — target=`none`, confidence=0.30
  - These are three distinct, unrelated issues affecting different packages and subsystems. Example 1 concerns error handling in WebSocket adapters, Example 2 concerns API interceptor typing in OpenAPI generation, and Example 3 concerns React hook usage rules in documentation. They share no common underlying pattern or principle—only surface-level similarity in being code quality issues. Promoting any of these would create false connections and dilute repository memory.
- Three unrelated package.json issues involving dependency versions, peer dependency bumps, and export path naming. — target=`none`, confidence=0.20
  - These are three distinct, one-off issues affecting different packages (eslint-plugin-react-hooks, @hey-api/openapi-ts, and @trpc/tanstack-react-query) with different root causes (version bug, peer dependency breaking change, export path naming). They share only surface-level similarity (all mention package.json and versions) but address fundamentally different problems. None represent a repeated pattern that would benefit future reviews.
- Various package.json and lockfile maintenance issues across different packages — target=`none`, confidence=0.30
  - These are three distinct, unrelated issues: (1) lockfile sync after dependency updates, (2) version pinning conflict between related packages, and (3) postinstall script referencing missing files. They happen to all involve package.json but address fundamentally different problems with different solutions. This is not a repeated pattern but rather a collection of one-off dependency management issues specific to individual PRs.
- Various TypeScript linting issues in auto-generated and test files — target=`none`, confidence=0.30
  - These are three distinct issues: (1) missing reason in @ts-expect-error directive in auto-generated code, (2) incorrect comment text describing the wrong API, and (3) generic type annotation placement violation. They share only surface-level similarity (TypeScript/linting) but address fundamentally different problems with different solutions. Not a genuine repeated pattern.
- Various unrelated code quality and logic issues across different packages — target=`none`, confidence=0.15
  - These four comments address fundamentally different problems: (1) null-handling in connection params, (2) payload wrapping in SSE streams, (3) abort signal timing in stream termination, and (4) non-null assertion style. Despite similar severity labels and appearing in the same cluster, they are one-off issues specific to their respective code locations with no underlying repeated pattern. Each requires context-specific fixes rather than a generalizable rule or convention.
- Various documentation and code examples contain incomplete or misleading placeholder content that needs correction. — target=`none`, confidence=0.30
  - These are distinct, one-off issues affecting different files and domains (data transformers, OpenAPI schemas, CLI documentation, Next.js setup). They share only surface-level similarity (placeholders/incomplete examples) but address fundamentally different problems: transformer setup, schema type handling, CLI text accuracy, and package installation. Each is specific to its context and doesn't represent a repeated pattern that would benefit from centralized documentation.
- Multiple unrelated code quality issues across different OpenAPI test files — target=`none`, confidence=0.20
  - These four comments address fundamentally different problems: (1) a file path hashing bug in a build script, (2) ambiguous OpenAPI schema encoding declarations, (3) JSON serialization of Date objects in URL bodies, and (4) conditional event callback logic in SSE handling. Despite surface similarity in phrasing and all appearing in openapi test files, they target distinct code domains with no shared underlying pattern or principle. They are one-off issues specific to their respective PRs.
- Various guard conditions missing in different code paths — target=`none`, confidence=0.30
  - These are three distinct issues in different files addressing different concerns: (1) service route validation, (2) stream detection logic unification, and (3) cache guard conditions. While all mention 'guard', they address fundamentally different problems with different solutions. They are not instances of the same repeated pattern.
- Various documentation and metadata quality issues across different files and contexts. — target=`none`, confidence=0.30
  - These three comments address fundamentally different issues: (1) incomplete TODO text in a README, (2) incomplete PR description template fields, and (3) incorrect file path in a YAML header comment. While all involve incomplete or inaccurate text, they are one-off quality issues specific to individual PRs and files, not a repeated pattern that would benefit from durable documentation. Each requires context-specific fixes rather than a general rule or convention.
- Multiple distinct issues about dependency version mismatches and reproducibility in a monorepo toolchain. — target=`none`, confidence=0.30
  - These are three separate concerns: (1) vitest version consistency across workspaces, (2) pnpm version declaration mismatch between package.json and .tool-versions, and (3) npm version pinning in CI workflows. While all relate to dependency management, they address different root causes and require different fixes. They are not instances of a single repeated pattern but rather separate one-off issues that happened to appear in the same PR review cycle.
- Typo fixes across various files (error messages, documentation, test assertions). — target=`none`, confidence=0.95
  - These are one-off typo corrections in different contexts (error messages, docs, test assertions) rather than a repeated pattern requiring durable knowledge. Each typo is specific to its location and has already been addressed in the PRs. There is no underlying coding convention, architectural decision, or cross-cutting practice to preserve. Typo fixes are routine maintenance and do not warrant promotion to repository memory.
- Three unrelated code review comments about file naming, variable naming, and import style conventions. — target=`none`, confidence=0.30
  - These comments address fundamentally different issues: (1) markdown file naming convention, (2) variable naming clarity in test code, and (3) import style consistency. They are not instances of the same repeated pattern—they just happen to be minor style/convention suggestions across different files. The cluster lacks cohesion around a single underlying principle or practice.
- Various linter rule violations across different files and domains — target=`none`, confidence=0.20
  - These are three distinct linting issues (Biome forEach callback return, Markdown language specifier, filename case convention) affecting different file types and paths. They share only surface-level similarity as 'linter warnings' but address fundamentally different problems with no underlying pattern that would benefit future code review or architectural guidance.
- Type parameter mismatches in generated SSE and React Query utility code. — target=`none`, confidence=0.40
  - These are distinct issues across different codebases (generated SSE files vs. React Query utilities) with different root causes. Examples 1-2 are about missing generic parameters in SSE client initialization, while Example 3 is about incorrect generic parameter ordering in React Query type inference. They share surface-level similarity (generic type problems) but address fundamentally different type propagation issues in unrelated systems. This appears to be generated code that may need regeneration rather than a repeatable pattern to document.
- Two unrelated issues: workflow path exclusion logic and missing files in npm package distribution. — target=`none`, confidence=0.20
  - These are fundamentally different problems. The first is about GitHub Actions workflow trigger conditions and path filtering logic. The second is about npm package manifest configuration and missing dependencies. They happen to both involve package.json files but address entirely separate concerns (CI/CD automation vs. package distribution). This is not a repeated pattern but rather two distinct issues that coincidentally mention similar file paths.
- Three unrelated documentation and type definition issues across different tRPC components — target=`none`, confidence=0.30
  - These are three distinct issues: (1) SSE authentication method documentation, (2) TypeScript type definition mismatch for keepAlive option, (3) sidebar ID naming inconsistency. They share only surface-level similarity (all are documentation/config problems) but address fundamentally different concerns with different solutions. Not a repeated pattern.
- Temporal dead zone issues with subscription callbacks in test utilities — target=`none`, confidence=0.40
  - These are distinct issues: (1) captures subscription in temporal dead zone during synchronous onError, (2) missing complete forwarding in spy link, (3) unsubscribe in onError handler with const declaration. While they share similar code patterns and appear in related test files, they describe different underlying problems (TDZ safety, observer contract compliance, and subscription lifecycle management). They are not instances of the same repeated pattern but rather separate bugs in related code.
- OpenAPI schema generation has issues with recursive types and tuple handling in JSON Schema 2020-12 compliance. — target=`none`, confidence=0.40
  - These are distinct technical issues: (1) cyclic type handling producing degenerate self-referential schemas, and (2) tuple validation using deprecated `items` array instead of `prefixItems`. While both relate to OpenAPI schema generation, they address different underlying problems and would require different fixes. The comments are about specific bugs in the codebase rather than a repeated pattern or convention.
- Documentation completeness and code example accuracy in tRPC setup guides — target=`none`, confidence=0.30
  - These are three distinct documentation issues: (1) verifying a guide exists, (2) missing documentation for a removed feature, and (3) incomplete code examples. They are not a repeated pattern but rather separate quality concerns about different documentation sections. Each requires different remediation and doesn't represent a generalizable coding convention or architectural principle.
- Async function handling in different contexts — target=`none`, confidence=0.40
  - The two examples address fundamentally different issues: the first is about awaiting an async function in a script entry point to prevent premature exit, while the second is about error handling patterns in documentation examples. Despite surface similarity in mentioning async/await, they are distinct problems in different domains (test scripts vs. documentation) with different solutions. This is not a repeated pattern but rather coincidental similarity in phrasing.
- Multiple unrelated issues in test infrastructure: async/await handling, Vitest export format, and dependency version mismatches. — target=`none`, confidence=0.40
  - These are three distinct problems with different root causes and solutions. Example 1 is about promise handling in a codegen script. Example 2 is about Vitest module export conventions. Example 3 is about dependency version alignment. They happen to appear in related test files but address fundamentally different issues, so promoting them as a single pattern would be misleading and unhelpful.
- Two unrelated documentation/API issues about incorrect defaults and misleading scope descriptions. — target=`none`, confidence=0.30
  - These are fundamentally different issues: one concerns a JSONL content-type default mismatch, the other concerns incorrect scope documentation for maxBatchSize. They happen to both be documentation problems but address different APIs and have different solutions. This is not a repeated pattern but rather two separate one-off documentation bugs.
- Two unrelated issues: glob pattern matching in codecov.yml and incomplete validation logic in a test helper function. — target=`none`, confidence=0.30
  - These are fundamentally different problems. The first is about fixing a specific glob pattern in codecov.yml to properly exclude files. The second is about improving validation logic in a test utility function. They happen to both involve the packages/openapi directory but address distinct concerns with no shared underlying pattern or principle.
- Avoid importing DOM-focused testing libraries in Node.js environment test files and prevent duplicate setup file execution in merged Vitest configs. — target=`none`, confidence=0.40
  - These are two distinct issues: (1) unnecessary DOM library imports in Node environments, and (2) duplicate setup file registration in config merging. They happen to involve testing infrastructure but address different problems with different solutions. The first is a code smell in specific test files; the second is a config management concern. Neither is a repeated pattern—they're one-off observations about different aspects of the test setup.
- Documentation and generated artifacts need updates to reflect code changes. — target=`none`, confidence=0.30
  - These are two distinct issues: one about updating documentation examples to match new type signatures, and another about regenerating JSON test artifacts. They happen to both involve sync issues but target different file types and have different root causes. The first is a documentation maintenance issue specific to PR #7279, while the second is a test artifact regeneration issue specific to PR #7231. Neither represents a repeatable pattern that should be preserved in repository memory.
- Documentation clarity issues in different contexts — target=`none`, confidence=0.30
  - These are two distinct documentation problems: one about ambiguous version alignment terminology in API docs, another about a content-type format mismatch in streaming documentation. They share only surface-level similarity (both are doc clarification requests) but address fundamentally different issues with no underlying pattern to preserve.
- Two unrelated bug fixes in different files with different root causes — target=`none`, confidence=0.20
  - These are distinct issues in different code domains (HTTP subscription links vs version string parsing) with no shared pattern or principle. The first is about preserving object properties during a merge operation; the second is about regex pattern matching. They happen to use similar phrasing ('Preserve') but address fundamentally different problems.
- Performance optimization suggestions for hash computation and array allocation in different contexts. — target=`none`, confidence=0.30
  - The three examples address fundamentally different performance issues: (1-2) are about redundant file hashing in a code generation script, while (3) is about premature array allocation in a path parsing utility. Despite surface similarity in being performance optimization suggestions, they target different code paths, different modules, and different underlying problems. Examples 1 and 2 are near-duplicates of the same issue in one file, while example 3 is unrelated. This is not a genuine cross-cutting pattern.
- Two unrelated dependency/configuration issues: pnpm version mismatch and missing peer dependency declaration. — target=`none`, confidence=0.30
  - These are distinct problems affecting different aspects of the repo. The first is a toolchain version mismatch between .tool-versions and package.json (pnpm 8 vs 9). The second is a missing peer dependency declaration for @tanstack/intent in a published bin entry. They happen to use similar phrasing ('Consider...') but address fundamentally different issues with different remediation paths. Neither is a repeated pattern—they are one-off configuration issues specific to their respective PRs.
- Two unrelated markdown/documentation issues: one about HTML/Markdown syntax mixing, another about filename mismatches in code examples. — target=`none`, confidence=0.30
  - These are distinct, one-off documentation issues in different files with different root causes. The first is about HTML tag nesting with Markdown syntax; the second is about inconsistent filenames in a code example. Neither represents a repeated pattern or cross-cutting convention that would benefit future reviews. Both are specific to their respective PRs and files.
- Two unrelated code review comments about different API compatibility issues in different files. — target=`none`, confidence=0.95
  - These are fundamentally different issues: one concerns OpenAPI Schema Object type compatibility in a types definition file, while the other concerns documentation accuracy for HTTP subscription link options. They happen to both involve type definitions and API compatibility, but they address different underlying problems in different contexts (type definitions vs. documentation). This is surface-level similarity, not a repeated pattern.
- Two unrelated documentation/example issues in different parts of the codebase — target=`none`, confidence=0.30
  - These are distinct, one-off issues affecting different files and systems. Example 1 is about using the correct API in a Next.js skill documentation example. Example 2 is about correcting JSDoc comments for serialization types. They share no underlying pattern—they are separate documentation corrections that happen to both involve examples/comments.
- Mixed issues: CLI error message prefixing, OpenAPI version documentation mismatch, and user-facing typo corrections. — target=`none`, confidence=0.30
  - These are three distinct, one-off issues in different files with different underlying problems. Example 1 is about error message formatting in CLI parsing. Examples 2-3 are about documentation/version mismatch in OpenAPI generation. Examples 4-5 are duplicates about a typo in an error message. They do not represent a repeated pattern worthy of durable storage—each is a specific bug fix for its respective PR.
- Various minor documentation and code quality issues across different files — target=`none`, confidence=0.30
  - These three comments address fundamentally different issues: (1) a TypeScript linting requirement for @ts-expect-error descriptions, (2) an incomplete TODO comment in documentation, and (3) incomplete warning text in docs. Despite similar severity levels and being flagged as 'minor', they are one-off issues specific to individual files and PRs, not a repeated pattern. Each has already been addressed in the respective PRs.
- Various documentation and configuration errors in a single PR — target=`none`, confidence=0.20
  - These are distinct, one-off issues affecting different files (SKILL.md, YAML paths, package.json bin entries) in PR #7252. They do not represent a repeated pattern across multiple PRs or a cross-cutting convention. Each issue is specific to its file and context, and all were addressed in that single PR. There is no evidence of this pattern repeating elsewhere in the codebase.
- Shell command safety and documentation formatting issues — target=`none`, confidence=0.30
  - The cluster conflates two unrelated issues: (1) security concerns about uncontrolled absolute paths in shell commands (examples 1-2), and (2) documentation formatting consistency for code fence syntax (example 3). These are fundamentally different problems requiring different solutions. Examples 1 and 2 appear to be duplicates of the same issue in one PR, not a repeated pattern across multiple PRs. Example 3 is a one-off documentation style comment unrelated to the security concern.
- Error handling specificity in filesystem operations and regex pattern matching in a build script. — target=`none`, confidence=0.40
  - The cluster contains two distinct issues: (1) overly broad error suppression in directory reads (examples 1-2), and (2) an overly broad regex pattern for fence matching (example 3). While examples 1 and 2 address the same underlying problem in the same file, example 3 is a different issue entirely. Additionally, these are one-off code quality issues specific to a single script file, not patterns that would benefit future development across the codebase. The knowledge is contextual to this specific PR and file.
- Two unrelated documentation issues in different skill files about API patterns — target=`none`, confidence=0.30
  - These are distinct issues in different files addressing different APIs (lazy import patterns vs mutationOptions guidance). They happen to both be documentation improvements but target fundamentally different knowledge domains and don't represent a repeated pattern that needs preservation.
- Implementation of cursor field handling in infinite queries does not match the documented fix for issue #6862, and test schema changes mask rather than validate the root issue. — target=`pr_only`, confidence=0.85
  - These comments are specific to PR #7361 and concern a particular implementation detail of cursor handling in tRPC's react-query integration. The issue is about whether the code correctly implements the stated fix (cursor: infiniteParams.pageParam ?? null) and whether tests properly validate the behavior. This is not a cross-cutting pattern or architectural principle—it's a one-off code review concern about a specific PR's correctness relative to its stated objectives.
- Multiple unrelated issues about bin exports and dependency versioning in a single PR — target=`none`, confidence=0.40
  - These are two distinct problems: (1) a missing bin directory and naming collision in package.json, and (2) version pinning inconsistency in a workflow file. They happen to appear in the same PR but address fundamentally different concerns with different remediation paths. The second example is also incomplete/truncated, making it difficult to assess the full pattern.
- Error handling and retry logic issues in generated OpenAPI client code — target=`none`, confidence=0.30
  - The cluster contains two distinct issues: (1) error transformation chaining in interceptors, and (2) retry budget and abort handling in SSE. These are fundamentally different problems affecting different code paths. The first example is incomplete/truncated, making it impossible to assess the full pattern. Additionally, these appear to be issues in generated test code (marked as .gen.ts files), not production patterns worth documenting in repository memory.
- OpenAPI version mismatch and serializer handling issues in generated code — target=`none`, confidence=0.30
  - These are two distinct issues: (1) a schema generation version compatibility problem between OpenAPI 3.1.1 declaration and 3.0-style nullable handling, and (2) a Date serialization bug in URL search params. They happen to appear in the same PR but address fundamentally different concerns with different solutions. The first is an architectural decision about OpenAPI compliance; the second is a specific bug in serializer logic. They should not be conflated into a single pattern.
- Mixed issues: camelCase filename exception for SKILL.md and an unrelated attribution error in skill documentation. — target=`none`, confidence=0.30
  - The cluster conflates two distinct issues: (1) a filename convention exception that should be documented in coding guidelines, and (2) a factual error about module attribution in skill documentation. These are fundamentally different problems requiring different solutions. The filename issue is real but affects only the guideline documentation itself, not code behavior. The attribution error is a one-off content mistake in a specific file.
- Mixed documentation issues including test file cleanup, misleading error-path examples, and relative import path corrections. — target=`none`, confidence=0.30
  - These are three distinct issues with different underlying problems: (1) test file organization, (2) example code accuracy/representativeness, and (3) relative import correctness. They happen to appear in the same PR but address different concerns. The first is about doc structure, the second about example fidelity, and the third about code correctness. None represent a repeated pattern that should be durably stored—they are one-off documentation quality issues specific to this PR.
- Avoid parameter destructuring and limit function parameters to 3 maximum. — target=`none`, confidence=0.30
  - These are two distinct coding conventions that happen to appear in the same PR but address different issues: one about destructuring patterns in callbacks, another about function parameter count. They are not genuinely repeated patterns—the first example shows destructuring avoidance in query handlers, while the second is about function signature design. Without evidence of the second pattern being repeated across multiple PRs or files, it appears to be a one-off refactoring suggestion specific to that PR.
- Three unrelated code review comments about caching route matching, workflow path configuration, and CORS headers. — target=`none`, confidence=0.30
  - These are three distinct issues with different underlying concerns: (1) cache eligibility logic using string matching, (2) incorrect workflow path filter configuration, and (3) CORS security in API examples. They happen to appear in the same PR but address fundamentally different problems in different domains. No genuine repeated pattern exists.

---
