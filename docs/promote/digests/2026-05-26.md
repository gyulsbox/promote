# Memory Promotion Digest — trpc/trpc
> Generated on 2026-05-26 · promote v0.7.1

## Configuration

- Provider: `anthropic` (LLM-direct (no embedding API, llmRefine inactive))
- Models: `claude-haiku-4-5` (classify), `claude-haiku-4-5` (draft)
- Output language: `en`
- Thresholds: similarity `0.8`, min confidence `0.75`, min occurrences `2`
- Window: `90` days (overridden via --since; config default is 60)
- Privacy: redactSecrets=`true`, sendDiffHunksToLLM=`false`

## Summary

- Scanned: **430** review comments across **65** PRs
- AI reviewer comments: **368**
- Clusters found: **152**
- Repeated clusters: **77**
- Promotion candidates: **18**
- Estimated cost: $0.4316
- Timings: fetch 6.1s · normalize 25ms · cluster 5m 12s · conv-fetch 6.8s · reply-ctx 9.7s · memory 1.2s · classify+draft 2m 15s · **total 7m 54s**

---

## 1. Test files must import all Vitest functions (beforeAll, describe, expect, it) that are used to avoid runtime errors.

- **ID**: `candidate_014`
- **Target**: `test` → `(test file)`
- **Confidence**: 0.95
- **Scope**: 🌐 cross-PR (4 PRs)
- **Occurrences**: 6 comments

### Evidence

- PR #7283 `packages/openapi/test/cyclicTypes.test.ts` — "⚠️ Potential issue | 🔴 Critical

Missing `beforeAll` import.

The `beforeAll` f..." [link](https://github.com/trpc/trpc/pull/7283#discussion_r2977503544)
- PR #7252 `packages/client/skills/links/references/link-options.md` — "⚠️ Potential issue | 🟡 Minor

Missing `httpSubscriptionLink` import in the `spl..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937228658)
- PR #7231 `packages/openapi/test/edgeCases.test.ts` — "`beforeAll` is used but not imported from Vitest, which will cause the test file..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2935826088)
- PR #7231 `packages/openapi/test/edgeCases.test.ts` — "`beforeAll` is used but not imported from Vitest, which will cause the test file..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2935826082)
- PR #7283 `packages/openapi/test/cyclicTypes.test.ts` — "`beforeAll` is used but not imported from vitest, so this test file will throw a..." [link](https://github.com/trpc/trpc/pull/7283#discussion_r2977503215)
- ... and 1 more

### Suggested patch

```markdown
# Test Import Validation Invariant

## Test File Location
`tests/invariants/vitest-imports.test.ts`

## What the Test Should Assert

The test should verify that all Vitest utility functions used within a test file are explicitly imported from the `vitest` package. Specifically:
- Scan test files for usage of Vitest functions (`describe`, `it`, `expect`, `beforeAll`, `beforeEach`, `afterAll`, `afterEach`, `test`, `vi`)
- Verify that each used function appears in the import statement from `vitest`
- Fail if any Vitest function is used without being imported
- Report the line number and function name for each missing import

## Code Sketch

```typescript
import { describe, it, expect } from 'vitest';
import fs from 'fs';
import path from 'path';

describe('Vitest Import Validation', () => {
  it('should ensure all used Vitest functions are imported', () => {
    const testFilesPattern = '**/*.test.ts';
    const vitestFunctions = [
      'describe', 'it', 'test', 'expect', 
      'beforeAll', 'beforeEach', 'afterAll', 'afterEach', 'vi'
    ];
    
    // Scan test files
    // Extract import statements
    // Extract function usages
    // Assert: usages ⊆ imports
    
    expect(missingImports).toEqual([]);
  });
});
```
```

### Why this target

This is a testable runtime invariant: test files that use Vitest functions without importing them will fail at runtime. This is a mechanical check that should be enforced across all test files in the repository. The pattern appears consistently across multiple test files (cyclicTypes.test.ts, edgeCases.test.ts, next-app-dir.test.ts) and represents a class of errors that can be prevented with proper linting or test setup validation.

### Alternatives considered

- `agents`: Could be documented as a coding convention for test files, but the runtime failure aspect makes it more of a testable invariant than a style guide.
- `path_scoped_rule`: Could apply specifically to test files (*.test.ts), but the issue is fundamental to how Vitest works rather than path-specific.

### Actions

```bash
promote candidate_014 --target test
promote ignore candidate_014 --reason "..."
promote snooze candidate_014 --days 30
```

---

## 2. Multiple packages declare CLI entrypoints in `bin/` but fail to include `bin/` in their `files` allowlist, causing broken binaries on npm publish.

- **ID**: `candidate_018`
- **Target**: `path_scoped_rule` → `AGENTS.md or a new PACKAGING.md`
- **Confidence**: 0.95
- **Scope**: 🌐 cross-PR (3 PRs)
- **Occurrences**: 8 comments
- **Path scope**: `packages/*/package.json`

### Evidence

- PR #7252 `packages/next/package.json` — "⚠️ Potential issue | 🔴 Critical

Critical: Broken CLI entrypoint across 4 packa..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937228660)
- PR #7252 `packages/server/package.json` — "The package defines a `bin` entry (`./bin/intent.js`), but `bin/` is not include..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968125965)
- PR #7252 `packages/tanstack-react-query/package.json` — "The package exports an `intent` binary but `bin/` is not included in the `files`..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968125922)
- PR #7252 `packages/next/package.json` — "The package declares `"bin": { "intent": "./bin/intent.js" }` but `bin/` is not ..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968125893)
- PR #7252 `packages/client/package.json` — "The package now exposes an `intent` binary, but `bin/` is not part of the `files..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968125972)
- ... and 3 more

### Suggested patch

```markdown
---
applyTo: "packages/*/package.json"
---
# CLI Entrypoint Publishing

- If `bin` field is declared, ensure `bin/` or `bin` is included in the `files` allowlist to prevent broken binaries on npm publish
- Verify `files` array explicitly lists `bin` directory when package exports CLI commands
- Test published tarball locally with `npm pack` to confirm binary files are included before merging
- Add both `bin` entry and corresponding `files` allowlist in the same commit to prevent drift
- Review all packages with `"bin"` keys to audit existing `files` configurations for this pattern
```

### Why this target

This is a repeated structural issue across 4 specific packages (next, client, server, tanstack-react-query) in the same monorepo. The pattern is: declared bin entry + missing files allowlist = broken published binary. This is a path-scoped rule that should apply to all packages/* that declare bin entries, ensuring they include bin/ in files. It's not a one-off PR issue—it's a systemic packaging misconfiguration that will recur unless documented.

### Alternatives considered

- `adr`: Could document the architectural decision about why bin entries should be included in files, but the issue is primarily mechanical/structural rather than about design rationale.
- `test`: Could be enforced as a test that validates: if package.json declares bin entries, files array must include bin/. However, this is more of a linting rule than a runtime invariant.

### Actions

```bash
promote candidate_018 --target path_scoped_rule
promote ignore candidate_018 --reason "..."
promote snooze candidate_018 --days 30
```

---

## 3. Test files should use the standard `testServerAndClientResource` pattern instead of manually managing server lifecycle and client setup.

- **ID**: `candidate_031`
- **Target**: `path_scoped_rule` → `packages/tests/README.md or a testing guidelines document`
- **Confidence**: 0.85
- **Scope**: 🌐 cross-PR (2 PRs)
- **Occurrences**: 2 comments
- **Path scope**: `**/*.test.ts`

### Evidence

- PR #7304 `packages/tests/server/httpSubscriptionLink.fetch.test.ts` — "🛠️ Refactor suggestion | 🟠 Major

Use the standard `testServerAndClientResourc..." [link](https://github.com/trpc/trpc/pull/7304#discussion_r3012813221)
- PR #7231 `packages/openapi/test/generate.test.ts` — "🛠️ Refactor suggestion | 🟠 Major

Use `testServerAndClientResource` for these ..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902034597)

### Suggested patch

```markdown
---
applyTo: "**/*.test.ts"
---
# Test Server and Client Setup

- Use the standard `testServerAndClientResource` pattern instead of manually managing server lifecycle
- Wrap the helper result in `konn()` to ensure proper resource cleanup and isolation between tests
- Avoid hand-rolling server startup/shutdown logic; delegate to the standardized pattern
- Do not mutate the generated client singleton directly; use the pattern's provided client instance
- Apply this pattern to all integration tests that require both server and client setup
```

### Why this target

This is a repeated pattern specific to test files (*.test.ts) across multiple packages. The comments consistently recommend replacing manual server/client setup with a standardized helper pattern. This is a path-scoped rule because it applies specifically to test files and involves a testing convention, not a general coding practice.

### Alternatives considered

- `agents`: Could be promoted to general AI instructions if this pattern should apply to all test writing across the repo
- `test`: Could be framed as a testable invariant that integration tests must use the standard pattern

### Actions

```bash
promote candidate_031 --target path_scoped_rule
promote ignore candidate_031 --reason "..."
promote snooze candidate_031 --days 30
```

---

## 4. Avoid non-null assertions (!) in TypeScript code; use explicit type guards or optional chaining instead.

- **ID**: `candidate_035`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.85
- **Scope**: 🌐 cross-PR (2 PRs)
- **Occurrences**: 2 comments

### Evidence

- PR #7176 `packages/client/src/createTRPCClient.ts` — "⚠️ Potential issue | 🟡 Minor

Remove the non-null assertion and unnecessary `an..." [link](https://github.com/trpc/trpc/pull/7176#discussion_r2876202463)
- PR #7195 `www/scripts/check-twoslash.ts` — "⚠️ Potential issue | 🟡 Minor

Remove non-null assertions in this file to mainta..." [link](https://github.com/trpc/trpc/pull/7195#discussion_r2869717508)

### Suggested patch

```markdown
## TypeScript Non-Null Assertions

- **Avoid non-null assertions (`!`)** in all TypeScript code; they bypass type safety and hide potential runtime errors
- **Use explicit type guards** instead: check with `if (value !== null && value !== undefined)` or `if (value)` before accessing properties
- **Use optional chaining (`?.`)** for safe property access on potentially null/undefined values (e.g., `obj?.property?.method()`)
- **Use nullish coalescing (`??`)** to provide default values instead of asserting non-null (e.g., `value ?? defaultValue`)
- **Apply to all files** including utility functions, client code, and array operations (e.g., replace `array.pop()!` with explicit length checks)
```

### Why this target

This is a repeated cross-cutting coding convention appearing in multiple files and PRs. The pattern reflects a deliberate choice to enforce strict TypeScript safety practices repo-wide, not a one-off issue. Future code reviews will benefit from having this guideline documented in agent instructions.

### Alternatives considered

- `path_scoped_rule`: Could be scoped to packages/client and www/scripts, but the pattern is general TypeScript practice applicable everywhere
- `test`: Could be enforced via a linter rule (e.g., @typescript-eslint/no-non-null-assertion), but the comments focus on the guideline itself rather than a testable invariant

### Actions

```bash
promote candidate_035 --target agents
promote ignore candidate_035 --reason "..."
promote snooze candidate_035 --days 30
```

---

## 5. ESLint rules and suppressions must be migrated to Oxlint equivalents before Oxlint is enabled in a package.

- **ID**: `candidate_037`
- **Target**: `path_scoped_rule` → `LINTING.md or OXLINT_MIGRATION.md`
- **Confidence**: 0.75
- **Scope**: 🌐 cross-PR (2 PRs)
- **Occurrences**: 2 comments
- **Path scope**: `packages/**/package.json`

### Evidence

- PR #7205 `packages/tanstack-react-query/package.json` — "⚠️ Potential issue | 🟠 Major

Migrate missing lint rules to Oxlint before enabl..." [link](https://github.com/trpc/trpc/pull/7205#discussion_r2877475262)
- PR #7206 `packages/react-query/src/internals/context.tsx` — "⚠️ Potential issue | 🟡 Minor

Remove or migrate the ESLint-specific suppression..." [link](https://github.com/trpc/trpc/pull/7206#discussion_r2877465841)

### Suggested patch

```markdown
---
applyTo: "packages/**/package.json"
---
# ESLint to Oxlint Migration

- Migrate all active ESLint rules from `eslintConfig` to `.oxlintrc.json` before enabling Oxlint in lint scripts
- Replace `eslint-disable-next-line` and `eslint-disable` comments with Oxlint equivalents (`// oxlint-disable-next-line`) to prevent stale suppressions
- Verify rule parity between ESLint and Oxlint configurations; document any intentional differences in migration notes
- Remove `eslintConfig` from `package.json` only after confirming all rules are active in Oxlint and tests pass
- Use `.oxlintrc.json` as the single source of truth for linting rules once migration is complete
```

### Why this target

This is a repeated pattern specific to the linting infrastructure transition from ESLint to Oxlint. It applies to packages that declare both `eslintConfig` and use Oxlint in their lint scripts. The pattern is actionable and scoped to packages undergoing this migration, making it a path-scoped rule rather than a general agent instruction.

### Alternatives considered

- `adr`: If the decision to migrate to Oxlint and the rationale for requiring complete migration before enablement is important to document
- `agents`: If this becomes a repo-wide policy that all packages must follow during Oxlint adoption

### Actions

```bash
promote candidate_037 --target path_scoped_rule
promote ignore candidate_037 --reason "..."
promote snooze candidate_037 --days 30
```

---

## 6. Test files should use `await using` pattern with `testServerAndClientResource` directly instead of `run(...)` wrapper, and configure client behavior via callback pattern.

- **ID**: `candidate_038`
- **Target**: `path_scoped_rule` → `packages/tests/README.md or a new TEST_PATTERNS.md`
- **Confidence**: 0.85
- **Scope**: 🌐 cross-PR (2 PRs)
- **Occurrences**: 2 comments
- **Path scope**: `**/*.test.ts`

### Evidence

- PR #7207 `packages/tests/server/httpSubscriptionLink.test.ts` — "🛠️ Refactor suggestion | 🟠 Major

Use `testServerAndClientResource` directly w..." [link](https://github.com/trpc/trpc/pull/7207#discussion_r2879708223)
- PR #7262 `packages/tests/server/regression/batchStreamErrorCallIndex.test.ts` — "⚠️ Potential issue | 🟡 Minor

Use the `client` callback pattern for consistency..." [link](https://github.com/trpc/trpc/pull/7262#discussion_r2948708218)

### Suggested patch

```markdown
---
applyTo: "**/*.test.ts"
---
# Test Server and Client Initialization

- Use `await using` pattern with `testServerAndClientResource` directly instead of wrapping with `run(...)`
- Initialize server and client fixtures as: `const { client, server } = await using(new testServerAndClientResource(), ...)`
- Configure client behavior through callback pattern rather than direct property assignment
- Pass client configuration via callback: `testServerAndClientResource((client) => ({ clientLink: 'httpBatchStreamLink' }))`
- Ensure all test fixtures follow the `await using` resource disposal pattern for proper cleanup
```

### Why this target

This is a repeated pattern specific to test files (*.test.ts) in the packages/tests directory. The comments address both a preferred initialization pattern (`await using` over `run()`) and a consistent configuration approach (callback pattern). These are conventions that apply across multiple test files in the same domain.

### Alternatives considered

- `agents`: Could be promoted to general AI instructions if this pattern applies beyond just the test suite to all test files repo-wide
- `none`: If these are one-off refactoring suggestions specific to those two PRs rather than a durable pattern

### Actions

```bash
promote candidate_038 --target path_scoped_rule
promote ignore candidate_038 --reason "..."
promote snooze candidate_038 --days 30
```

---

## 7. @types/node major version must align with engines.node specification across package.json files.

- **ID**: `candidate_041`
- **Target**: `path_scoped_rule` → `DEPENDENCY_RULES.md or similar`
- **Confidence**: 0.85
- **Scope**: 🌐 cross-PR (2 PRs)
- **Occurrences**: 2 comments
- **Path scope**: `**/package.json`

### Evidence

- PR #7210 `package.json` — "⚠️ Potential issue | 🟠 Major

Downgrade `@types/node` to `^24.x` or update `eng..." [link](https://github.com/trpc/trpc/pull/7210#discussion_r2884107893)
- PR #7200 `package.json` — "⚠️ Potential issue | 🟠 Major

Update `@types/node` to align with Node 24 runtim..." [link](https://github.com/trpc/trpc/pull/7200#discussion_r2872399545)

### Suggested patch

```markdown
---
applyTo: "**/package.json"
---
# @types/node Version Alignment

- Ensure `@types/node` major version matches the Node.js major version specified in `engines.node`
- If `engines.node` is `^24.0.0`, use `@types/node: ^24.x.x`
- If `engines.node` is `^25.0.0`, use `@types/node: ^25.x.x`
- Update both fields together when changing Node.js runtime requirements
- Verify alignment across all package.json files in the monorepo to maintain consistency
```

### Why this target

This is a repeated pattern specific to dependency management in package.json files. The rule applies consistently: @types/node's major version should match the Node.js major version specified in engines.node. This is a structural constraint that applies to all package.json files in the monorepo and should be enforced as a path-scoped rule rather than a general agent instruction, since it's specifically about package.json configuration.

### Alternatives considered

- `agents`: Could be a repo-wide convention if this applies to all packages, but it's more precisely a package.json-specific constraint
- `adr`: Could document the architectural decision to keep types and runtime in sync, but the pattern is primarily a structural rule rather than a rationale

### Actions

```bash
promote candidate_041 --target path_scoped_rule
promote ignore candidate_041 --reason "..."
promote snooze candidate_041 --days 30
```

---

## 8. Avoid destructuring directly in function parameter declarations; use property access instead.

- **ID**: `candidate_049`
- **Target**: `agents` → `AGENTS.md`
- **Confidence**: 0.85
- **Scope**: 🌐 cross-PR (2 PRs)
- **Occurrences**: 2 comments

### Evidence

- PR #7283 `packages/openapi/test/types.ts` — "🛠️ Refactor suggestion | 🟠 Major

Avoid destructuring directly in function par..." [link](https://github.com/trpc/trpc/pull/7283#discussion_r2978047825)
- PR #7199 `packages/tests/server/transformer.test.ts` — "⚠️ Potential issue | 🟡 Minor

Use direct property access instead of parameter d..." [link](https://github.com/trpc/trpc/pull/7199#discussion_r2871680407)

### Suggested patch

```markdown
## Parameter Destructuring

- Do not destructure objects directly in function parameter declarations; use property access on the parameter object instead
- Apply this rule to all function types: callbacks, event handlers, utility functions, and API methods
- Instead of `function handler({ prop1, prop2 })`, write `function handler(obj) { const prop1 = obj.prop1; }`
- This applies across all packages and test files in the repository
- Exception: destructuring in function bodies is acceptable; only parameter-level destructuring is prohibited
```

### Why this target

This is a repeated cross-cutting coding convention that appears in multiple packages and test files. The pattern is consistent enough to warrant documentation as a repo-wide AI instruction to prevent future violations across the codebase.

### Alternatives considered

- `path_scoped_rule`: Could be scoped to test files (packages/tests/**, packages/*/test/**) if the rule is only enforced in test code
- `none`: If this is already documented in existing style guides or linters, promotion may be redundant

### Actions

```bash
promote candidate_049 --target agents
promote ignore candidate_049 --reason "..."
promote snooze candidate_049 --days 30
```

---

## 9. Directory-read error handling in www/scripts/check-twoslash.ts should only suppress ENOENT errors, not all filesystem errors.

- **ID**: `candidate_058`
- **Target**: `path_scoped_rule` → `www/scripts/README.md or a path-scoped rule file`
- **Confidence**: 0.85
- **Scope**: 🌐 cross-PR (2 PRs)
- **Occurrences**: 2 comments
- **Path scope**: `www/scripts/check-twoslash.ts`

### Evidence

- PR #7195 `www/scripts/check-twoslash.ts` — "⚠️ Potential issue | 🟠 Major

Do not swallow all directory-read errors.

Line 1..." [link](https://github.com/trpc/trpc/pull/7195#discussion_r2869693505)
- PR #7196 `www/scripts/check-twoslash.ts` — "⚠️ Potential issue | 🟠 Major

Narrow directory-scan error handling to ENOENT on..." [link](https://github.com/trpc/trpc/pull/7196#discussion_r2869759041)

### Suggested patch

```markdown
---
applyTo: "www/scripts/check-twoslash.ts"
---
# Directory Read Error Handling

- Only suppress `ENOENT` errors when reading directories; let all other filesystem errors propagate
- Use `error.code === 'ENOENT'` checks instead of catching all errors to avoid masking permission or I/O failures
- Distinguish between "file not found" (acceptable skip) and "permission denied" or "I/O error" (should fail loudly)
- Test error handling paths to ensure real failures are not silently ignored during directory scans
- Document why specific errors are suppressed to prevent future overly-broad error handling
```

### Why this target

This is a specific, repeated pattern in a single file (www/scripts/check-twoslash.ts) where overly broad error suppression masks real failures. The pattern appears twice in consecutive PRs affecting the same code path, indicating a genuine issue that needs to be enforced for this particular script. It's not a cross-cutting convention but rather a specific rule for this directory-scanning utility.

### Alternatives considered

- `test`: Could be enforced as a runtime test that verifies permission/IO errors are not silently swallowed
- `adr`: If there's architectural reasoning about why this script specifically needs strict error handling, that context could be documented

### Actions

```bash
promote candidate_058 --target path_scoped_rule
promote ignore candidate_058 --reason "..."
promote snooze candidate_058 --days 30
```

---

## 10. Multiple package.json files incorrectly reference non-existent ./bin/intent.js entry points, and workflow paths reference incorrect documentation directory structure.

- **ID**: `candidate_060`
- **Target**: `path_scoped_rule` → `AGENTS.md or a new MONOREPO_CONVENTIONS.md`
- **Confidence**: 0.85
- **Scope**: 🌐 cross-PR (2 PRs)
- **Occurrences**: 6 comments
- **Path scope**: `packages/*/package.json`

### Evidence

- PR #7252 `packages/client/package.json` — "`bin.intent` points to `./bin/intent.js`, but there is no `packages/client/bin/`..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937362621)
- PR #7252 `packages/openapi/package.json` — "`bin.intent` points to `./bin/intent.js`, but there is no `packages/openapi/bin/..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937362632)
- PR #7252 `packages/tanstack-react-query/package.json` — "`bin.intent` points to `./bin/intent.js`, but `packages/tanstack-react-query/bin..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937362635)
- PR #7252 `packages/next/package.json` — "`bin.intent` points to `./bin/intent.js`, but there is no `packages/next/bin/` d..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937362630)
- PR #7252 `.github/workflows/notify-intent.yml` — "`paths` includes `packages/www/docs/`, but this repo’s docs live under `www/docs..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968259257)
- ... and 1 more

### Suggested patch

```markdown
---
applyTo: "packages/*/package.json"
---
# Binary Entry Points Validation

- Remove or correct all `bin` field entries that reference non-existent files; verify each referenced script exists at the exact path before committing
- Do not add `bin.intent` entries unless `./bin/intent.js` is present in the package directory and properly implements the intended CLI functionality
- Run `npm pack` locally to simulate installation and verify all bin shims are created correctly without errors
- Document any legitimate bin entry points in the package's README with usage examples and ensure they are tested in CI
- Use `npm ls` and `npm exec` to validate bin references work as expected before opening a pull request
```

### Why this target

This is a systematic issue affecting multiple packages in the monorepo (client, openapi, tanstack-react-query, next) plus a workflow configuration. The pattern is specific to the monorepo's package structure and bin/ directory conventions. It's not a one-off issue but a repeated misconfiguration across multiple package.json files that needs to be caught and prevented. This is best captured as a path-scoped rule for packages/* that validates bin entry points exist before publishing.

### Alternatives considered

- `adr`: Could document the decision to use intent shims and why they should exist, but the core issue is a missing file, not a design decision.
- `test`: Could create a test that validates all bin entry points in package.json files actually exist, but this is more of a linting/validation concern than a runtime invariant.

### Actions

```bash
promote candidate_060 --target path_scoped_rule
promote ignore candidate_060 --reason "..."
promote snooze candidate_060 --days 30
```

---

## 11. The `maxBatchSize` parameter must be validated as a finite positive integer to prevent runtime bugs from NaN, Infinity, and invalid values.

- **ID**: `candidate_061`
- **Target**: `path_scoped_rule` → `VALIDATION_RULES.md`
- **Confidence**: 0.85
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/server/src/unstable-core-do-not-import/http/**`

### Evidence

- PR #7191 `packages/server/src/unstable-core-do-not-import/http/types.ts` — "`maxBatchSize` accepts any `number` at runtime (including `NaN`, `Infinity`, neg..." [link](https://github.com/trpc/trpc/pull/7191#discussion_r2867601942)
- PR #7191 `packages/server/src/unstable-core-do-not-import/http/contentType.ts` — "`typeof maxBatchSize === 'number'` will treat `NaN`/`Infinity` as valid, but the..." [link](https://github.com/trpc/trpc/pull/7191#discussion_r2974473943)

### Suggested patch

```markdown
---
applyTo: "packages/server/src/unstable-core-do-not-import/http/**"
---
# maxBatchSize Parameter Validation

- Validate `maxBatchSize` as a finite positive integer using `Number.isFinite(maxBatchSize) && maxBatchSize > 0 && Number.isInteger(maxBatchSize)`
- Reject `NaN`, `Infinity`, negative numbers, zero, and non-integer values with a descriptive error message
- Avoid relying solely on `typeof maxBatchSize === 'number'` as it incorrectly accepts `NaN` and `Infinity`
- Test that invalid values (`NaN`, `Infinity`, -1, 0, 1.5) throw or are rejected before runtime comparisons
- Place validation at the entry point of functions accepting `maxBatchSize` to prevent downstream limit-enforcement failures
```

### Why this target

This is a specific validation rule for the `maxBatchSize` parameter that appears in multiple files within the http package. It's not a general coding convention but rather a domain-specific validation requirement for this particular parameter across the http module. The pattern shows the same underlying issue (insufficient input validation) in related files, making it suitable for a path-scoped rule.

### Alternatives considered

- `test`: This could be enforced as a runtime test invariant to ensure maxBatchSize validation is mechanically checked
- `agents`: Could be promoted as a general AI instruction about validating numeric configuration parameters
- `adr`: Could document the architectural decision to validate numeric limits rather than rely on runtime comparison behavior

### Actions

```bash
promote candidate_061 --target path_scoped_rule
promote ignore candidate_061 --reason "..."
promote snooze candidate_061 --days 30
```

---

## 12. Server-sent events retry logic should not consume retry budget on initial connection and should exclude AbortError from retryable conditions.

- **ID**: `candidate_062`
- **Target**: `path_scoped_rule` → `packages/openapi/test/routers/RETRY_LOGIC.md`
- **Confidence**: 0.85
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/openapi/**/serverSentEvents.gen.ts`

### Evidence

- PR #7231 `packages/openapi/test/routers/errorFormatterRouter-heyapi/core/serverSentEvents.gen.ts` — "⚠️ Potential issue | 🟠 Major

Don't spend the retry budget on the initial conne..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128592)
- PR #7231 `packages/openapi/test/routers/appRouter-heyapi/core/serverSentEvents.gen.ts` — "⚠️ Potential issue | 🟠 Major

Don't spend the retry budget on the initial conne..." [link](https://github.com/trpc/trpc/pull/7231#discussion_r2902128577)

### Suggested patch

```markdown
---
applyTo: "packages/openapi/**/serverSentEvents.gen.ts"
---
# Server-Sent Events Retry Logic

- Initialize `attempt` counter to 0, not 1, so the first connection attempt does not consume retry budget
- Exclude `AbortError` from retryable error conditions; treat aborts as intentional cancellations, not failures
- Increment `attempt` only after a failed request, not before the initial connection
- Validate that `sseMaxRetryAttempts: 1` allows exactly one retry after the initial attempt, not zero
- Use error type checking to distinguish between network failures (retryable) and user-initiated aborts (non-retryable)
```

### Why this target

This is a specific bug pattern in SSE retry handling that appears in multiple generated serverSentEvents files. It's scoped to the SSE implementation path and describes a concrete behavioral rule about when retries should be attempted. The pattern is repeated across similar files in the same domain (errorFormatterRouter and appRouter variants), indicating a systematic issue in how retry logic is implemented.

### Alternatives considered

- `test`: This could be enforced as a test invariant: verify that AbortError does not trigger retries and that sseMaxRetryAttempts correctly reflects actual retry count.
- `adr`: If there is architectural reasoning about why initial connects should not consume budget or why aborts are special, that rationale could be documented.

### Actions

```bash
promote candidate_062 --target path_scoped_rule
promote ignore candidate_062 --reason "..."
promote snooze candidate_062 --days 30
```

---

## 13. GitHub Actions workflows using `lerna publish` must either grant `permissions.contents: write` or explicitly use `--no-push` and `--no-git-tag-version` flags to avoid permission failures.

- **ID**: `candidate_064`
- **Target**: `path_scoped_rule` → `.github/workflows/README.md or a new WORKFLOWS.md`
- **Confidence**: 0.85
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `.github/workflows/**/*.yml`

### Evidence

- PR #7247 `.github/workflows/release-tmp.yml` — "Job now explicitly sets `permissions.contents: read`, but `lerna publish` typica..." [link](https://github.com/trpc/trpc/pull/7247#discussion_r2936535934)
- PR #7247 `.github/workflows/release-canary.yml` — "Job now explicitly sets `permissions.contents: read`, but `lerna publish` typica..." [link](https://github.com/trpc/trpc/pull/7247#discussion_r2936535922)

### Suggested patch

```markdown
---
applyTo: ".github/workflows/**/*.yml"
---
# Lerna Publish Permission Requirements

- Grant `permissions.contents: write` if `lerna publish` performs git operations (tagging, pushing), or explicitly use `--no-push` and `--no-git-tag-version` flags
- Verify that `lerna publish` commands do not attempt git writes when `permissions.contents: read` is set
- Use `--no-push` flag to prevent automatic git pushes during publish operations
- Use `--no-git-tag-version` flag to prevent automatic git tag creation during publish operations
- Document which permission level is required for each publish job to prevent silent failures
```

### Why this target

This is a specific constraint that applies to all GitHub Actions workflows in `.github/workflows/` that use Lerna for publishing. The pattern identifies a concrete incompatibility between read-only content permissions and Lerna's default git operations. It's not a general coding convention (agents) but rather a path-scoped rule for workflow files that use this tool.

### Alternatives considered

- `adr`: Could document the architectural decision about why these workflows need specific permission levels and when git operations are necessary
- `test`: Could be enforced as a CI check that validates workflow files don't have this permission/lerna mismatch

### Actions

```bash
promote candidate_064 --target path_scoped_rule
promote ignore candidate_064 --reason "..."
promote snooze candidate_064 --days 30
```

---

## 14. Multiple @trpc/* packages expose a generic `intent` binary that causes node_modules/.bin collisions and shadows @tanstack/intent; use namespaced binary names instead.

- **ID**: `candidate_065`
- **Target**: `path_scoped_rule` → `AGENTS.md or a path-scoped rule document`
- **Confidence**: 0.85
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 5 comments
- **Path scope**: `packages/*/package.json`

### Evidence

- PR #7252 `packages/server/package.json` — "Exposing a generic `intent` binary from this package is likely to cause `node_mo..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968259331)
- PR #7252 `packages/server/package.json` — "`bin.intent` points to `./bin/intent.js`, but `packages/server/bin/` doesn't exi..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937362645)
- PR #7252 `packages/server/package.json` — "`bin.intent` references `./bin/intent.js`, but there’s no `bin/intent.js` in thi..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937220221)
- PR #7252 `packages/client/package.json` — "Exposing a generic `intent` binary from this package is likely to cause `node_mo..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968259335)
- PR #7252 `packages/next/package.json` — "Exposing a generic `intent` binary from this package is likely to cause `node_mo..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968259301)

### Suggested patch

```markdown
---
applyTo: "packages/*/package.json"
---
# Binary Entry Point Naming and Implementation

- Use namespaced binary names in the `bin` field to avoid collisions (e.g., `@trpc/server:intent` or `trpc-server-intent` instead of generic `intent`)
- Ensure all referenced bin files exist in the package before publishing; verify `bin/*.js` files are present in the PR
- Do not expose generic binary names that could shadow other packages' CLIs or cause `node_modules/.bin` collisions across multiple `@trpc/*` installations
- If a bin entry is added, include the actual executable file in the same PR; broken bin references will cause installation failures
- Reference the actual file path correctly in `bin` field and confirm the file exists in the package directory structure
```

### Why this target

This is a specific, repeated issue affecting multiple package.json files in the packages/* directory. The pattern is consistent across @trpc/server, @trpc/client, and @trpc/next packages, making it a path-scoped rule for the packages/* directory. The issue involves both missing bin files and naming collisions that need to be prevented across this specific path scope.

### Alternatives considered

- `adr`: The architectural decision to avoid generic binary names in favor of namespaced ones could be documented as a design rationale
- `agents`: Could be promoted to general guidance if this pattern applies beyond just @trpc packages

### Actions

```bash
promote candidate_065 --target path_scoped_rule
promote ignore candidate_065 --reason "..."
promote snooze candidate_065 --days 30
```

---

## 15. Prerelease version handling in scripts/version.ts must preserve full prerelease identifiers (e.g., alpha.1) and respect ALPHA_PACKAGES allowlist to prevent unintended stable releases.

- **ID**: `candidate_066`
- **Target**: `path_scoped_rule` → `scripts/version.ts or a path-scoped rule document`
- **Confidence**: 0.75
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `scripts/version.ts`

### Evidence

- PR #7244 `scripts/version.ts` — "If this script needs to enforce that certain packages stay prerelease (as the re..." [link](https://github.com/trpc/trpc/pull/7244#discussion_r2936375683)
- PR #7244 `scripts/version.ts` — "⚠️ Potential issue | 🟠 Major

Preserve the full prerelease identifier, not just..." [link](https://github.com/trpc/trpc/pull/7244#discussion_r2936377189)

### Suggested patch

```markdown
---
applyTo: "scripts/version.ts"
---
# Prerelease Version Handling

- Preserve full prerelease identifiers (e.g., `alpha.1`, `beta.2`) in version strings; do not truncate to prefix-only patterns
- Maintain the `ALPHA_PACKAGES` allowlist to enforce which packages remain prerelease; removing it changes publish semantics unintentionally
- Use regex patterns that capture the complete prerelease suffix including numeric qualifiers (e.g., `/^(\d+\.\d+\.\d+)-(alpha|beta)\.\d+$/`)
- Validate that packages in `ALPHA_PACKAGES` are never promoted to stable versions during automated version bumps
- Test version transformations with real prerelease identifiers to ensure no truncation occurs in output
```

### Why this target

This is a specific rule about version scripting behavior in scripts/version.ts that affects how prerelease packages are published. It's not a general coding convention (agents), but rather a domain-specific rule for the versioning script. The pattern involves both preserving prerelease suffixes and maintaining an allowlist for packages that should stay prerelease—both critical to publish semantics.

### Alternatives considered

- `adr`: If the architectural decision about why certain packages must stay prerelease (e.g., @trpc/openapi stability guarantees) is more important than the implementation rule itself
- `test`: If version progression for prerelease packages should be enforced as a testable invariant in CI

### Actions

```bash
promote candidate_066 --target path_scoped_rule
promote ignore candidate_066 --reason "..."
promote snooze candidate_066 --days 30
```

---

## 16. Multiple packages incorrectly declare `bin.intent` entries pointing to non-existent `./bin/intent.js` files, breaking CLI executables in published packages.

- **ID**: `candidate_070`
- **Target**: `path_scoped_rule` → `AGENTS.md or a new PACKAGE_CONVENTIONS.md`
- **Confidence**: 0.85
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 10 comments
- **Path scope**: `packages/*/package.json`

### Evidence

- PR #7252 `packages/server/package.json` — "⚠️ Potential issue | 🟠 Major

`bin.intent` references non-existent files across..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937228665)
- PR #7252 `packages/client/package.json` — "`bin.intent` points to `./bin/intent.js`, but there is no `bin/intent.js` (or ev..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937220200)
- PR #7252 `packages/tanstack-react-query/package.json` — "This package declares a `bin` entry `intent: ./bin/intent.js`, but there is no `..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937220224)
- PR #7252 `packages/client/package.json` — "⚠️ Potential issue | 🟠 Major

Client manifest declares non-existent `bin` entry..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2937228654)
- PR #7252 `packages/next/package.json` — "This package publishes a `bin` entry that requires `@tanstack/intent` to be inst..." [link](https://github.com/trpc/trpc/pull/7252#discussion_r2968259347)
- ... and 5 more

### Suggested patch

```markdown
---
applyTo: "packages/*/package.json"
---
# Binary Entry Point Validation

- Verify all `bin` entries reference files that exist in the package directory
- Ensure referenced bin scripts are included in the `files` array for npm publishing
- Remove `bin` entries for non-existent scripts (e.g., `./bin/intent.js`) to prevent broken CLI executables
- Confirm bin script files have proper shebang (`#!/usr/bin/env node`) and executable permissions
- Test published package locally with `npm pack` to validate all declared bin entries are accessible
```

### Why this target

This is a repeated pattern across multiple package.json files in different packages (server, client, next, tanstack-react-query) within the same PR. It's a structural/configuration issue specific to how bin entries are declared in package.json files across the packages/ directory. A path-scoped rule would prevent future occurrences by enforcing that bin entries must reference files that actually exist and are included in the package's files array.

### Alternatives considered

- `adr`: If the decision to have a shared CLI entry across multiple packages has architectural significance, an ADR explaining the intended design would be valuable.
- `test`: A test could verify that all bin entries in package.json files reference files that exist and are included in the files array.

### Actions

```bash
promote candidate_070 --target path_scoped_rule
promote ignore candidate_070 --reason "..."
promote snooze candidate_070 --days 30
```

---

## 17. Test helpers must avoid temporal dead zone errors by declaring subscription variables before subscribe() calls to handle synchronous onError callbacks safely.

- **ID**: `candidate_071`
- **Target**: `test` → `TEST_PATTERNS.md`
- **Confidence**: 0.85
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 3 comments

### Evidence

- PR #7279 `packages/tests/server/websockets.test.ts` — "This helper can throw a ReferenceError if the subscription calls `onError` synch..." [link](https://github.com/trpc/trpc/pull/7279#discussion_r2970571732)
- PR #7279 `packages/tests/server/httpSubscriptionLink.test.ts` — "This helper can throw a ReferenceError if `subscribe(...)` invokes `onError` syn..." [link](https://github.com/trpc/trpc/pull/7279#discussion_r2970571739)
- PR #7279 `packages/tests/server/websockets.test.ts` — "⚠️ Potential issue | 🟡 Minor

Avoid capturing `subscription` before it exists.
..." [link](https://github.com/trpc/trpc/pull/7279#discussion_r2970588775)

### Suggested patch

```markdown
# Test Helper Temporal Dead Zone Prevention

## Test File Location
`tests/helpers/subscription-helpers.test.ts`

## What the Test Should Assert

The test should verify that subscription test helpers safely handle synchronous `onError` callbacks without triggering ReferenceError due to temporal dead zone violations. Specifically:

1. When a source observable calls `onError` synchronously during the `subscribe()` call, the error callback should execute without referencing uninitialized variables
2. The subscription variable must be declared (with `let`) before the `subscribe()` call, ensuring it exists in scope even if the callback executes immediately
3. Error handling should complete successfully regardless of whether the error occurs synchronously or asynchronously

## Code Sketch

```typescript
describe('Subscription helpers - temporal dead zone safety', () => {
  it('should handle synchronous onError without ReferenceError', () => {
    const source = new Observable(subscriber => {
      subscriber.error(new Error('sync error'));
    });

    let errorCaught: Error | null = null;
    let subscription: Subscription; // Declared BEFORE subscribe()
    
    subscription = source.subscribe({
      next: () => {},
      error: (err) => {
        errorCaught = err;
        // This should not throw ReferenceError
        subscription?.unsubscribe();
      }
    });

    expect(errorCaught).toBeDefined();
    expect(errorCaught?.message).toBe('sync error');
  });

  it('should not reference subscription in TDZ', () => {
    // Verify pattern: let sub; ... sub = source.subscribe(...)
    // NOT: const sub = source.subscribe(...)
    expect(() => {
      let sub: Subscription;
      new Observable(s => s.error(new Error('test'))).subscribe({
        error: () => sub.unsubscribe() // Safe: sub declared before subscribe
      });
    }).not.toThrow(ReferenceError);
  });
});
```
```

### Why this target

This describes a testable runtime invariant specific to subscription test helpers: synchronous error callbacks must not reference uninitialized variables. The pattern appears across multiple test files in the same PR and describes behavior that causes flaky tests when not handled correctly. This is a mechanical invariant that should be enforced in test code.

### Alternatives considered

- `path_scoped_rule`: Could be scoped to packages/tests/** since it only affects test helpers, but the underlying issue (TDZ with async callbacks) is a general JavaScript pattern worth documenting broadly.
- `agents`: Could be a general coding convention, but the issue is specific enough to subscription testing patterns that test-scoped documentation is more appropriate.

### Actions

```bash
promote candidate_071 --target test
promote ignore candidate_071 --reason "..."
promote snooze candidate_071 --days 30
```

---

## 18. TanStack React Query test files must use namespace imports for React (import * as React) rather than named imports.

- **ID**: `candidate_072`
- **Target**: `path_scoped_rule` → `packages/tanstack-react-query/.coderabbit.yaml or CONTRIBUTING.md`
- **Confidence**: 0.85
- **Scope**: 📍 within-PR (1 PR)
- **Occurrences**: 2 comments
- **Path scope**: `packages/tanstack-react-query/**/*.test.tsx`

### Evidence

- PR #7362 `packages/tanstack-react-query/test/client.test.tsx` — "⚠️ Potential issue | 🟠 Major

Use namespace React import in TanStack React Quer..." [link](https://github.com/trpc/trpc/pull/7362#discussion_r3144118971)
- PR #7362 `packages/tanstack-react-query/test/polymorphism.test.tsx` — "⚠️ Potential issue | 🟠 Major

Keep `import  as React` in TanStack React Query t..." [link](https://github.com/trpc/trpc/pull/7362#discussion_r3144118976)

### Human signal

- Agreed: **1** reviewer(s) (@Zelys-DFKH)
  - First agreement: *"Thanks for the review. There's no documented convention in `.coderabbit.yaml` or `CONTRIBUTING.md` requiring namespace i"*

### Suggested patch

```markdown
---
applyTo: "packages/tanstack-react-query/**/*.test.tsx"
---
# React Import Convention

- Use namespace imports for React: `import * as React` instead of named imports
- Do not use destructured imports like `import { useState }` directly from React
- Reference React hooks as `React.useState`, `React.useEffect`, etc.
- This convention applies consistently across all test files in this package
- Update existing named imports to namespace imports when modifying test files
```

### Why this target

This is a consistent pattern specific to the packages/tanstack-react-query/test/ directory. The comments appear in multiple test files within this path and establish a local convention for that package's test suite. It's not a repo-wide convention (agents) but rather a path-scoped requirement for this specific package's tests.

### Alternatives considered

- `agents`: Could be promoted if this namespace import convention applies across all test files repo-wide, but evidence suggests it's specific to TanStack React Query tests
- `none`: If this is merely a style preference without documented rationale, it might not warrant preservation

### Actions

```bash
promote candidate_072 --target path_scoped_rule
promote ignore candidate_072 --reason "..."
promote snooze candidate_072 --days 30
```

---

## Filtered out

Items that were excluded from promotion candidates. Useful for tuning thresholds or for team review of edge cases.

### Not promotable (target=none / invalid) (59)

- Various isolated documentation and code comment improvements across different files — target=`none`, confidence=0.20
  - These are fundamentally different issues: (1) punctuation/grammar in markdown, (2) outdated comment documentation, (3) misleading code snippet presentation, (4) inaccurate log message. They share only surface-level similarity (all are 'fix the text') but address unrelated problems in different domains. None represent a repeated pattern worth preserving as institutional knowledge.
- Miscellaneous documentation and implementation inconsistencies across different server and test files — target=`none`, confidence=0.15
  - These are five distinct, unrelated issues: (1) JSDoc mismatch for serialization types, (2) parameter validation scope issue, (3) slot alias processing order, (4) file path handling in cache patterns, and (5) minor performance optimization. They share no common underlying pattern, convention, or architectural principle. Each is a one-off code quality observation specific to its PR context.
- Various unrelated CI/CD and script logic errors across different files — target=`none`, confidence=0.15
  - These are five distinct, unrelated issues: (1) GitHub Actions branch restriction for canary publishes, (2) JavaScript comma operator bug in cache logic, (3) regex pattern mismatch in Docusaurus fence detection, (4) bash pipefail error handling in grep pipeline, (5) error suppression in skill staleness check. They share only surface-level similarity (all are bugs/issues) but involve completely different systems, languages, and failure modes. No underlying pattern or shared principle connects them.
- Various documentation and configuration improvements across different subsystems — target=`none`, confidence=0.15
  - These five comments address fundamentally different issues: version tag handling logic, MDX parser configuration, documentation accuracy, API default values, and CI messaging. While each is a valid improvement suggestion, they share no underlying pattern—they're scattered across unrelated code paths (versioning, docs tooling, documentation content, server config, CI workflows) with no common principle or convention. This is a false cluster of surface-level similarity (all are 'improve this') masking distinct, one-off concerns.
- Various code quality and safety issues across different files and domains — target=`none`, confidence=0.20
  - These comments address fundamentally different problems: schema validation mismatches, type safety with optional chaining, unsafe type assertions, incomplete documentation fixes, and incorrect async resource handling. While all are legitimate code review concerns, they do not represent a single repeated pattern or convention. Each issue is specific to its context and file type.
- Mixed documentation and configuration issues across unrelated packages — target=`none`, confidence=0.30
  - These five comments address fundamentally different problems: (1) tsdown export naming in React Query, (2) placeholder transformer code in docs, (3) BigInt schema handling in OpenAPI, (4) placeholder CLI text in migration docs, and (5) incomplete install command in Next.js docs. Despite surface similarity in phrasing ("potential issue", "placeholder"), they are one-off issues specific to individual PRs and files, not a repeated pattern. Each requires domain-specific knowledge and fixes.
- Miscellaneous documentation and code quality fixes across unrelated files — target=`none`, confidence=0.20
  - These are one-off, file-specific issues with no underlying pattern. Example 1 and 5 are both about www/docs/client/openapi.md but address different problems (missing words vs. punctuation). Example 2 is a markdown linting issue, Example 3 is phrasing tightening, and Example 4 is a code example bug. They share only surface-level similarity (all minor documentation/copy issues) but target completely different underlying problems in different contexts. None represent a repeatable pattern that would benefit future reviews.
- Mixed unrelated review comments including shell command security warnings and CodeRabbit bot messages — target=`none`, confidence=0.25
  - The cluster contains fundamentally different types of comments: (1) legitimate security concerns about shell commands with uncontrolled paths, (2) CodeRabbit bot status messages unrelated to code review, and (3) generic severity warnings. These are not instances of the same repeated pattern but rather noise mixed with one legitimate concern. The shell command issue appears only once substantively, making it insufficient for promotion.
- Various defensive programming issues in error handling and data validation across different modules — target=`none`, confidence=0.20
  - These are four distinct bugs in different subsystems (error message extraction, HTTP body handling, OpenAPI schema generation, and WebSocket subscription management) that happen to share a defensive programming theme but have no common underlying pattern, shared code path, or unified solution. They are one-off issues specific to their respective implementations.
- Various documentation and configuration issues across different packages and files. — target=`none`, confidence=0.30
  - These are fundamentally different issues: (1) import/export mismatches in example code, (2) hardcoded file paths in documentation, (3) language parsing in a code extraction tool, and (4) missing binary files and naming collisions. They share only surface-level similarity (all are 'potential issues') but address unrelated problems in different domains. No cross-cutting pattern exists.
- Various unrelated code quality issues across different packages and domains. — target=`none`, confidence=0.15
  - These five comments address fundamentally different problems: URLSearchParams serialization, generic type parameters, error handling in WebSocket procedures, DoS mitigation in path parsing, and cache validation in code generation. While all are labeled as potential issues, they have no shared underlying pattern, affect different code domains, and require distinct fixes. This is a false cluster of surface-level similarity rather than a genuine repeated pattern.
- Various unrelated code review issues across different packages and concerns — target=`none`, confidence=0.25
  - These four comments address fundamentally different problems: (1) QueryCache preservation logic, (2) package export naming convention, (3) test setup file duplication, and (4) HTTP header caching behavior. They share only surface-level similarity (all are 'potential issues') but involve completely different code domains, modules, and fix strategies. There is no repeated pattern or shared underlying principle that would benefit from centralized documentation.
- Cluster contains unrelated code review comments about different issues across multiple files and PRs. — target=`none`, confidence=0.15
  - These five comments address fundamentally different problems: a .prettierignore configuration issue, documentation about an archived library, missing router definitions in examples, missing guards in service routing, and stream detection logic duplication. They share only surface-level similarity (all are 'potential issues') but involve different codebases, different types of problems, and different solutions. This is not a repeated pattern but rather a collection of disparate review findings.
- Various linting and code style violations across different file types and domains. — target=`none`, confidence=0.15
  - These are four distinct linting issues affecting different file types (TypeScript, Markdown) and different domains (generated API code, tests, documentation). While they all involve linting rule violations, they address fundamentally different problems: TypeScript error suppression documentation, callback patterns, file naming conventions, and Markdown syntax. They are not instances of the same repeated pattern but rather separate lint violations that happen to appear in the same review period. Each is specific to its context and would not benefit from centralized documentation.
- Multiple distinct OpenAPI schema generation issues across different code paths and concerns. — target=`none`, confidence=0.30
  - These five comments address fundamentally different problems: (1) OpenAPI 3.1 nullable syntax, (2) empty schemas for recursive types, (3) nested description application with $ref, (4) symbol-based property name stability, and (5) allOf schema correctness. While all occur in the openapi package, they are separate bugs with different root causes and fixes, not a single repeated pattern. Promoting them would conflate unrelated issues.
- Various type safety and assertion issues across different packages and contexts. — target=`none`, confidence=0.30
  - These are fundamentally different issues despite surface similarity: (1) SSE generic type parameter mismatches in generated code, (2) React Query generic parameter ordering in type inference, and (3) function type assertion logic in proxy testing. They address distinct problems in different domains with no shared underlying pattern or principle.
- Mixed concerns about dependency pinning and version constraints in a monorepo versioning workflow. — target=`none`, confidence=0.40
  - The cluster conflates two distinct issues: (1) an intentional versioning behavior in scripts/version.ts that reviewers acknowledged as by-design, and (2) a separate peer dependency constraint tightening issue in packages/openapi/package.json. The first two examples are about the same intentional script behavior (dismissed by maintainers), while the third is about a different problem (minimum version constraint). This is not a repeated pattern requiring preservation—it's a mix of one-off concerns and an already-resolved design decision.
- Various concurrency and security concerns in CI/CD workflows and package scripts — target=`none`, confidence=0.30
  - These three comments address fundamentally different issues: (1) a security vulnerability with workflow_dispatch and secrets, (2) a concurrency problem with job serialization in GitHub Actions, and (3) a race condition in npm script execution. Despite surface similarity in discussing concurrency/parallelism, they are distinct technical problems requiring different solutions and affecting different systems. They do not represent a single repeated pattern.
- Various subscription and stream handling issues in different contexts — target=`none`, confidence=0.30
  - These three comments address fundamentally different problems: (1) temporal dead zone in callback capture, (2) missing observer completion forwarding, and (3) abort signal handling in SSE streams. While all involve async/subscription patterns, they are distinct bugs in different code paths with different root causes and fixes. They are not instances of the same repeated pattern.
- Multiple unrelated issues across codegen caching, dependency management, and version constraints in the openapi package. — target=`none`, confidence=0.30
  - These are distinct problems: (1) performance issue with getRouterInputsHash() re-traversal, (2) cache key incompleteness for tsconfig.json, (3) dependency classification error (openapi-types in devDependencies), and (4) peerDependency version bump. They happen to occur in the same package but address fundamentally different concerns with different solutions. No single durable rule or pattern emerges.
- Documentation examples and setup verification across multiple unrelated tRPC docs pages — target=`none`, confidence=0.30
  - These are distinct documentation issues affecting different pages (context setup, batch linking, aborting procedures, and redirects). While all involve tRPC documentation, they address fundamentally different problems: missing setup guides, incomplete code examples, missing configuration guidance, and broken redirects. They are not instances of a repeated pattern but rather scattered documentation gaps that happen to appear in the same review cycle.
- Mixed documentation and naming convention issues across different files and concerns — target=`none`, confidence=0.30
  - These four comments address fundamentally different issues: (1) documentation sync for error metadata, (2) filename naming convention exceptions, (3) filename casing violations, and (4) import ordering configuration. Despite similar severity levels and formatting, they are not instances of the same repeated pattern—they are distinct concerns in different domains (docs, skills, config). The cluster appears to be grouped by coincidence rather than genuine repetition.
- Various documentation and configuration inconsistencies across different files and domains. — target=`none`, confidence=0.30
  - These are fundamentally different issues: (1) toolchain version mismatch in .tool-versions, (2) missing dependency declaration in package.json, (3) prerelease versioning inconsistency in docs, (4) artifact version drift, and (5) documentation table count mismatch. They share only surface-level similarity (inconsistency/mismatch language) but address unrelated problems in different systems. None represent a repeated pattern that would benefit from durable storage.
- Three unrelated documentation issues involving markdown formatting, HTML rendering, and code example consistency. — target=`none`, confidence=0.30
  - These are distinct, one-off documentation problems affecting different files with different root causes: heading hierarchy in vanilla client docs, markdown-in-HTML rendering in README, and filename mismatch in nextjs adapter docs. They do not represent a repeated pattern or cross-cutting convention that would benefit from durable storage.
- Various dependency and configuration issues across different files and concerns — target=`none`, confidence=0.30
  - These are four distinct, unrelated issues: (1) CLI version pinning in CI workflows, (2) missing dependency declaration for a bin entry, (3) unused Prettier dependency, and (4) Node version constraint. They happen to appear in the same repo but address fundamentally different problems with different solutions. They are not a repeated pattern but rather a collection of one-off code quality observations.
- Mixed concerns about version detection heuristics, eslint suppression scope, and console logging patterns across different files. — target=`none`, confidence=0.30
  - These are three distinct issues with different underlying concerns: (1) semver detection strategy in version.ts, (2) eslint directive scope in codegen.ts, and (3) console logging bypass in generate.ts. While all touch on code quality, they address fundamentally different problems and would require different solutions. They are not instances of the same repeated pattern.
- Mixed cluster containing a specific typo fix (occured→occurred) and an unrelated incomplete TODO item. — target=`none`, confidence=0.30
  - The cluster conflates two distinct issues: (1) a specific typo in one error message that happens to be user-facing and test-asserted, and (2) an unrelated incomplete TODO in documentation. The typo is a one-off code fix, not a repeated pattern requiring durable documentation. The incomplete TODO is a separate editorial issue. Neither warrants promotion to repository memory.
- Various security and error-handling guards needed in different contexts — target=`none`, confidence=0.30
  - These are four distinct issues affecting different systems (service routing, stream detection, cache guards, CORS headers) with different underlying problems and solutions. They share only surface-level similarity (all mention 'guard') but address unrelated concerns in unrelated code paths. No cross-cutting pattern exists.
- Three unrelated code review comments about different issues in different files — target=`none`, confidence=0.30
  - These are three distinct issues: (1) a logic bug in cursor handling for infinite queries, (2) a test schema that masks the root issue, and (3) incorrect documentation about mutationOptions API. They share only surface-level similarity (all marked as issues in PR reviews) but address fundamentally different problems in different domains (implementation logic, test design, and documentation accuracy). They do not represent a repeated pattern that should be preserved in repository memory.
- Multiple unrelated architectural issues across error handling and type system components — target=`none`, confidence=0.30
  - These comments address fundamentally different problems: (1) object property collision in error flattening, (2) missing error registration context in WebSocket handlers, and (3) TypeScript type guard logic in intersection types. While all are marked as issues in the same PR series, they are distinct bugs requiring separate fixes, not a repeated pattern or convention.
- Two unrelated documentation issues: tRPC auth method deprecation and sidebar ID mismatches. — target=`none`, confidence=0.20
  - These are distinct, one-off documentation corrections in different files addressing different problems (auth security vs. sidebar configuration). They share only surface-level similarity as 'documentation fixes' but involve completely different domains and solutions. Neither represents a repeated pattern worth preserving.
- Type definition mismatches between runtime behavior and TypeScript annotations in different contexts — target=`none`, confidence=0.20
  - These are fundamentally different issues: the first is about conditional type support for keepAlive in Fastify plugin options, while the second is about missing Zod error code discriminators in OpenAPI schema. They share only surface-level similarity (runtime vs type mismatch) but address unrelated domains and require different solutions.
- Mixed issues about JSON Schema generation including $ref handling and tuple validation in OpenAPI generator — target=`none`, confidence=0.40
  - These are distinct technical issues affecting different parts of the OpenAPI generator (recursive $ref handling, JsonValue fixture validation, and tuple schema format), not a repeated pattern. They appear in the same PR but address separate bugs rather than a shared convention or practice.
- Two distinct ESLint configuration issues: flat config API usage and reportUnusedDisableDirectives format consistency. — target=`none`, confidence=0.30
  - These are two separate, unrelated issues. The first is about using the correct flat config API for react-hooks (a specific library migration issue). The second is about consistency in reportUnusedDisableDirectives formatting across different config files. They happen to appear in the same PR but address fundamentally different problems. Neither is a repeated pattern—they are one-off configuration issues specific to these particular files and ESLint versions.
- Two unrelated test concerns: OpenAPI schema intersection handling and proxy method chaining behavior. — target=`none`, confidence=0.20
  - These comments address fundamentally different issues in different domains (OpenAPI schema generation vs. proxy object chaining). The surface similarity is coincidental—both mention test assertions and current behavior limitations, but they concern distinct technical problems with no shared underlying pattern.
- Two unrelated TypeScript issues in different packages with different root causes and fixes. — target=`none`, confidence=0.95
  - These are fundamentally different problems: one is about overly broad predicate matching in computed property detection (packages/openapi), the other is about type safety in object spread operations (packages/react-query). They happen to use similar warning language but address distinct code issues in separate packages with no shared pattern or principle.
- Various async/await and error handling issues in different contexts — target=`none`, confidence=0.30
  - These three comments address fundamentally different problems: (1) missing error handling around server.listen(), (2) React hooks called in async functions (a rules violation), and (3) unawaited async function in a Node script. While they all involve async code, they are distinct issues with different solutions and contexts. They are not instances of the same repeated pattern.
- Multiple unrelated Vitest configuration and dependency issues across different files — target=`none`, confidence=0.30
  - These are three distinct problems: (1) a named export vs default export issue in globalSetup.ts, (2) a peer dependency version mismatch between vitest and its plugins, and (3) a postinstall script referencing files not included in the published package. While all involve Vitest, they address fundamentally different concerns and would not benefit from unified documentation. Each is specific to its context and unlikely to recur as a pattern.
- Two unrelated workflow configuration issues in GitHub Actions files — target=`none`, confidence=0.30
  - These are distinct problems: one about branch naming collisions in check-skills.yml and another about unused workflow inputs in release-manual.yml. They happen to appear in similar workflow contexts but address fundamentally different issues (naming strategy vs. input validation). Not a repeated pattern.
- Documentation clarity issues in different contexts — target=`none`, confidence=0.30
  - These are two distinct documentation problems: one about ambiguous version alignment terminology in API docs, another about a content-type format mismatch in streaming documentation. They share only surface-level similarity (both are doc clarification requests) but address fundamentally different issues with no underlying pattern to preserve.
- Regenerate pnpm-lock.yaml when package.json dependency specifications are updated to keep lockfile in sync with CI requirements. — target=`pr_only`, confidence=0.75
  - These are one-off comments about specific PRs where dependency versions were changed without regenerating the lockfile. While the pattern is real, it's a procedural reminder tied to specific code changes rather than a durable convention. The second example about @tanstack/query-core version pinning is a distinct issue about dependency management strategy, not the same pattern.
- TypeScript expect-error comments need proper documentation or correction — target=`none`, confidence=0.30
  - These are two distinct issues: one about auto-generated code lacking required ESLint comment reasons (infrastructure/tooling), and another about an incorrect comment description (typo/accuracy). They do not represent a repeated pattern of the same underlying problem that would benefit from durable documentation.
- Non-null assertions should be replaced with guards or optional chaining in TypeScript code. — target=`none`, confidence=0.40
  - While both comments mention non-null assertions, they address fundamentally different issues: one is about array/regex safety in a build script, the other is about environment variable validation in application code. The underlying problems, contexts, and solutions are distinct. This is surface-level similarity rather than a repeated pattern. Additionally, avoiding non-null assertions is already a standard TypeScript best practice documented in most style guides.
- Two unrelated import issues in different files with different underlying problems. — target=`none`, confidence=0.30
  - These are distinct, one-off issues: the first is a missing import in documentation example code (SKILL.md), the second is an import statement formatting violation in actual source code. They happen to both involve imports but address fundamentally different problems in different contexts. Neither represents a repeated pattern worth preserving as institutional knowledge.
- Two unrelated bug reports about different technical issues in different parts of the codebase. — target=`none`, confidence=0.95
  - These are two distinct bugs affecting different systems (error handling in TRPCError vs. stream processing in SSE). They happen to both be labeled as 'Major' issues but address fundamentally different problems with different root causes and solutions. They are not a repeated pattern but rather separate code review findings.
- Multiple distinct issues about dependency version mismatches and reproducibility in a monorepo setup — target=`none`, confidence=0.30
  - These are three separate problems: (1) vitest version inconsistency across workspaces, (2) pnpm version mismatch between package.json and .tool-versions, and (3) npm version pinning in CI workflows. While all relate to dependency management, they address different root causes and require different fixes. They are not instances of a single repeated pattern but rather separate one-off issues discovered during a dependency upgrade effort.
- Missing imports in test files cause runtime errors. — target=`none`, confidence=0.95
  - These are two distinct issues: (1) a missing vitest import in a test file, and (2) an unused import in an MDX file. While both involve imports, they affect different file types, have different root causes, and require different fixes. The first is a test setup error; the second is cleanup after content removal. They do not represent a repeated pattern of the same underlying problem.
- Documentation and PR description quality issues in openapi package — target=`none`, confidence=0.30
  - These are two distinct issues: one about incomplete TODO text in README.md, another about incomplete PR description template text. They happen to both involve incomplete/placeholder text but target different artifacts (README vs PR description) and require different fixes. Not a repeated pattern worth preserving.
- Mixed issues: CLI error message prefixing, OpenAPI version documentation mismatch, and file path comment accuracy. — target=`none`, confidence=0.30
  - These are three distinct, unrelated issues affecting different files and systems. Example 1 is about error message formatting in CLI parsing. Example 2 is about documentation-code version mismatch in OpenAPI generation. Example 3 is a duplicate of Example 2. Example 4 is about a file path comment in a YAML artifact file. They share no common underlying pattern—they are one-off code quality issues specific to individual PRs and files, not a repeated cross-cutting concern or architectural pattern worth preserving in repository memory.
- Two unrelated issues: debugger statement removal and string escaping in CodeQL — target=`none`, confidence=0.30
  - The cluster conflates two distinct problems. Example 1 is about removing a debugger statement from scripts, while Example 2 is about incomplete string escaping detected by CodeQL. These are fundamentally different issues with different remediation paths and do not represent a repeated pattern.
- OpenAPI version mismatch and serializer handling issues in generated code — target=`none`, confidence=0.30
  - These are two distinct issues: (1) a schema generation version mismatch between declared OpenAPI 3.1.1 and 3.0-style nullable handling, and (2) a separate bug in Date/bigint serialization in helper functions. They happen to appear in the same PR but address fundamentally different problems with different solutions. The first is an architectural decision about OpenAPI compliance; the second is a specific serializer bug. They should not be conflated into a single pattern.
- Optimize getRouterInputsHash() to compute shared input hash once instead of per-router to avoid O(routers × files) IO complexity. — target=`pr_only`, confidence=0.85
  - This is a specific performance optimization suggestion for a single file (packages/openapi/test/scripts/codegen.ts) in a particular PR. The issue is localized to one code path and one PR context. While the pattern is repeated within the same PR, it does not represent a cross-cutting convention, architectural decision, or testable invariant that would benefit future code or other parts of the repository. The fix is straightforward refactoring specific to this implementation.
- Two unrelated comments about OpenAPI type compatibility and TypeScript generic constructor style. — target=`none`, confidence=0.30
  - These are fundamentally different issues: the first is about missing OpenAPI schema field compatibility, the second is about TypeScript linting style. They happen to appear in the same PR but address different concerns with no underlying pattern.
- Three distinct bugs in HTTP subscription link implementation and documentation — target=`none`, confidence=0.30
  - These are three separate, unrelated issues: (1) null handling in connectionParams callback, (2) incorrect data wrapping for SSE resume, and (3) documentation listing wrong API options. They happen to appear in the same PR but address different code paths and problems. Promoting them would conflate unrelated concerns.
- Two unrelated issues: one about cache route matching logic and another about workflow path configuration. — target=`none`, confidence=0.30
  - These are fundamentally different problems. The first is a caching logic bug (using includes() instead of startsWith()), while the second is a workflow configuration error (wrong path pattern). They happen to appear in the same PR but address distinct technical domains with no shared pattern or principle.
- Avoid parameter destructuring and limit function parameters to 3 maximum. — target=`none`, confidence=0.30
  - These are two distinct coding conventions that happen to appear in the same PR but address different issues: one about destructuring patterns in callbacks, another about function parameter count. They are not genuinely repeated patterns—the first example shows destructuring avoidance in query handlers, while the second is about refactoring a function signature with too many parameters. Without evidence of the second pattern repeating across multiple PRs or files, it appears to be a one-off refactoring suggestion rather than a systemic issue.
- Three distinct code quality issues in OpenAPI test fixtures and generated code — target=`none`, confidence=0.20
  - These are three separate, unrelated bugs affecting different files and systems: (1) test fixture using wrong OpenAPI version, (2) incorrect percent-encoding in URL path generation, (3) event handler calling convention mismatch. They share only surface-level similarity (all marked as major issues in the same PR) but address fundamentally different problems with no common underlying pattern or principle.
- Header comments in artifact files contain incorrect path references that should be corrected to match actual file locations. — target=`pr_only`, confidence=0.85
  - This is a specific issue affecting a particular file (_artifacts/skill_tree.yaml) in a single PR. While the pattern of incorrect header comments is valid, it appears to be a one-time documentation/maintenance issue rather than a systemic coding convention or architectural principle that would recur across the codebase. The fix is straightforward (update the comment) and doesn't represent a broader pattern requiring durably stored guidance.
- Shell commands depending on uncontrolled absolute paths in test files — target=`none`, confidence=0.30
  - The cluster appears to be duplicate or truncated entries from the same PR and file, not genuinely repeated patterns across different contexts. Without distinct examples showing this issue appearing in multiple PRs or files, this cannot be classified as a durable pattern worth preserving. Additionally, this appears to be a linting/security tool output rather than a substantive code review comment requiring documentation.

---
