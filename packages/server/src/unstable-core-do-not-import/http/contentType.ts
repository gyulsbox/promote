---
applyTo: "packages/server/src/unstable-core-do-not-import/http/**"
---
# 경로 내 쉼표 개수 제한 규칙
- `opts.path.split(',')` 호출 전에 경로 내 쉼표 개수를 직접 세어 제한하는 로직을 구현한다.
- 쉼표 개수 제한을 초과하는 경우 요청을 즉시 차단하거나 오류를 반환한다.
- 정규 표현식 또는 문자열 검색 방법을 활용하여 쉼표 개수 검사를 수행한다.
- `maxBatchSize`와 별개로, 경로 내 쉼표 개수에 따른 DoS 공격 방지 로직을 명확히 분리한다.
- 보안 강화를 위해 경로 길이와 쉼표 개수 모두를 검증하는 유틸리티 함수를 재사용한다.
