---
applyTo: "packages/**/generate.test.ts"
---
# 환경 변수로부터 절대 경로 사용 규칙
- 환경 변수로부터 가져온 절대 경로를 사용하는 셸 명령어는 제어되지 않은 경로 의존성을 초래할 수 있으므로, 경로를 검증하거나 정적 경로로 대체할 것
- 테스트 파일 내에서만 사용하는 경우, 경로를 하드코딩하거나 안전한 경로로 제한하여 사용하도록 유도할 것
- 환경 변수 값이 신뢰할 수 없는 경우, 절대 경로를 사용하는 셸 명령어를 실행하기 전에 검증하는 유틸리티를 활용할 것 (예: `realpath`, `test -d`)
- 경로 의존성을 최소화하기 위해, 환경 변수 대신 정적 또는 구성 파일 기반의 경로를 사용하는 방안을 고려할 것
- 관련 유틸리티 또는 패턴 예시: `readlink -f`, `test -d`, `pushd/popd` 등을 활용하여 안전한 경로 처리 방식을 적용할 것
